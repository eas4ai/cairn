DECISION

Question:   Which contract change should the next specification phase bring in: evidence that names the kernel that wrote it, one evidence record per run, or two mechanisms proving one requirement? The backlog is empty and the hooks are built.
Recommend:  Specify evidence-names-the-kernel-that-wrote-it next: every evidence record and review carries the running kernel's digest, wake treats a record written under a different kernel as stale and says so, and the working agreement says the kernel is upgraded at Done, never inside a commitment.
Because:    This session ran a whole commitment's verdicts through the production checkout's older kernel before noticing, exactly the failure the item records; with the hooks now registered from a checkout path, the kernel in use is decided by configuration, and a digest on each record is what makes a mismatch visible.
If wrong:   If evidence provenance is not the priority, one-record-per-run-rather-than-one-per-requirement cuts the evidence commit noise you see on every check, at the cost of a record format change; two-mechanisms-that-both-prove-one-requirement changes assess from one mechanism to a set and can wait for an adopter who needs it.
Instead:    instead one-record-per-run-rather-than-one-per-requirement, or instead two-mechanisms-that-both-prove-one-requirement. An ok starts a specification phase for the kernel-digest item in which you confirm each falsifier before anything is built.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: LOOP-091
Status: open
Raised: 2026-09-14T23:36:27.783Z
Raised after: LOOP-091=7
