DECISION

Question:   The 0.7.0 release notes open with a bullet about the stop hook that describes what 0.6.0 already shipped, not what changed in 0.7.0. I wrote that bullet and it is wrong: bin/hooks/ and bin/hook.mjs are byte-identical between the v0.6.0 and v0.7.0 tags, and the 0.6.0 entry already lists all five of those behaviours (PKG-018, PKG-033, PKG-021, PKG-044, PKG-043, LOOP-139). The same text is the v0.7.0 tag message, so it is the first thing someone upgrading reads. Nothing is pushed yet, so nobody has seen it. Do you want me to re-cut 0.7.0 with that bullet corrected, or ship it as it stands with the error recorded?
Recommend:  Re-cut it. Delete the v0.7.0 tag, correct the entry so the first bullet names what 0.7.0 actually adds, and run the release again.
Because:    Nothing has been pushed, so the whole cost of a re-cut falls on me and none of it on a user: one more independent review round of about twenty minutes, plus re-running the checks. Shipping as it stands means the first line of your release notes, and of the tag message, tells people a fix arrived in 0.7.0 that arrived in 0.6.0. The rest of the entry is accurate; the fortieth reviewer checked every other claim against the kernel and they held. If this isn't clear, ask me to explain it another way before you decide.
If wrong:   If I re-cut and you did not want the delay, you lose about half an hour and nothing else; if I ship as it stands and you did want it fixed, the wrong bullet is public in the tag message and correcting it later needs a 0.7.1.
Instead:    Answer instead ship and I will leave the tag alone, record the wrong bullet as a known limit in the review, and push 0.7.0 as it stands when you give the word.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: PKG-039
Raised: 2026-09-18T11:10:08.803Z
Raised after: PKG-039=88
