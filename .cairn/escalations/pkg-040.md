DECISION

Question:   Where should the v0.7.0 tag go, or should the release be 0.7.1? PKG-040 says a release is one commit that sets the version and carries that version's changelog entry. No commit does both any more: 96f60acb set the version but holds the changelog entry you told me to drop the stop-hook bullet from, and the corrected entry landed two commits later. The release script cannot redo it, because it compares against package.json, which already reads 0.7.0. Three ways out. First, tag the finished commit: anyone checking out v0.7.0 gets every version file reading 0.7.0 and the corrected notes, but the tagged commit is not itself the one that set the version, so it meets PKG-040 in effect and not in letter. Second, release it as 0.7.1 by the shipped script, which is fully correct and burns the 0.7.0 number, which nobody has seen. Third, undo the release commit and cut 0.7.0 again, which is fully correct and keeps the number.
Recommend:  Release it as 0.7.1 by the shipped script. Rename the changelog heading to 0.7.1, leave it uncommitted, and run scripts/release.mjs, which commits the version and the entry together and tags it.
Because:    Cairn's whole claim is that the record matches what happened, so its own release should satisfy its own requirement rather than nearly satisfy it. Of the two correct routes, 0.7.1 costs one more independent review round, about half an hour with nothing for you to do, and undoing the release commit to keep the 0.7.0 number costs two, because the undo itself needs a round before the script will run. The number is the cheapest thing to give up: nothing carrying 0.7.0 has left this machine, so no one will ever look for it. If this isn't clear, ask me to explain it another way before you decide.
If wrong:   If you take the tag on the finished commit and later care about the letter of PKG-040, the fix is a fresh version, which is what option two does now; if you take 0.7.1 and wanted the 0.7.0 number kept, that number is gone for good, since versions only go up.
Instead:    Answer instead tag and I will put an annotated v0.7.0 on the finished commit with the corrected entry as its message, record the deviation from PKG-040 as a named limit in the review, and push on your word; answer instead recut and I will undo the release commit and cut 0.7.0 again, at two more rounds.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: PKG-040
Raised: 2026-09-18T13:27:35.155Z
Raised after: PKG-040=93
Answer: instead Re-cut it as 0.7.0. The developer answered "ok" to a message whose recommendation was the re-cut: revert the version files, lift the entry, reach Done, and let scripts/release.mjs write the version and the entry in one commit, which keeps the 0.7.0 number and satisfies PKG-040 in full. The escalation's own ok line offered 0.7.1, so this records the reading rather than the letter: the number is not spent until the script runs, two rounds away, and the developer has been told that switching to 0.7.1 before then costs nothing.
Answered: 2026-09-18T14:03:15.607Z
Answered after: PKG-040=93
Answered order: 9
