commitment: evidence-remains-tied-to-the-checked-state
commit: ccd4d5c
examined:
  - Mechanism construction against the original kernel at c53ef3f: 38 focused tests ran, 35 failed on the reported behaviors and three existing-behavior controls passed. No command startup error was mistaken for a caught product assertion; the deletion/submodule cases asserted the unwanted fatal exit explicitly.
  - The original failures covered changing inputs and HEAD, executable and link kind identity, both missing and corrupt logs, absent receipt output fields, concurrent ownership, malformed declarations, unsupported gitlinks, and 206 Git processes for 100 identical input lists.
  - During implementation, four additional failing probes exposed inconsistent Unicode path ordering, Git flags hiding uncommitted contents or modes, and a live legacy kernel action. Each failure was observed before its correction. Separate-worktree execution and error cleanup were successful controls.
  - The focused corrected suite reached 43 passing tests. The complete 219-test suite passed against committed code at e3eeec8 through the repaired local kernel. Its receipt and complete logs are tracked; package and specification mechanisms also produced current passing evidence.
  - Reviewed the implementation diff without changing code: both candidate boundaries use fresh input state, starting entries are compared with the commit even when Git hides changes, and rejected candidates retain logs without receipts. The original input digest is used for accepted evidence.
  - Reviewed identity consistency: both views hash a version marker, path, normalized Git mode, and content digest in the same order. Link target paths remain identities; executable changes and file-to-link changes are distinct. Unicode ordering, large blobs, historical selections, and shared inputs have successful regression coverage.
  - Reviewed evidence durability: combined and stderr logs have separate digests, missing or corrupt latest output names a rerun, historical receipts stay unchanged, and shared output is hashed once per wake in bounded chunks. Ordinary signal and per-requirement reporting retain their existing meanings.
  - Reviewed execution ownership: exclusive creation is separate from the workflow record, ownership is per Git worktree, ordinary exceptions release the lock, and incomplete/dead ownership is never reclaimed automatically. A live legacy kernel action also prevents execution. Eight simultaneous checks in an isolated fixture produced exactly one execution and one receipt; seven were refused. Killing a supervising process while its command survived kept new checks blocked until the command finished and its lock was explicitly reconciled; wake then recovered the dead in-progress record and a new check passed.
  - Repeated the review benchmark: 1, 10, 50, and 100 identical mechanism inputs each used eight Git subprocesses. The 100-mechanism wakes took 52, 50, and 47 ms, compared with the original 439, 777, and 589 ms and 206 Git subprocesses. This is one machine's fixture measurement, not a latency guarantee for large repositories.
  - Reviewed declaration validation and agent/human guidance: missing required fields and unsupported populated or unpopulated gitlinks are named before execution; scalar one-item declarations remain supported. The working agreement and shipped template match, and upgrade guidance explains the one-time evidence refresh.
  - Ripwire edit checks found no incompatible callers; inputsDigest adds a defaulted cache argument and check keeps its interface. Quality review reduced the new nesting and declaration complexity. Remaining tool flags concern historical churn, small complexity increases, and extracted internal functions. The root test gate listed Markdown symbols and did not establish coverage for spawned CLI processes; the executed integration suite supplies that verification instead.
  - Limits remain explicit: boundary validation cannot detect edits restored between observations, external runtime state needs the mechanism's judgment, Git submodules are refused rather than modeled, and hostile repository tampering is outside Cairn's trust model. Historical Git blob batches still buffer selected contents; the output verifier itself uses bounded reads. Cross-platform execution was not claimed from this Linux run.
  - Final self-audit: changes match the authorized repair and declared footprint, preserve unrelated user files and receipt history, keep the kernel dependency-free and under its size ceiling, record decisions and demonstrations, and have no unresolved finding from this review.
findings:
  - resolved: all six reproduced findings and repeated input reads are repaired and verified
