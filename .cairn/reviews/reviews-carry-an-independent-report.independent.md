commitment: reviews-carry-an-independent-report
commit: d68b89da
reviewer: I had none of the build's context, read no diff and no session history, and worked read-only in my own clone of the repository at d68b89da; I wrote nothing in the project checkout.
examined:
  - the gate at d68b89da, read as source (bin/cairn.mjs: independentGap, reviewOf, listOf, headerOf, the finding-prefix test, withoutFences in bin/spec.mjs) and then judged only by what `node bin/cairn.mjs wake` actually did
  - 52 review-and-report pairs built, committed and put through `node bin/cairn.mjs wake` in my clone at d68b89da; setup first: a clean pair with one carried finding reaches Done, and a pair whose lists are both declared empty reaches Done, so the harness can tell acceptance from refusal
  - every silent case below paired with a control that differs in one detail and is refused
  - `node --test tests/*.test.mjs` at d68b89da in a separate pristine clone: 527 tests, 527 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, exit code 0
  - all 68 records in .cairn/reviews/ at d68b89da put through the record reader one at a time: 59 accepted, 9 refused; the nine are five for a line in the examined list the reader cannot read, two for a one-word value where the list belongs, one for a heading whose title names findings, and one for a prefixed line under a heading, which matches the limit accepted on 2026-09-18
  - the carry and citation rules: a citation not last on its line, two review lines citing one finding, one review line citing two, a citation numbered past the report's count, and a finding no line cites are each refused and named precisely; sound, with one soft edge noted below
  - the commit comparison: report and review must resolve to the same commit, short and full forms agree, a report naming another commit is answered with a new report rather than a repair; sound
  - an uncommitted, modified, deleted or emptied record: each is named as differing from its committed version before either record is judged, and a committed empty or whitespace-only report is named as empty; sound
  - LOOP-032 and LOOP-058 freshness: the untouched wake at d68b89da names the review at 5a198efe as stale against a changed declared input, and a review pointed at the repository's root commit is refused because its reviewed requirement text is unavailable; sound
  - the findings-naming heading: caught with and without a space after the hash, and a record title with no space after its hash, or underlined instead, is a title to both parsers; sound
  - the examined list's own reading: a prefixed bullet there is refused rather than absorbed, a stray field line inside it is named with the right fix, and the list ends at the findings field written at the margin; sound except for the gap in finding 1
  - fences: a fence anywhere between the findings field and the end of the header is refused, an unclosed one too, a fenced example below the first heading is correctly ignored, and a second findings field below the list is refused while a fenced one is not; sound
findings:
  - The prefix test allows up to four characters that are neither letter nor digit between the word and its colon, so a digit inside that gap escapes it entirely, and a reviewer who numbers findings loses them with nothing named. A report bullet reading the word open, then a space, then the digit 2, then a colon, then a defect, placed under a plain heading, reaches Done at d68b89da; the identical line with the digit and space removed is refused as a prefixed line under a heading. The same shape is lost in two more places: written as an entry of the report's examined list it is silently counted as a thing examined (the same entry without the digit is refused), and written under a heading in the review record itself it is an unresolved finding that never reaches LOOP-033, so the wake reports Done while the review's own words say the defect stands. Inside the findings list the shape is harmless, because the entry is still read and still has to be carried; the loss is only outside it. (silent)
  - Honest text is refused when an angle-bracketed placeholder sits immediately before a colon, because the line is read twice and the second reading removes only the angle brackets, joining the word to the colon across a placeholder that Markdown hides when rendered. A report bullet reading "the report prints", then a placeholder in angle brackets containing the word open, then a colon, then "three states for the flag" is refused, and the message claims a prefixed line sits under a heading when nothing a reader sees carries one; the tag-stripped reading of that same line correctly finds no prefix. The same line as an examined entry is refused too, and there the message instead says the examined list needs to be nonempty, which points the writer at the wrong repair. (blocked)
  - A prefixed line below the findings list is still answered with a repair of the reviewer's report, not with a new report, so the round's fourth change only reaches half the cases. The same prefix above the lists gives the action "review <slug>" and asks for a fresh reviewer, while under a heading it gives the action "repair .cairn/reviews/<slug>.independent.md" - and that repair's own fix text says to ask the reviewer again, while the sibling message says never to edit a reviewer's report to fit. The named action contradicts both sentences, and an agent that obeys the action edits a file the gate forbids it to edit. (wording)
  - The test that the report names the review's commit somewhere other than its commit line reads the whole file, fences included. A report whose only other mention of d68b89da sits inside a fenced shell command reaches Done, so a report carried over from an earlier round can pass the "written for this review" test with a pasted command block rather than with anything the reviewer examined. (wording)
  - A review line carries a report finding when it merely begins with the finding's words, compared without a word boundary, so a line about something else can satisfy the carry when the finding's last word is a prefix of the line's next word: a report finding ending in a single letter is accepted by a review line that continues that letter into a different word and then denies the defect. The words are still on the page, so nothing vanishes, but the test is weaker than "begins with its words" reads. (wording)
