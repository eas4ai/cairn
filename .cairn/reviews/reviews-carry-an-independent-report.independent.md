commitment: reviews-carry-an-independent-report
commit: f6bdf509
reviewer: a nineteenth independent subagent given only the commitment, LOOP-020 with LOOP-086/LOOP-071/LOOP-032/LOOP-033/LOOP-108, the AGENTS.md record paragraphs and commit f6bdf509, working read-only in its own scratch clone with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 and its falsifier, LOOP-086, LOOP-071, LOOP-032, LOOP-033 and LOOP-108 in docs/spec/loop.md, and the AGENTS.md paragraphs on the review record, its lists and the independent reviewer, all at f6bdf509; AGENTS.md is byte-identical to skills/new-project/templates/AGENTS.md
  - the code at f6bdf509: listOf, ENTRY, FINDING, entryFix, reviewOf and independentGap in bin/cairn.mjs, withoutFences and recordFields, and the order in which the wake chooses repair, resolve, review, moved-freshness and Done
  - tests/independent.test.mjs and tests/record-boundaries.test.mjs at f6bdf509; the whole suite in the scratch clone is 516 tests, all pass, and `node bin/cairn.mjs wake` in the clone names `review reviews-carry-an-independent-report` because the review examined 69ec524c and a declared input has changed
  - the eighteenth round's five shapes retested as concrete records built at f6bdf509 and run through `node bin/cairn.mjs wake`, not read from the diff: all five hold; a capital `Findings:` or `FINDINGS:` line inside examined: is now named with "and the entries after it are unread" instead of reaching Done; the truncating `Findings: none yet` line is named; a record with findings: above examined: is told "write examined: above findings:, both above the first heading" on both the review and the report side; a line indented under a finding that itself begins `open:` is named, and AGENTS.md's reworded sentence now states that exception, while an ordinary indented continuation still joins and reaches Done; and a review line citing `(independent <sha> 7)` against a one-finding report is named
  - about forty further hand-built review and report records run through the wake at f6bdf509: citation numbers 0, 000, 2, 7, 99 and repeated citations, examined/findings boundary variants including `findings:[]`, setext-underlined headings, a leading `# Title`, blank-line continuations, terminated and unterminated fences, a report naming its commit only inside a fence, a duplicated `commit:` line, a report whose findings sit under an ATX heading, and an empty report finding
  - the committed-state reads at f6bdf509: an uncommitted edit, an on-disk deletion and an untracked version of each record, plus a committed open finding erased only on disk and a committed uncarried finding carried only on disk; every one is named `commit <path>` or `resolve`, never Done
  - the freshness rule with a mechanism whose inputs are src/exit, src/other and docs/manual.md with docs/manual.md declared as a document: a documents-only change costs one check and keeps the review fresh, a change to src/other costs a check and then names `review first`
  - all 67 committed review records in .cairn/reviews read through the reader as it stands at f6bdf509, each installed as the current commitment's review with only its commit: line rewritten to the fixture's HEAD; 61 are read (Done, or the independent gate) and 6 are refused: command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md and skills-can-be-installed-with-the-skills-cli.md because a later field (`verification:`, `initial finding:`) sits inside the examined: list and is named as a line the loop cannot read, and the-answer-reaches-the-agent.md and the-wake-names-an-action-or-refuses.md because their only finding entry is the word "none", which is an unrecognized prefix; all six are legacy records for past commitments, which no gate reads again
findings:
  - A review line citing finding number 0 reaches Done, so the eighteenth round's citation rule still lets a review claim support the report never gave: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n  - resolved: my own, fixed (independent <short> 0)" against a report holding one finding gives "Done: first ... the review at <sha> and its independent report are clean", while the identical record with 2 in place of 0 gives "the review cites (independent <short> 2), and the report holds 1 finding"; the cause is that `beyond` is computed with Array.prototype.find and 0 is also the sentinel for another round's citation, so `if (beyond)` is false for a real citation 0, and "(independent <short> 000)" behaves the same.
  - A findings field written `findings:[]` with no space is read as an empty findings list by the field reader but does not end the examined: list, so the record is refused: "examined:\n  - x at <sha>\nfindings:[]" gives "examined: holds a line the loop cannot read as an entry: \"findings:[]\"", which contradicts AGENTS.md's "An `examined:` list ends at `findings:`", and the report side gives the same refusal.
  - The reorder advice is offered for any unread line that begins `examined:` or `findings:` in any case, so it names a fix the record already has: "examined:\n  - x at <sha>\nFindings:\n  - open: a real defect that nobody resolved\nfindings: []" is refused with "write examined: above findings:, both above the first heading" although examined: is already above findings: and both are above the first heading, and the same advice is given for an "Examined: also the tests" note inside a findings list and for `findings:[]`; the diagnosis is right but the only fix, bulleting or removing the line, is named nowhere.
  - A setext-underlined heading does not end the header, so an honest record whose prose sits under one is refused and told to do what it has already done: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n\nAttacked\n--------\n\n- the reader" gives "findings: holds a line the loop cannot read as an entry: \"Attacked\", and the entries after it are unread; write each finding as a - entry, and put prose after a heading", and the report side gives "put a heading above prose that follows the findings"; AGENTS.md says only "heading", and tests/record-boundaries.test.mjs itself treats "Example\n-------" as one of the section forms records use.
  - A blank line before an indented continuation breaks the join the agreement promises: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n\n    I reproduced it in a scratch clone." is refused with "findings: holds a line the loop cannot read as an entry: \"I reproduced it in a scratch clone.\"", the same shape in a report and in examined: is refused the same way, and the identical record without the blank line reaches Done, while AGENTS.md says "a line indented under an entry joins it" and "Blank lines between entries are nothing" and Markdown reads that line as the entry's own paragraph.
  - A report whose `commit:` line is duplicated passes the check that is meant to catch a re-dated report: "commitment: first\ncommit: <sha>\ncommit: <sha>\nreviewer: r\nexamined:\n  - the kernel\nfindings: []" reaches Done, although the report names the commit nowhere but its commit: lines, because only the first `^commit:` line is stripped before the text is searched; the same report with one commit: line gives "does not name commit <short> anywhere but its commit: line, so it was not written for this review".
