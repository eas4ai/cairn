commitment: reviews-carry-an-independent-report
commit: 36411289
reviewer: a fresh agent with none of the build's context, which read only the commitment file, the five requirement texts and AGENTS.md, and worked read-only in two scratch clones of its own; the project checkout at /home/shawn/workspace2/cairn-dev was never written to
examined:
  - the gate at 36411289, as about forty concrete review/report pairs committed on a probe branch of my own clone and judged by `node bin/cairn.mjs wake`; every claim below is a wake run, never a reading of the diff
  - the setup first: a pair with one carried finding and a `findings: []` pair both reach Done at 36411289, and every silent case below is paired with a control record that differs in one detail and is refused
  - rule 1, the markup stripper: about a hundred wrapper and marker shapes round `open:` screened through the kernel's own `bare` and `saysFinding` (extracted from bin/cairn.mjs lines 903-989 into a throwaway module), then the survivors confirmed by wake, in the report, in the review, in a table cell, as a list entry, as a nested bullet and as an examined entry
  - rule 2, the fence: seventeen opener/closer shapes swept through the kernel's own `withoutFences` for both directions (an example no longer hidden, and a marker that swallows), then three swallows confirmed by wake
  - the carry and citation rules: no citation, a citation not last on its line, two lines citing one finding, one line citing two, a number beyond the report, and a review line that does not begin with the finding's words - all six named, each message saying which finding and why
  - the commit comparison: a report at another commit, a report that names the commit only on its commit: line, a report for another commitment, and fields pushed below a heading - all four named; the review's own commit missing (LOOP-108), invalid (LOOP-058) and stale by a changed declared input (LOOP-032, review at 815cab13 against a tree at HEAD) - all three named
  - a record out of step with its commit: the report modified on disk, deleted from disk, committed empty and committed as a deletion, and the review modified on disk - all five named, the first two as `commit <path>` in one state
  - the findings-named heading over its span: "Findings in detail" holding only prose beside a nonempty list reaches Done, while a bullet in its subsection is named, so the span is judged, not the first level
  - `node --test tests/*.test.mjs` at 36411289: 530 tests, 530 pass, 0 fail, 0 cancelled, 0 skipped, 39.0s
  - all 68 files in .cairn/reviews/ at 36411289 read through the kernel's own reviewOf: 61 accepted, 7 refused - three of the seven (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, plus skills-can-be-installed-with-the-skills-cli, four in all) put a `verification:` field inside the examined list, named as a line the loop cannot read as an entry; two (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses) write `- none` as finding 1, named as unrecognized; one is reviews-carry-an-independent-report.independent.md, refused only because a report read as a review needs the open:/resolved: prefix its own gate does not ask for, which is an artifact of my audit, not a defect
findings:
  - open: under a heading, a bulleted finding whose words begin with a label is read as prose and the wake says Done. `- Blocked: open: the gate refuses an honest examined entry` and `- silent: open: a report finding under a heading is read as prose` under `## Notes` both reach Done in the report, and `- Blocked: open: the citation regex needs one space` under `## Notes` reaches Done in the review, where the same bullet without the label (`- open: ...`) is refused in both records. The cause is that the label branch of saysFinding applies its `^[A-Za-z][\w -]*:` strip to the raw cell rather than to the stripped text, so it fires only when no markup stands before the label: as a list entry the bullet is already gone, so `- silent: open: X` in examined: IS named, but as a bullet under a heading the same text is prose. `> Note: open: X`, `| Note: open: X |`, `- **Blocked:** open: X`, `- Note (1): open: X` and `- Finding #1: open: X` are hidden the same way, and any label holding a character outside `[\w -]` is hidden even unmarked. A reviewer asked to rate findings writes exactly this shape (silent)
  - open: one unpaired fence marker blanks every line to the next marker, and nothing is named. A report with two blocks opened ```` ```console session ```` and a real `- open: the second wake still names review after the fix, so the gate loops` between them reaches Done with that finding gone; the same record with ```` ```console ```` is refused. An info string with a space does not open a fence, so its closing marker opens one instead, and the next recognised marker closes it over the intervening lines. Two stray closing markers, from output pasted with the opener forgotten, do the same and also reach Done. Swept through withoutFences, six shapes leave an unpaired marker this way: an info string with a space, an opener indented four spaces with its closer at the margin, the reverse, a closer carrying trailing text, a marker on a bullet line, and an opener longer than its closer. Every one of them both un-hides the example it was meant to hide and swallows what follows (silent)
  - open: a decorative marker that is not a code fence hides its whole span. A report using `~~~~~~~~~~~~~~~~~~~~~~~~` twice as a section divider, with `## What I found` and `- open: the carry gate accepts a citation for a finding the report never made` between the two dividers, reaches Done; the section is blanked and no message names it. The same two dividers in the review hide `- open: the citation regex needs one space, and I have not fixed it`, so the wake says Done while the review carries an unresolved defect and LOOP-033 never fires. The fence rule asks only that a marker stand alone on its line, and a tilde rule of any length does, so a divider used twice is a closed fence (silent)
  - open: a fenced example the rule refuses to open is read as a live finding, and the message tells the reviewer to do what they already did. A report quoting the record shape inside ```` ```text example ```` is refused with "says open: or resolved: under a heading ... The loop hides an example only inside a fence", advising "quotes an example of the shape inside a fence"; the same is true of a fence inside a blockquote (`> ``` `). Both are fences by any Markdown reader, and no message says that a second word in the info string, or a quote marker before the fence, is what broke it, so the repair is guesswork (blocked, and the message is wording)
  - resolved: the carry and citation rules, the commit comparison, an uncommitted, modified, deleted or empty record, LOOP-032 freshness, and the findings-named heading judged over its span are all sound at 36411289, each refusal naming the finding or the field and what to write; the only sharp edge I met is that a review carrying line written `- **resolved:** words (independent 3641128 1)` is refused as "finding 1 is unrecognized" rather than as a markup problem, which is actionable and consistent with the review's own list being read literally (wording)
