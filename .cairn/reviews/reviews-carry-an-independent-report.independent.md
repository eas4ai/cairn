commitment: reviews-carry-an-independent-report
commit: 35a9ca09
reviewer: I had none of the build's context; I cloned the repository into my own scratch directory, checked out 35a9ca09, never wrote in the working copy under review, and answered every case with a real committed record and a run of `node bin/cairn.mjs wake`.
examined:
  - the gate at 35a9ca09: independentGap, listOf (its new per-line `below`), headerOf, the section/`hits`/`named` reader, reviewOf and the Done branch of wake in bin/cairn.mjs, plus withoutFences in bin/spec.mjs
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020, LOOP-032, LOOP-033, LOOP-086 and LOOP-108 in docs/spec/loop.md, and the record-grammar paragraph of AGENTS.md
  - thirty-eight review/report pairs built as real records in my clone, each committed and each answered by a wake at 35a9ca09; the two baselines pass, so the setup is sound: a clean pair carrying one finding as `resolved: ... (independent 35a9ca0 1)` reaches Done, and a `findings: []` pair reaches Done
  - this round's five changes, all of which hold where I could reach them: a `- open:` bullet behind thirty earlier bullets and three levels of nesting under `## Method` is refused; a numbered item and a `- open:` bullet under `## Findings` beside a non-empty `findings:` list are both refused; a single leading `# ` title, a `# ` title after a blank line and a title inside a fence are all accepted; a report short of `commitment:` and `commit:` is answered "carries no commitment: or commit: line" and one short of only `commitment:` is still repaired; and `## Findings ##` is quoted as "Findings"
  - `node --test tests/*.test.mjs` at 35a9ca09: 526 tests, 526 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, exit 0
findings:
  - open: a bullet inside a blockquote is invisible to the whole reader, so a finding written as `> - open: ...` reaches Done with nothing named. A report with `findings: []`, `## Notes` and `> - open: a defect the loop never names` is Done; `>- open: ...` is Done; and with `findings:` holding one carried finding and the section titled `## Findings`, `> - open: a second defect the loop never names` is still Done. The same record as the agent's own review (`findings: []`, `## Notes`, `> - open: my own unresolved defect`) is also Done, so LOOP-086 loses it too. The cause is ENTRY's `^([ \t]*)` anchor: `>` is not indentation, so the line matches neither `below` nor `named`'s `body.some(l => ENTRY.test(l))`. This is not the accepted prose limit -- the bullet does begin `open:`, which AGENTS.md promises the loop refuses wherever it sits (silent)
  - open: beside a non-empty `findings:` list, a heading whose title names findings may carry a whole finding, as long as the finding is not a bullet. A report with `findings:` holding one carried finding plus `## Findings` over the paragraph `open: a second defect the loop never names` is Done; the same with the finding in a table row (`| 2 | open: a second defect the loop never names |`) is Done; and the agent's own review in either shape is Done. With `findings: []` the identical section is refused ("writes the heading "Findings", whose title names findings, where the loop reads no finding"), so the protection is switched off by the presence of the entries it is meant to complete. The cause is `named = entries.length ? hits.find(s => s.body.some(l => ENTRY.test(l))) : hits[0]`, which contradicts the comment above it that "the heading's presence is the whole test" and that "a table row, a paragraph and a subsection can each carry a finding" (silent)
  - open: a bullet that begins `open:` at the top level of the `examined:` list is absorbed as an examined entry, and Done is reached. A report whose `examined:` holds `- the gate at 35a9ca09 in bin/cairn.mjs` then `- open: a defect the loop never names`, with `findings: []`, is Done; the same two lines in the agent's own review are Done. One indent deeper the loop does name it (`examined: holds a line the loop cannot read as an entry: "open: a defect the loop never names"`), because FINDING is consulted only on the `deep` branch, so the reader can recognise the shape and declines to at the margin. A reviewer that puts a finding one bullet too high loses it with no message (silent)
  - open: the prefix is recognised only when `open:` is the literal start of the bullet's text, so four ordinary ways of writing the same bullet are unread. With `findings: []` and a `## Notes` section, each of `- [ ] open: a defect the loop never names`, `- **open:** a defect the loop never names`, `- open : a defect the loop never names` and `<ul><li>open: a defect the loop never names</li></ul>` reaches Done. The space-before-colon and bold forms are plain typing and markup a reviewer uses without thinking, and in the review's own `findings:` list the bold form is caught ("finding 1 is unrecognized"), so the reader is stricter inside the list than in the body it is meant to sweep (silent)
  - open: headerOf reads past exactly one `# ` title, so four ordinary title shapes are refused with a repair that cannot be carried out as written. `## Independent report`, two `# ` titles, a `# ` title after one line of prose, and a setext title underlined with `===`, each above correct `commitment:`, `commit:`, `examined:` and `findings: []` lines, all give `repair <report>`: "names commitment and commit and examined and findings below a heading, where the header ends ... keep the record's fields at the top, each on its own line, put prose after a heading, and change none of the reviewer's words". The fields already are at the top, each on its own line, with no prose above them; the only fix is to change the reviewer's own heading -- `##` to `#`, delete one of two titles, move the title above the prose, delete the underline -- which the same sentence forbids. A single leading `# ` title is accepted, so a fix exists and the message never names it (blocked)
  - open: a `commit:` value with a parenthetical costs a whole new reviewer. `commit: 35a9ca09 (the commit the review names)` above a correct report gives `review <slug>`: "names 35a9ca09 (the commit the review names), which is not a commit", so an honest report that names the right commit is discarded over one parenthesis, while the agent's own review with a two-word `commit:` value is answered with a repair that explains itself (blocked)
  - open: a report that quotes the finding shape in an indented code block is refused, and the message names only a fence as the safe way to quote it. `    - open: an example of the shape, quoted in an indented code block` under `## Notes`, with `findings: []`, gives `repair <report>`: "holds findings under a heading ... If those lines are notes, ask the reviewer for a report whose notes do not begin open: or resolved:, or that puts such an example inside a fence". A four-space code block renders as code exactly as a fence does, and ENTRY matches it at any indent, so the one illustration a reviewer is most likely to write is read as a live finding (blocked)
  - open: a report short of `commitment:` and `findings:` is sent round two repairs, although the comment above the branch says what the record is short of is "said once". `commit:` plus `examined:` with one entry gives `repair <report>`: 'add "commitment: reviews-carry-an-independent-report" above its fields'. Adding exactly that line and nothing else gives a second `repair <report>`: "needs findings: as a list of - entries, or findings: [] for none, above any heading". Both repairs are completable, so this costs a round rather than losing anything (wording)
  - open: a `resolved:` carry needs no resolution. The review line `resolved: the first defect (independent 35a9ca0 1)` -- the report's words, the citation, and nothing else -- reaches Done, while the gate's own message and AGENTS.md both ask for "resolved: <its words>, and how". Echoing each finding's words back with its citation dismisses every finding an independent reviewer raised, and the gate reports the review "clean" (wording)
  - open: uncarried findings are named one per round. A report holding `- open: defect one`, `- open: defect two` and `- open: defect three` against a review that carries none names only "finding 1 is not carried"; carrying finding 1 exactly as told then names finding 2. A reviewer's three findings cost three wakes to learn about, when one message could list them (wording)
  - open: the fix clause attached to a report repair still does not match the fault it follows. A report needing `findings:` gets "keep the reviewer's own words: give a finding its own - entry, put a heading above prose that follows the findings, or ask the reviewer again", which concerns neither the missing field nor anything the reviewer wrote; the findings-under-a-heading repair pairs "keep the reviewer's own words" with "ask the reviewer for a report whose notes do not begin open: or resolved:" in one sentence, which are opposite instructions about the same text (wording)
