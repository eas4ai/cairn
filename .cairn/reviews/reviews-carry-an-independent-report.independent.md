commitment: reviews-carry-an-independent-report
commit: 8efe6c02
reviewer: an independent reviewer with none of the build's context, given only the commitment file, the requirement texts named in the brief, the commit 8efe6c02 and the twenty-fourth round's outcome, working read-only in its own scratch clone
examined:
  - a scratch clone checked out at 8efe6c02, every case written as a review/report pair, committed on a branch whose parent is 8efe6c02 (touching only .cairn/reviews, so the review stays current) and run through `node bin/cairn.mjs wake`; two baselines reach Done at 8efe6c02, one carrying a cited report finding and one a `findings: []` pair, so every refusal below is the case and not the setup
  - the seven fixes of the twenty-fourth round, each rebuilt as a concrete record at 8efe6c02 and run: an unterminated fence inside a findings list is now named as an unread line in both the report and the review, and no open finding is lost; an underlined `Findings` over dashes above a bullet is refused exactly as a hashed one is, in report and review; a body line and an indented note that mention `commitment:` or `findings:` decide nothing, and the omitted `findings:` still gets "findings: is missing"; `Commit:` and `commit :` at the margin are still named as a spelling; the finding-shaped-line repair now reads "retitle a heading that names findings, reword a note that begins open: or resolved:, and put an example inside a fence"; a report short of only its `commitment:` line is repaired with the line to add and not discarded; and AGENTS.md and docs/manual.md state the limit as refusing what the loop can recognise. All seven hold.
  - the rewritten fence stripper: withoutFences called directly on closed, unclosed, four-backtick, tilde-versus-backtick, info-string, 3-space and 4-space-indented and CRLF cases; parseSpec on a specification file where a fenced requirement is hidden and an unterminated one is not; and end to end, a closed fence still hides a decoy findings list in a review body and in a report body while an unterminated one exposes it
  - bin/cairn.mjs at 8efe6c02: fields, recordFields, headerOf with ATX/SETEXT/ENTRY/OWN, listOf including its titles and heading clauses, entryFix, reviewOf, independentGap and its wanted, hidden, blocked and spelled branches, and bin/spec.mjs withoutFences
  - docs/spec/loop.md LOOP-020 and its falsifier, LOOP-032, LOOP-033, LOOP-058, LOOP-071, LOOP-086, LOOP-108; AGENTS.md on the review record, its lists, the findings-titled heading, the stated limit and the independent reviewer; the independent-report paragraph of docs/manual.md; .cairn/escalations/loop-020-loop-086.md; and the committed review and .independent.md
  - tests/independent.test.mjs, its twenty-one cases including the new round-24 case, and tests/record-boundaries.test.mjs, then `node --test tests/*.test.mjs` at 8efe6c02: 522 pass, 0 fail
  - all 68 files in .cairn/reviews/ at 8efe6c02 read through the reader, each copied to this commitment's review path with its commit: line re-dated against a clean report: 61 accepted, 7 refused, which are the four carrying a verification: line inside the examined list, the two listing "- none" under findings:, and this commitment's own report, which read as a review has a prefixless finding
  - fresh attacks beyond the seven: a bare examined: and an examined: whose first line is verification:; a report missing its commitment: line at an older commit, with no commit: line at all, and whose body names another commitment; an H1 title above the fields; a findings table and a prose paragraph under a findings-titled heading; a bare findings: line with and without a bulleted section; a findings-naming heading distant from the bullets; a hashed title with no space, a title over a single dash, and a colon-terminated underlined title; a closed fence at two-space indent inside a findings list; a nested open: bullet under a finding; findings: repeated in a header; CRLF records; and the short sha reachable only inside a fence or only in the reviewer: line
findings:
  - A report whose `examined:` list holds no readable entry is refused with a false diagnosis: `examined:` bare, or `examined:` followed by `verification: node --test tests/independent.test.mjs` and then a bullet, both give "does not read examined:, although the line is there, because a line above it is neither a field nor a heading; keep the record's fields at the top", although the line is read and the fields are at the top, and independentGap's own correct messages sit below the branch that pre-empts them, while the review reader names the identical record correctly as "examined: needs a nonempty list of what the review examined" (blocked).
  - A report short of only its `commitment:` line but naming an older commit, `commit: 3ba15a82` while the review names 8efe6c02, is told to add the commitment line, and the very next wake discards the report it just had edited with "names commit 3ba15a82 and the review names 8efe6c02 ... start a new reviewer", so the named repair does not make the record acceptable and the earlier branch's guard that a report for another commit is replaced rather than repaired is not applied to the new repair (blocked).
  - A heading that names findings holds no bullet list but does hold a table: a report with `findings: []` and a `## Findings` section whose defect is a Markdown table row, and the same section written as one prose paragraph, both reach Done with nothing named, while the identical bullet is refused, so a recognisable finding disappears under a heading whose title does name findings, which is outside the limit the developer accepted in loop-020-loop-086 and contrary to AGENTS.md's rule that such a heading holds no list (silent).
  - A `findings:` line carrying neither `[]` nor any entry is read as no findings and reaches Done when nothing follows it, yet the same line plus an unrelated `## What else I read` bullet list is refused with "holds findings under a heading" in the report and "a finding sits under a heading" in the review, and none of the three named repairs applies, since no heading names findings, no bullet begins open: or resolved: and nothing is an example, while the fix that works, writing `findings: []`, is not named (blocked).
  - A report with `findings: []`, a bulleted `## What else I read` section and a later prose-only heading titled `## Findings I could not place` is refused as holding findings under a heading although no list sits under the findings-naming heading, because the check pairs every bullet below the first heading with every heading's title anywhere in the record; removing only the distant heading makes the identical record Done, and the refusal is wider than the documented rule (blocked).
