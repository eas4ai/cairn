commitment: reviews-carry-an-independent-report
commit: 20029ff0
reviewer: I had none of the build's context, read no session transcript and no working-history diff beyond the two commits I was pointed at; I worked read-only in my own clone and wrote nothing in the project checkout.
examined:
  - the commitment at commit 20029ff0, cloned fresh into my own scratch directory and checked out there; node bin/cairn.mjs wake in that clone names review reviews-carry-an-independent-report with the LOOP-058 reason, which is the action this report serves
  - the kernel deletions: git diff 82633728 20029ff0 -- bin/ is exactly two hunks, the constant beside ENTRY and the local in listOf, and nothing else in bin/. At 82633728 the constant appears only on its own declaration line and the local only on its declaration and three assignments, so neither was ever read
  - whether either deletion changed behaviour: I restored bin/cairn.mjs from 82633728 into the clone and ran node --test tests/*.test.mjs, which gave 537 tests, 537 pass, 0 fail, the same counts as 20029ff0's own kernel, then restored the file
  - whether anything else in bin/ is now dead: I wrote a scanner that strips comments, strings, template bodies and regex literals before counting identifier reads, checked it against 82633728 where it names both deleted declarations, and ran it on bin/cairn.mjs, bin/hook.mjs and bin/spec.mjs at 20029ff0. Its five remaining candidates (retentionReasons, RUNS, legacy, wakeVerdict, stamp in cairn.mjs, loose in hook.mjs, parseSpec in spec.mjs) are all read, at lines 1081, 694 and 1388, 719, 1089, 1390, hook.mjs 53, and from cairn.mjs, spec-lint and three test files respectively. No dead declaration is left
  - the record reader through wake, at the place the deleted local sat: I built review and report pairs in the clone, committed each, and ran wake. A blank line between entries is nothing and an indented line under an entry joins it, in both records and in both lists, exactly as the agreement describes; a report whose findings are carried reaches Done and the same report with the carrying line removed names every uncarried finding by number
  - a blank-line sweep: a blank line inserted at each of the 20 line positions of a rich report, each beside a review that carries nothing, and at each of the 9 positions of a carrying review beside that report. No insertion turned an uncarried finding into Done, and none turned a carried one into a refusal
  - an insertion sweep: a finding-shaped bullet placed at each of the 15 line positions of an otherwise clean report, beside a review that carries nothing. Every one was named; none reached Done
  - fourteen further pairs, each with a control differing in one detail: a missing report, a report at another commit, a citation not last on its line, a citation to a finding the report never made, an indented prefixed line after a blank line inside the list, a deeper prefixed bullet under an entry, a prefixed line inside a fence in the body, a prefixed entry in the examined list, a heading titled about findings against the same heading titled Notes, a report that names the commit only on its commit line, a blank line before the first entry, and two blank lines mid-list. Every shape either carried the finding or named something; no shape reached Done with a finding unaccounted for
  - the CHANGELOG's claim that markers, labels, tags, table cells, headings and fences are markup and not cover: a table row, an unchecked task box, an HTML comment, a blockquote and an emphasised word, each carrying a finding prefix in the report body, are all named, while prose containing the word "opened" is not, and a fenced commit or commitment line in the body is kept out of the metadata without hiding anything
  - the PKG-004 fixtures, by mutating a throwaway copy of scripts/pkg-lint.mjs: dropping nested paths from the counted set fails tests/pkg-lint.test.mjs 1 of 19, dropping the unterminated-last-line term fails 1 of 19, and three threshold mutations (greater than 2001, greater than 1999, and greater than or equal to 2000) each fail 1 of 19. Both new fixtures bite
  - what the lint's test still does not observe: the kernel text set on line 20 that PKG-003, PKG-009 and PKG-012 read, and the line count printed in the clean summary. Findings 4 below, and adding 500 to the printed count leaves the suite green
  - the round count against the repository's own history: I summed the tracked files under bin/ at every round fix commit from round 25 to round 38 and at 20029ff0, which matches pkg-lint's own figure of 1900 at HEAD. Findings 1 and 2 below
  - every record the fix touched, for stale sizes, contradictions, unfinished sentences and wrapping: docs/spec/package.md, docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md, CHANGELOG.md, the next-iteration item and tests/pkg-lint.test.mjs. The sentences are complete, the developer's words are now quoted the same way in the decision record and the captured item and not at all in the specification, and node scripts/spec-lint.mjs reports docs/spec clean. Finding 3 below
  - every live record naming 1900, 1872, 1600 or 1500: the superseded ceiling decisions, roadmap line 464, recon lines 34 and 141, the 0.6.0 and earlier changelog entries and the audit report are all dated historical statements, and the sibling next-iteration item carries an Answered on 2026-09-18 paragraph that gives the new number. No live record states a stale ceiling as the current one
  - the changelog citation: CHANGELOG.md line 49 now cites escalation loop-020-2, whose question is whether the rule that ends the hiding places ships as 0.7.0 and whose reasoning states that price in those words, where loop-020.md asks a different question. The correction points at the right record
  - the 0.7.0 entry claim by claim against the code: the defect capture and the fix action at bin/cairn.mjs lines 1211 to 1214 and 706, documents in a declaration at lines 1198 to 1200, the uncommitted changelog joining the release commit at scripts/release.mjs lines 36 to 47, the revision gate reading only the commitment's own requirements at line 1358, the attribution policy and reword action at lines 1108 to 1116, and the reader rule verified by the probes above. All five version files read 0.7.0. I found no claim the code does not support
  - the full suite at 20029ff0: node --test tests/*.test.mjs gives 537 tests, 537 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo in 55.9 seconds. No failure, so no isolation re-run was needed
findings:
  - The corrected round count is still wrong, in all three records it was corrected in. docs/spec/package.md line 49, docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11 and CHANGELOG.md lines 16 to 17 now say rounds 31 to 34 each paid for their fix by trimming comment. Summing the tracked files under bin/ at each round's fix commit shows the kernel reached exactly 1900 at round 30's fix 1376ec71, which took it from 1899 to 1900, and stayed at 1900 until the ceiling rose at edb4460a, so rounds 31 to 38 each had to remove as much as they added: 35ebf8c4 added 10 and deleted 10, 19921633 11 and 11, 92aa67f0 12 and 12, 601f567a 10 and 14, f0b5bf54 16 and 12, d469ae1b 19 and 19 including a twelve-line comment block, d137f971 14 and 14, c133f0a7 14 and 14. The number was taken from the two next-iteration items, which disagree with each other, one naming rounds 31, 32 and 33 and the other 31, 32, 33 and 34, and neither agrees with the kernel history, so an inflated count was replaced by an understated one in the requirement's rationale, the decision record it rests on and the release notes. (wording)
  - The paragraph rewritten in .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md contradicts the history in both halves of its first sentence. Lines 145 and 146 say the files under bin/ reached 1900 in round 33 because giving the prefix to one list cost less code than the rules it replaced. bin/ reached 1900 at round 30's fix 1376ec71 and already stood at 1900 before round 33's fix 92aa67f0, which went from 1900 to 1900; and the change that gave the prefix to one list is round 34's fix 601f567a, which came after round 33 and took bin/ from 1900 down to 1896. The stated cause postdates the stated effect by one round, and the stated date is three rounds late. This is the same paragraph the forty-first report sent back as a splice, rewritten without the number being checked against the repository. (wording)
  - The edit to PKG-004's rationale left its paragraph unwrapped. docs/spec/package.md line 51 runs to 102 characters where lines 47, 48, 49, 50 and 52 of the same paragraph run 68, 68, 72, 66 and 66, because the replacement text was spliced in without rewrapping the line it joined. An over-long line in a paragraph that otherwise wraps near 72 is exactly the mark the forty-first report used to identify the botched paragraph it sent back, so the commit that fixed that splice left this one in the specification. (wording)
  - tests/pkg-lint.test.mjs still does not observe the file set the lint reads for PKG-003, PKG-009 and PKG-012, only the set it counts for PKG-004. Narrowing the filter on line 20 of scripts/pkg-lint.mjs to direct children of bin/, so that bin/hooks/session-start.mjs and bin/hooks/stop.mjs stop being scanned for commands, test imports and network or model calls, leaves node --test tests/*.test.mjs at 537 tests, 537 pass, 0 fail and the lint still printing kernel 1900 lines, clean; tests/pkg-lint.test.mjs is the only test of that script, and it stays 19 of 19. The shipped lint does catch the case, since a scratch repository with a fetch call in bin/hooks/stop.mjs is reported as PKG-012 and exits 1, so only the test is blind. This is the same shape as the forty-first report's fifth entry, which was closed for the line count and left standing for the three text checks that read the same directory. (wording)
