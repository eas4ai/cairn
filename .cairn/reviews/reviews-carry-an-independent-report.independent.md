commitment: reviews-carry-an-independent-report
commit: 5a198efe
reviewer: I had none of the build's context - no round history, no notes, no author - and worked read-only in my own clone of the repository at 5a198efe, judging every case only by what the kernel there printed.
examined:
  - the gate at 5a198efe: independentGap, listOf, headerOf, the prefix test and reviewOf in bin/cairn.mjs, against LOOP-020, LOOP-032, LOOP-033, LOOP-058, LOOP-071, LOOP-086 and LOOP-108 in docs/spec/loop.md, docs/commitments/reviews-carry-an-independent-report.md, and the record-grammar paragraph of AGENTS.md
  - the harness first: a pair with one carried finding, and a pair whose report lists none, both reach Done at 5a198efe, so every refusal below is the case and not my setup; every case is a committed review and report pair judged by a real wake run
  - the prefix rule, one detail at a time: the word with a plain space, in bold and wrapped in emphasis, against the word in square brackets, in double quotes, in round brackets, with a numeric character reference for the colon, with a no-break space or a zero-width space inside it, and the bare word inside angle brackets
  - the findings list's own lines: entries numbered, wrapped, nested one level deeper, cut by a rule at the margin, written above the examined field, empty by brackets, empty by a bare field, declared again in a body section, and held inside a fence
  - a heading whose title names findings, holding a list, holding only prose, and beside a header list that already holds entries
  - the carry and citation rules: an uncarried finding, two review lines citing one number, one line citing two, a citation that does not end its line, a citation the report never made, and an earlier round's citation - each refused, naming the number and the reviewer's words; sound
  - the commit comparison: a report at another commit, at a value that is not a commit, at a six-character mention, an old report re-dated, and a review whose commit is not a commit - each refused; sound
  - the record's states: a report modified, deleted, emptied and blanked on disk and then committed, and an empty or deleted review - the uncommitted ones are named for commit before they are judged, the committed ones are named as what they are; sound
  - freshness: a declared input changed after the review, and a requirement's text revised after it, both take the wake off Done before the report is read (LOOP-032, LOOP-058); sound
  - node --test tests/*.test.mjs at 5a198efe: 533 tests, 533 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo
  - all 68 files in .cairn/reviews/ through the review reader at 5a198efe: 58 accepted with no open finding, 10 refused - four because a verification field sits inside the examined list and stops it (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, skills-can-be-installed-with-the-skills-cli), two because their one findings entry is the word none (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses), one for the heading titled Documentation review finding (freshness-and-scope-guidance-agree), one for a prefixed line in an examined entry (escalation), one for a prefixed line under a heading (the-loop-continues-past-done), and one that is this commitment's independent report rather than a review, whose entries carry no prefix by design; read through the report reader instead, that file is well formed and holds seven findings, none of them carried by the review at 1ca39d93
findings:
  - The sweep that catches a finding written outside the findings list recognises the prefix only when a space or a tab stands between the word and the colon, so a reviewer who writes it in almost any other shape loses the finding with nothing named. With one real finding carried and a second written as a bullet in a body section titled Notes, each of these reached Done at 5a198efe: the word in square brackets, `[open]:`; in double quotes, `"open":`; in round brackets, `(open):`; with a numeric character reference for the colon, `open&#58;`; with a no-break space or a zero-width space inside it; and the bare word inside angle brackets, where the tag stripper deletes the word and leaves only the colon. One detail changed refuses each of them - a plain space, the word in bold, in emphasis or in backticks - and every leading mark the working agreement names is caught: a quote marker, a hash, a list marker, a task box checked or not, an emoji, a footnote marker, and a quotation mark around the whole line. The hole is markup around the word itself other than emphasis and tags, an unusual space, and a character reference, so the shape of the line and not its content decides whether a real finding reaches the gate. (silent)
  - A fenced block inside the findings list hides ordinary entries from the list and from the sweep at once. Fenced lines are blanked before the list is walked, so an entry between an opening and a closing fence is no entry at all; and because a blanked line is not one of the list's own lines the sweep does read it, but finds no prefix there to catch. A report whose list held one entry, a fence, two more ordinary entries and the closing fence reached Done with only the first finding carried; the same two entries with the two fence lines deleted were refused as uncarried, and the same fence holding a prefixed line was refused. Both of the gate's messages about such a line, and the working agreement's paragraph on the prefix, tell the agent that a fence hides no finding from the sweep; that holds only for a line that carries the prefix. (silent)
  - A body section that writes the findings field a second time is read as nothing when the header's list is not empty, and refused when it is. The same block - a heading titled Appendix, the field name, and one bullet naming a real defect - reached Done both when the header's list held one carried entry and when the header wrote the empty-bracket form, and was refused, as naming findings with no entries above a list the loop does not read, only when the header wrote the bare field. The reviewer's second list therefore disappears in exactly the two shapes where the report otherwise looks answerable, and the helper written to catch a bullet, a prefixed line or a table row in such a section is defined in listOf and never called. (silent)
  - A leading title written with no space after its hash refuses an honest record and the message names the wrong fix. headerOf accepts that line as the record's one leading title, so the examined and findings lists are read from the text below it, while recordFields treats it as prose and stops there, so the commitment and commit fields are lost. The verdict then says the record does not read those two fields although the lines are there, because a line above them is neither a field nor a heading, and advises keeping the fields at the top, each on its own line, with prose after a heading - but the fields are already at the top, each on its own line, and there is no prose. Nothing names the repair that works, a space after the hash, or the line's removal. A review record with the same first line is refused the same way under LOOP-108. (blocked)
  - A prefixed line anywhere but the list's own lines names repair of the report, while the same sentence forbids the only repair it leaves. The why tells the agent to keep the reviewer's own words and to ask the reviewer for a report that describes the prefix rather than writing it, and AGENTS.md reads a repair action as fixing the named record and nothing else; for an honest sentence that happens to carry the prefix there is nothing to move into the list, so the action and the advice point opposite ways. The heading-title case, equally unrepairable, names a new review instead, which is what this case needs too. (wording)
