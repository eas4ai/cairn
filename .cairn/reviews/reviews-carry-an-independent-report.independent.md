commitment: reviews-carry-an-independent-report
commit: ff0e9e56
reviewer: a sixteenth independent subagent given the commitment, LOOP-020, the working agreement paragraphs and the commit range 6ef294ba..ff0e9e56, with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 with its falsifier, and LOOP-086, LOOP-071 and LOOP-108 in docs/spec/loop.md, against the AGENTS.md paragraphs on the review record, its lists and starting the independent reviewer
  - git log and git diff 6ef294ba ff0e9e56 for bin, tests, docs and AGENTS.md: at commit ff0e9e56 the new above set in listOf, the rewritten heading rule, the rewritten report repair message, and the six new assertions in tests/independent.test.mjs
  - the fifteenth independent report and the three resolutions claimed at ff0e9e56 in .cairn/reviews/reviews-carry-an-independent-report.md, each retested as a concrete record rather than read
  - the whole declared suite in a scratch clone at ff0e9e56 with TMPDIR outside the repository: 513 tests, all pass; and node bin/cairn.mjs wake there, which asks for this review
  - about twenty hand-built review and report records run through node bin/cairn.mjs wake in that clone, and the same records run against a worktree at 6ef294ba to separate what this commit changed from what it inherited
  - every committed record in .cairn/reviews scanned for the shape the new heading rule refuses: none holds it today, so that refusal is latent rather than live
findings:
  - A review whose findings list holds a margin line beginning "Reviewer:" still ends in silence and an open finding after it reaches Done, because reviewer: is the one of the five field names a review never carries above its lists, so the new "already above" rule never covers it; the same line inside the examined list drops every entry after it with no word in the verdict.
  - The new heading rule refuses honest records: any bullet or number anywhere in the body, together with any body heading whose title contains the word "finding" (for example "## Findings from earlier rounds", or a report's "## How I reproduced the findings"), makes wake refuse the review with "a finding sits under a heading" or the report with "holds its findings under a heading" although every finding sits in the header list and is read; the same records report Done at 6ef294ba, so this is new at ff0e9e56.
  - The report side still misses findings under a heading whose title does not contain the word "finding": a report with "- one" and then "## Further defects", "## Defects" or "## What I found" holding "- two is broken" reports Done saying "its independent report are clean" with finding 2 carried nowhere, so the fifteenth round's third finding is resolved only for headings named after findings.
  - The report's repair for findings that genuinely sit under a heading offers two fixes that do not apply, "give a finding its own - entry" and "put a heading above prose that follows the findings", and never the one that does, moving the entries above the first heading as the review's own message says, so a developer following the refusal cannot repair the report.
  - AGENTS.md still says each list "ends at the next field of the record (commitment:, commit:, examined:, findings: or reviewer:)" and that "every other line inside a list is named by the loop rather than read past", and the kernel now does neither: a repeated name is named instead of ending the list, and a reviewer: line ends a review's list in silence.
