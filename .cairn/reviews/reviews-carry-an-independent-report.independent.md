commitment: reviews-carry-an-independent-report
commit: 82633728
reviewer: I had none of the build's context, read no session transcript and no diff of the build's working history beyond the two areas I was pointed at; I worked read-only in my own clone and wrote nothing in the project checkout.
examined:
  - the whole review is at commit 82633728, cloned fresh and checked out in my own scratch directory; `node bin/cairn.mjs wake` there names `review reviews-carry-an-independent-report` with the LOOP-058 reason, which is the action this report serves
  - the ceiling end to end in a scratch git repository of my own: a kernel of exactly 2000 lines produces no PKG-004 line from `node scripts/pkg-lint.mjs`, and the same file at 2001 produces "PKG-004: kernel is 2001 lines". That is PKG-004's falsifier word for word ("the line count of the files under bin/ exceeds 2000"), and scripts/pkg-lint.mjs line 61 compares with `> 2000`
  - the real kernel: `wc -l` over the five tracked files under bin/ gives 1900 (cairn.mjs 1733, hook.mjs 96, hooks/session-start.mjs 14, hooks/stop.mjs 14, spec.mjs 43), and pkg-lint prints "kernel 1900 lines, clean", so 100 lines of the new ceiling are unused
  - whether the PKG-004 test can fail: I mutated the threshold four ways in a throwaway copy (`> 2001`, `>= 2000`, `> 1900`, `> 1999`) and tests/pkg-lint.test.mjs failed on every one, 18 of 19 passing each time. The boundary is genuinely observed
  - whether the PKG-004 test observes what is counted: I mutated the file set and the counting rule instead of the threshold, and the whole suite stayed green both times. Finding 5 below
  - every file in the repository that names the ceiling: the two next-iteration items, the reviews, the decision records, docs/spec/glossary.md, docs/spec/roadmap.md and docs/recon.md. roadmap.md line 464 and recon.md lines 34 and 141 name 1500 and 1600, but both are dated historical records of a past state and say so, so I do not count them. The live claims that are wrong are in one next-iteration item, findings 1 and 2 below
  - the CHANGELOG 0.7.0 entry claim by claim against the v0.6.0 tag: `git grep` over bin/ at v0.6.0 finds no defect, attribution, reword, independent, policy or documents code, and HEAD has all of it, so bullets two to six describe work that is genuinely new since 0.6.0; the release script's CHANGELOG handling (lines 36 to 47 of scripts/release.mjs) is likewise absent at v0.6.0, and the revision-scope line at bin/cairn.mjs 1358 is too
  - the "five fixes" lead sentence: the five commitment files that carry the same sentence are defects-are-fixed-without-a-promotion, documents-and-the-changelog-cost-one-check, check-asks-mechanism-reviews-only-for-the-commitment, unpushed-commits-carry-no-ai-attribution-when-forbidden and reviews-carry-an-independent-report, and the entry's five non-ceiling bullets map one to one onto them, the third bullet covering both halves of the documents-and-the-changelog commitment. The sentence holds
  - the LOOP-020 gate by what wake actually does: about 370 review-and-report pairs built, committed and put through `node bin/cairn.mjs wake` in a scratch clone, every one beside a control differing in one detail. The harness first: a pair whose two lists are both empty reaches Done, the same empty review beside a report holding one defect is refused by name, and the same report beside a review that cites it reaches Done
  - a position sweep: ten spellings of a finding-shaped line (a bare one, a bullet, a resolved bullet, a number, a quote marker, emphasis, a task box, a table row, an HTML comment, capitals with a space before the colon) inserted at each of the 24 line positions of a rich report, 240 committed pairs in all, each beside a review carrying nothing. Not one reached Done. Inside a closed fence it is still named, as the entry promises
  - the control sweep for that: four benign lines at the same 24 positions, 96 pairs, of which 78 reached Done and 18 were refused, all 18 for sitting inside the header lists where a non-entry line is a named repair. "opened" and "unresolved" are correctly not the prefix. Acceptance is plainly distinguishable from refusal
  - 27 further hand-built shapes: a defect in the examined list, between the two fields, after a setext heading, at end of file with no final newline, in a second field below the list, as the field's own value, in the record's own title, nested one level deeper, and a heading whose title names defects beside a carried list. Every one named. Two shapes reached Done and both are the limit the developer accepted on 2026-09-18 and the entry records: a defect written as prose, and as a plain bullet, under a heading that does not name them
  - the carry and citation rules: a wrapped report entry carried by its full words reaches Done and by its first line's words alone is refused; two defects and one citation, one defect and two citations, a citation not last on its line, and a citation numbered past the report's length are each named. I found no silent loss
  - bin/ is byte-identical from 3833b02a through 82633728, so the gate code is the same one the previous rounds examined; nothing in this change touches it
  - the full suite: `node --test tests/*.test.mjs` gave tests 537, pass 537, fail 0, cancelled 0, skipped 0, todo 0, in 55.0 s. Nothing failed, so nothing needed re-running in isolation. I ran it twice more under mutations and it reported 537 each time, so 537 is the real count
  - the mechanism record: .cairn/mechanisms/pkg-lint carries a fourth PKG-004 digest, and wake asks for no mechanism review, so the digest matches the revised text. The mechanism review entry in the commitment review states what I independently reproduced
  - the commitment review at 82633728 still carries one unfixed entry, about the stop-hook bullet that was removed at edb4460a. It blocks Done rather than hiding anything, and wake names a fresh review first, so this round rewrites it. I record it as checked, not as a defect
  - the release state: the five version files and `cairn --version` all read 0.7.0. I did not treat the absent tag as a defect, as briefed, but I did test whether the shipped path can restore it. Finding 7 below
findings:
  - .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md line 161 says PKG-004 is "not violated at 1900 of 1900, though there is no headroom left", and gives that as the reason to defer deleting the dead constant at bin/cairn.mjs line 909. At 82633728 the ceiling is 2000 and the kernel is 1900, so there are 100 lines of headroom; line 150 of the same file says the developer raised the ceiling to 2000, which "buys 100 lines and no more". The file contradicts itself eleven lines apart, and the stale figure is the stated reason for not doing work. This is the same class of defect the fortieth report raised as its second entry, reintroduced by the edit that answered it. (wording)
  - The paragraph at lines 145 to 151 of the same file is a botched edit that leaves an unfinished sentence: "stand at 1900 now, the ceiling itself, because what giving the / prefix to one list saved has since been spent / less code than the rules it replaced." The tail "less code than the rules it replaced" is the end of the sentence that was replaced and was never removed, so the clause has two verbs and no object; line 148 also runs to 110 characters where the rest of the file wraps near 72, which is the mark of the splice. "Stand at 1900 now, the ceiling itself" is false at this commit as well, since the ceiling is 2000. (wording)
  - The rationale added to PKG-004 (docs/spec/package.md line 49), the decision record (docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11) and the CHANGELOG (lines 16 to 17) all say the last twelve review rounds each paid for their fix by trimming comment, and the repository's own records say three or four: .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md names "Rounds 31, 32, 33 and 34" and the sibling item names "Rounds 31, 32 and 33". The kernel history agrees with the records and not with the rationale: `git diff 3833b02a 82633728 -- bin/` is empty, so rounds 39 and 40 changed no kernel file at all and paid for nothing, and the round 27 fix c355cdc9 took the kernel from 1860 to 1872, adding twelve lines with 28 to spare under the old ceiling. An inflated count is a weak argument for the requirement that rests on it. (wording)
  - Three committed records quote the same direction from the developer and two of them disagree with the third, so at most one is verbatim: docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11 and .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md line 10 both quote "set the package cveiling to 2000", while docs/spec/package.md line 47 quotes "set the package ceiling to 2000". In a project whose current commitment is that a reviewer's words are never edited, a quotation of the developer that differs between the specification and the decision record it rests on is worth settling one way or the other. (wording)
  - tests/pkg-lint.test.mjs observes the PKG-004 threshold but not what the lint counts, so two real enforcement errors pass the whole suite. Replacing the filter on line 60 of scripts/pkg-lint.mjs with one that skips nested paths makes the lint print "kernel 1872 lines, clean", silently dropping bin/hooks/session-start.mjs and bin/hooks/stop.mjs, which PKG-038 makes Cairn execute at run time; `node --test tests/*.test.mjs` then reports 537 tests, 537 pass, 0 fail. Removing the unterminated-last-line term from the same line does the same: 537 pass, 0 fail. No PKG-004 fixture ever places a file under a subdirectory of bin/, and none ever omits a final newline. The threshold mutations are all caught, so the gap is exactly the file set and the counting rule. (wording)
  - CHANGELOG.md line 49 cites "escalation loop-020" for the limit that an honest line carrying the finding prefix outside the list is refused, and that limit is recorded and answered in .cairn/escalations/loop-020-2.md, whose question is "The rule that ends the hiding places also refuses three committed reviews that used to read" and whose reasoning states exactly that price. .cairn/escalations/loop-020.md asks a different question, whether to keep fixing every finding or record the rest as limits, and nowhere mentions that refusal. The preceding clause on line 46 cites loop-020-loop-086 correctly and by the same pattern, so a reader follows this one to the wrong record. (wording)
  - The 0.7.0 release cannot be re-cut by the shipped path, and no commit carries both the version and the corrected entry. `node scripts/release.mjs 0.7.0` in a throwaway copy of the checkout refuses with "release: 0.7.0 is not an increase over 0.7.0" and exit 3, because line 26 compares against package.json, which already reads 0.7.0. The only commit that sets the version is 96f60acb, and its CHANGELOG still opens with the stop-hook bullet that was removed at edb4460a, so a tag placed there would carry the superseded entry as its message, while a tag placed on a later commit sits on a commit that sets no version. docs/releasing.md step 3 describes the script as the way a release is cut, and PKG-040 describes a release as one commit that sets the version and carries that version's changelog entry; neither is reachable for 0.7.0 from this state without a further version. (wording)
  - bin/cairn.mjs line 945 declares a local in listOf that is assigned on lines 947, 957 and 959 and never read anywhere in the function or the file. It is dead code in the record reader, beside the unused constant on line 909 that the thirty-ninth report found and that the waiting next-iteration item already files for deletion; this one is not filed anywhere. The ceiling was raised on the argument that the reader needs room to be written plainly, so the reader's dead declarations are the first thing that room should buy. (wording)
