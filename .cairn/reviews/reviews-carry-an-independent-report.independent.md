commitment: reviews-carry-an-independent-report
commit: 3833b02a
reviewer: I had none of the build's context, read no diff and no session history, and worked read-only in my own clone of the repository at 3833b02a; I wrote nothing in the project checkout.
examined:
  - the gate at 3833b02a read as source (bin/cairn.mjs: independentGap, reviewOf, listOf, headerOf, entryFix and the line test for the finding prefix; withoutFences in bin/spec.mjs), then judged only by what node bin/cairn.mjs wake actually did in my own clone
  - the harness first, at 3833b02a: a pair with one carried finding reaches Done, a pair whose two lists are both declared empty reaches Done, and that same empty review beside a report holding one finding is refused by name, so acceptance can be told from refusal. Every case below is paired with a control that differs in one detail
  - about 360 review-and-report pairs built, committed and put through wake at 3833b02a
  - 50 report shapes an honest reviewer would write, each beside a review carrying nothing, so that reaching Done would mean the finding was lost: five bullet markers, two numbering forms, five indents including a tab, a leading hashed title and a leading underlined title, a rule between the fields, blank lines after the key line and between entries, a wrapped entry, a nested detail, a trailing rating, emphasis and code spans round the words, a task box, the whole record fenced, the whole header indented, the fields written out of order, the key line spelled in capitals, front matter, an appendix section, a record with no final newline, and a CRLF record. Every one was named
  - 200 further report shapes generated at random from the same vocabulary, each beside the same empty review. All 200 were named and none reached Done
  - a position sweep for the region question: a plainly prefixed bullet inserted at each of the 24 line positions of a rich report, and at each of the 23 line positions of a rich review with sections and a title. Every one of the 47 was named; none reached Done, so outside a fence I found no region the reader neither reads nor sweeps
  - 20 review shapes holding a real unfixed finding beside a clean report: capitals, emphasis, a space before the colon, a nested entry, an entry below the declared-empty form, a second key line, a table row, a fenced block, a section titled about findings, a rule inside the list, a blank line inside the list, the value written on the key line, the finding in the examined list, and a CRLF record. All but one were named, and the one that was not is the limit accepted on 2026-09-18
  - the file states of both records: the report modified on disk, the report deleted from disk, the report committed empty, the report committed as whitespace only, and the review deleted from disk. Each was named with an action that fits, and the deleted report was told to be restored or its deletion committed
  - the commit comparison: the full 40-character identifier on both sides, seven characters, eight characters, upper case, a 40-character string that is not a commit, and a report that names its commit nowhere but on its own key line. The first four were accepted and the last two refused by name
  - the carry and citation rules in 15 shapes: a citation followed by a full stop, a citation not last, two lines citing one finding, one line citing two, a doubled citation, an eight-character and a 40-character identifier in the citation, a leading zero in the number, a capitalised keyword, trailing spaces, a numbered carrying line, a wrapped carrying line, a nested note under a carrying line, and a carrying line that begins with fewer words than the finding. Each behaved as the record grammar says
  - freshness: at 3833b02a the repository's own review names 962a52fa and wake refuses it because a declared input changed since (LOOP-032); revising the falsifier of LOOP-020 and committing it stops Done at 3833b02a and names the mechanism review for LOOP-020 (LOOP-058, LOOP-059); restoring the text returns the verdict to Done
  - the repository's own 166 KB review record at scale: retargeted to 3833b02a, with one new carrying line added beside its 38 rounds of earlier citations and a fresh report holding one finding, it reaches Done, and its earlier citations at other identifiers are neither counted nor mistaken for this report's
  - node --test tests/*.test.mjs at 3833b02a in a pristine second clone, run twice: 528 tests, 528 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, both times, with no flake to re-run
  - all 68 files in .cairn/reviews/ at 3833b02a put through the review reader one at a time: 58 accepted, 10 refused. One of the ten is the independent report, which that reader is not meant to read; of the 67 review records, 58 are accepted and 9 refused, which matches the limit accepted on 2026-09-18
  - I found no silent loss. Every shape I built either kept the finding or named something, and the findings below are all about what a message asks the agent to do next
findings:
  - A report that says in words that it found nothing is counted as a finding and must be carried. At 3833b02a a report whose findings list holds the single entry "none", or "None.", or "I found no defects at 3833b02a", is answered with the message that finding 1 is not carried, followed by the instruction to carry it in the prefixed form with the citation last. I followed that instruction literally and reached Done, leaving a review entry that records as a finding something the reviewer never found. The controls: the declared-empty form reaches Done, and the same word written as the key line's value is refused as not a list. (wording)
  - The guard that stops the gate telling an agent to declare a report empty recognises only the finding prefix. At 3833b02a a report carrying the other three fields, with its defect written as a plain bullet under a heading titled "Results", is answered with a repair whose words are to add the declared-empty form when the reviewer found none; I followed that literally and reached Done with the defect unnamed. The control is the same report with the prefix on that bullet, which is answered instead by asking the reviewer for a report whose findings list holds the defects it names. The reading is the limit accepted on 2026-09-18; the instruction to declare the report empty is not. (wording)
  - A report whose examined field is present but holds no entry is answered with a repair on the report telling the agent to write each entry under it and change none of the reviewer's words, which asks the agent to author the reviewer's own observations. The control is the same report with its commit line also absent, where the gate says instead that the record carries no commit line and that examined is there and holds no entry, and asks for a new reviewer. The kernel's own comment draws that line, that a field only the reviewer can write is a new report, and the empty examined list falls on the wrong side of it. (wording)
  - The kernel's message for an indented field is unreachable when the indented field is the examined one, so an indented header is never told that it is indented. At 3833b02a a report whose examined field and its entries are indented by two spaces is answered that its commit line carries more than the commit, and an agent that followed that literally would delete the reviewer's entries; a report whose whole header is indented is answered that it carries no commitment, commit, examined or findings line, and asked for a new reviewer, although removing the indent changes no word of the reviewer's. The control is the same record with only the findings field indented, which is answered precisely, that the field is indented and each field belongs at the margin. (wording)
  - bin/cairn.mjs line 909 declares a second constant beside ENTRY that nothing in the kernel or the tests ever reads. It is dead code in the record reader while PKG-004 caps the files under bin/ at 1900 lines and they stand at 1872, and the review record says the ceiling is now a real constraint on this class of work. (wording)
