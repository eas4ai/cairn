commitment: reviews-carry-an-independent-report
commit: 606d046c
reviewer: a fresh agent with none of the build's context, working read-only in its own clone of the repository checked out at 606d046c, which was never written to; the project checkout was not touched
examined:
  - the gate at 606d046c: about forty review-and-report pairs, each written into .cairn/reviews/, committed in the clone, and judged by running node bin/cairn.mjs wake
  - the setup first, so a pass means something: a pair with one carried finding, and a pair whose report declares an empty findings list, both reached Done at 606d046c
  - the new markup reader, through the kernel's own helper and then end to end: leading quote markers, hashes, list markers, brackets, task boxes checked and unchecked, emphasis, HTML tags and comments, lettered and roman list markers, and table rows with and without outer pipes
  - a mechanical region sweep, rather than a list of cases: a finding-shaped line inserted at every line position of two rich records (leading hashed title, wrapped examined and findings entries, a table, an HTML details block, a margin rule, a setext heading, a deeper heading, tab and space indents) and judged for each of nine markup shapes, to look for a region that is neither read for the prefix nor exempt as a list entry; outside a fence there is none, so the reader's region coverage is complete and both losses below are reader rules, not unread regions
  - the carry and citation rules: a finding no review line cites, a citation that is not last on its line, one line citing two findings, a number the report does not have, and a carry written in the review's body instead of its list; each behaved as the requirement says
  - the commit comparison and the record's state: a report naming another commit, a report deleted from the tree, a report modified on disk, a review modified on disk, and an empty report file
  - LOOP-032 freshness: at 606d046c the repository's own review, which examined 356e94fc, is named stale because a declared input changed since
  - node --test tests/*.test.mjs at 606d046c: 529 tests, 529 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, exit 0
  - all 68 .md files in .cairn/reviews/ read through the kernel's own reader: 62 accepted, 6 refused; four (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, skills-can-be-installed-with-the-skills-cli) for an unbulleted "verification:" line inside examined: with the entries after it unread, and two (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses) for a findings value of "none" read as one unrecognized finding; every one of the six was last written in 2026-09-05 or 2026-09-06, before this commitment opened on 2026-09-17
  - the limits already accepted were used as controls, not reported: a prose finding under a heading that does not name findings, an indented four-space code block, an honest table cell that begins with the prefix, a resolved line with no account, a heading that merely mentions findings, the cost of a version bump, and old runtimes
  - three wording defects the previous round reported are fixed and were re-run as controls: a header-level prefix line is now named as sitting above the findings list, a field after the findings list is now told to move above examined:, and a report missing from the tree is now told it may be restored from HEAD
findings:
  - the markup stripper reads a line from its first letter, so any marker that itself contains a letter hides the prefix, and the most ordinary such marker is a checked task box: "- [x] open: X" is invisible while "- [ ] open: X" is refused, which also contradicts the helper's own comment naming a task box as markup. The hole has five reproduced arms at 606d046c, each with the unchecked box as a control that refuses: a checked-box finding in the report's body under a heading titled Notes reached Done; the same line inside the report's examined: list was read as something examined and reached Done; the same line in the report's header just above examined: reached Done; the same line in the builder's own review, under a heading, reached Done with an unresolved defect, so LOOP-033 and LOOP-086 lose by this hole and not only LOOP-020; and "a) open: X", "i. open: X" and "- (x) open: X" in the report body reached Done together. Of 33 markers tried, every one containing a letter before the prefix is missed and every one without a letter is seen, so one rule on the stripper closes all five arms (silent)
  - withoutFences pairs fence markers by position from the top of the file, so a line that merely begins with three backticks or tildes opens a fence and swallows every finding down to the next such line, with nothing named. Reproduced at 606d046c: a report declaring "findings: []" whose body held a prose line beginning "``` opens a fence, and the next such line closes it.", then a section headed Findings with one real finding, then a fenced block quoting what the mechanism prints, reached Done, because the prose line's marker paired with the example block's opening fence and blanked the finding between them; deleting only that one prose line from the same report is refused. A decorative "~~~" used twice as a separator round the same section did it too, and so did the same shape in the builder's own review, which reached Done with an unresolved open finding. This is not the accepted limit that only a fence hides an example: the reviewer fenced nothing here, and the kernel's own repair advice tells reviewers to quote the shape inside a fence, so reports in this project carry the fence markers that make the mispairing available (silent)
  - an honest examined: entry that begins with the word the reader treats as a prefix is refused as a line it cannot read, and the repair offered would fabricate a finding: a report whose examined: list held "- open: and resolved: are the two prefixes the reader knows" (exactly what a reviewer of this commitment examines) is refused with "move it into the findings: list, and change none of the reviewer's words", which would turn a statement about what was examined into a finding the reviewer never made; the reviewer cannot reword it either, since the gate forbids editing a report, so the only lawful repair is to ask for a whole new report (blocked)
  - every repair the kernel names for a review record's findings ends with two requirement tags: the review whose finding sat under a heading was refused with "... and put an example inside a fence (LOOP-086) (LOOP-108)", because the composed sentence already carries its own tag before LOOP-108 is appended (wording)
