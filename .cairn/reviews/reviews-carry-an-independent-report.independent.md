commitment: reviews-carry-an-independent-report
commit: 7f3032b2
reviewer: I had none of the build's context, asked for none, and worked read-only in my own clone of the repository checked out at 7f3032b2, driving every case through node bin/cairn.mjs wake.
examined:
  - the gate at commit 7f3032b2: twenty-six committed review and independent-report pairs, each judged by a wake run, never by reading the diff; a clean pair with one carried finding and a findings: [] pair both reach Done, which is the baseline every other case is measured against
  - the five changes of this round where I could reach them: the shared open:/resolved: helper (its leading-marker stripping, table cells and space before the colon), examined: refusing a finding at any indent, the one-leading-title rule in both parsers (two titles, a title after prose, a title inside a fence), the commit: line repaired when it carries more than the commit, and the one-message forms for uncarried findings and missing header lines
  - node --test tests/*.test.mjs at 7f3032b2 in a pristine clone: tests 527, pass 527, fail 0, cancelled 0, skipped 0, todo 0, duration 38.0s, exit 0
  - every file in .cairn/reviews/ at 7f3032b2 read through the kernel's own review reader (68 files, including the commitment's independent report): 62 accepted, 6 refused. Four refused because examined: holds a bare verification: line that is not an entry, which also drops the entries after it (command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md, skills-can-be-installed-with-the-skills-cli.md); two refused because finding 1 is the unrecognized entry "none" (the-answer-reaches-the-agent.md, the-wake-names-an-action-or-refuses.md). Every refusal predates this commitment and none is caused by this round's changes
  - the controls for each silent case, so the contrast is recorded: a plain "- open: x" bullet under a plain body heading is refused; the same bullet directly under a heading titled "Findings in detail" is refused; a bulleted "- **open:** x" inside examined: is refused; a report with one finding and a review carrying none is refused by number
findings:
  - open: a line that says open: or resolved: in the report's header above the examined: line is read by no sweep, and the report reaches Done with the finding named nowhere: with findings: [] and the line "open: the gate accepts a report whose finding sits above examined:" placed after a reviewer: line, wake says Done, and so does the same finding written there as an indented "- open: ..." bullet under reviewer:; the body sweep only covers text after the header, and listOf only reads lines after its own key line, so the region between the first field and examined: is unswept. The same line in the review record itself (findings: [] plus an open: line above examined:) also reaches Done, so LOOP-033 never sees the review's own defect (silent)
  - open: the shared helper misses a prefix whose emphasis or HTML closes after the keyword, so a real body finding disappears: with findings: [] and "- **open**: the gate reads no finding when the prefix is bolded as a word" under a plain heading, wake says Done, and "- <b>open</b>: ..." does the same, while the identical line as "- open: ..." or "- **open:** ..." is refused; LEAD strips only leading emphasis and leading tags, so the characters between the keyword and the colon defeat it (silent)
  - open: a finding written as its own heading is read by no sweep: "### open: the examined sweep drops a real defect" in the body of a report whose list is findings: [] reaches Done, because LEAD does not strip the hashes and the heading's own title is only tested for the word finding; the same heading is refused only when its text happens to contain that word, which is an accident of wording, not a rule (silent)
  - open: the join that folds an unbulleted indented line into the entry above it tests the strict prefix, not the new helper, so examined: does hold a finding: an indented "**open:** the gate absorbs this defect into the examined entry" under an examined: entry is swallowed into that entry and wake says Done, and so is an indented "open : ..." with a space before the colon, while the same words as a bulleted entry are refused. Change 2 claims examined: holds no finding at any indent; the FINDING guard on the continuation line makes that untrue for exactly the shapes the helper was taught to see (silent)
  - open: a bullet in a subsection of a heading whose title names findings, or after a rule inside it, reaches Done: a report carrying finding 1 in its list plus "## Findings in detail" containing "### The second one" and the bullet "- the gate accepts a report whose second defect sits in a subsection" is Done, and so is the same bullet under a setext-underlined "The second one" inside that section, while the same bullet placed directly under the findings-naming heading is refused; the sections parser assigns those lines to the subsection, so the findings-naming section's own body looks empty. The code's comment claims the heading's presence is the whole test and that nothing hides in a subsection or after a rule, which the two cases disprove (silent)
  - open: a table under a heading whose title names findings reaches Done beside a non-empty list: "## Findings in detail" holding a two-column table whose row reads "| the gate never reads a table row under this heading | listOf |" is accepted, because the named test is ENTRY.test(l) || saysFinding(l) and a table row is neither a list entry nor a colon-prefixed claim; the same comment claims a table row there carries a finding (silent)
  - open: an honest outcome table in the report body is refused as a hidden finding with no repair available: a section titled "What I tried" holding the row "| a bullet under a plain heading | resolved: the gate names it |" makes wake ask for a repair of the reviewer's own report, and the only fix it offers is to ask the reviewer for another report, because the agent may not edit the words; a summary table of cases and outcomes is the natural way a reviewer reports, and the table-cell test added this round now reads it as a buried finding (blocked)
  - open: the review's commit: line carrying extra text is blamed on the wrong line and is not repaired the way a report's is: "commit: 7f3032b2 (HEAD at the time)" in the review makes wake say it reads the commit as that whole string "because the line below it joined the field", though nothing below it joined anything, while change 4 gives a report in the same shape the accurate reason that the line carries more than the commit; relatedly, when examined: holds a bulleted finding the fix offered is "write each as a - entry under examined:" although the line already is a - entry, so neither message names the move into findings: that the writer must make (wording)
