commitment: reviews-carry-an-independent-report
commit: 26598af4
reviewer: an independent reviewer given the commitment, its requirement texts and the commit range, and none of the build's context
examined:
  - Setup: cloned /home/shawn/workspace2/cairn-dev into my own scratch clone, checked out 26598af4, ran the kernel only as `node bin/cairn.mjs` inside the clone, never the `cairn` on PATH, and wrote nothing inside the project checkout; `git status --porcelain` in /home/shawn/workspace2/cairn-dev after my work still shows only the two pre-existing untracked assets and HEAD is still 26598af4.
  - Read docs/commitments/reviews-carry-an-independent-report.md and the requirement texts for LOOP-020, LOOP-032, LOOP-033, LOOP-058, LOOP-059 in docs/spec/loop.md and PKG-001 to PKG-045 in docs/spec/package.md, plus AGENTS.md, docs/manual.md, README.md, docs/walkthrough.md, the three LOOP-020 escalations, escalation pkg-039 and all seven waiting next-iteration items.
  - Read the gate itself rather than only running it: independentGap, listOf, headerOf, reviewOf, entryFix, the finding predicate and withoutFences in bin/cairn.mjs and bin/spec.mjs, and traced which lines of a record each one reads as an entry, names as unreadable, or sweeps; the only lines exempt from the sweep are the list's own lines and its key line, so no region of a record is both unread and unswept.
  - Built and committed about 1,230 review-and-report pairs in my clone and judged every one by what `node bin/cairn.mjs wake` actually prints, with a control differing in one detail for every acceptance: no pair carrying an uncarried finding reached Done, and every control did.
  - Position sweep 1: a finding-shaped line in 20 markup spellings (bullet, star, numbered, bare, blockquote, bold, code span, HTML element, task box, bracketed, quoted, numbered-inside-bullet, entity colon, HTML comment, spaced colon, upper case, mixed case) inserted at each of the 13 line positions of a plain report, 260 committed cases, zero reached Done.
  - Position sweep 2: the same injection on the review side in 13 spellings at each of the 11 line positions of a review, 143 committed cases, zero reached Done; the same review with no injection reaches Done.
  - Position sweep 3: a rich report an honest reviewer would write (title, five fields, a wrapped examined entry, a setext heading, a fenced block, a table, a blockquote, an HTML comment, hashed headings at two levels, nested bullets) with the injection in 20 spellings at each of its 39 line positions, 780 committed cases, zero reached Done; the un-injected rich report reaches Done, so the fence and table did not merely break it.
  - Targeted probes with controls at 26598af4: a second finding uncarried; a nested bullet joining entry one and its carry; a fenced finding in the body; a table row inside the list; a section titled "Findings I could not place" beside a list holding entries against the identical report titled "What else I saw"; a declared-empty list beside "Other findings" against "What else I saw"; a duplicate field; a prefixed line inside the examined list; a CRLF record; a report at another commit; a citation the report does not support; two lines citing one finding; a citation not last on its line; a twelve-finding report with the tenth, twelfth and first uncarried in turn; a review declaring none; a finding whose own words quote a citation; a body section titled "Defects" with and without the prefix. Every silent candidate was named and every control reached Done.
  - File states: the report deleted, emptied or given an extra finding on disk only are each named as a commit action before anything else is judged; a committed empty or whitespace-only report, a report with no list at all while its prose names a defect, and a report that names the commit only on its commit line are each named.
  - Challenged the mechanism rather than trusting it: six mutations of the gate in bin/cairn.mjs, each run against tests/independent.test.mjs, are all killed - disabling independentGap (2 pass, 33 fail), removing the sweep for the prefix outside the list (31/4), removing the refusal of a heading whose title names findings (30/5), disabling the carry check (28/7), dropping the commit match (32/3), and allowing an empty examined list (34/1).
  - Fix 1, measured rather than trusted: summing the tracked files under bin/ exactly as scripts/pkg-lint.mjs counts them at every first-parent commit of the last 120, the kernel is 1900 at edb4460a, the commit dated 2026-09-18 that raised the ceiling, and 1900 at its parent 535f30a1, so "stood at 1900 that day" in docs/spec/package.md, "on the day of this decision" in docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md and "on the day it rose" in the first 0.7.0 changelog bullet are each true; none of the three now asserts constancy, which is what made the earlier three versions false.
  - Fix 2, measured: 1376ec71 is the first commit at 1900 (its parent is 1899), 601f567a takes bin/ to 1896 for eleven consecutive commits ending at 209773d3, and f0b5bf54 adds four back to 1900, so the corrected sentence in .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md, which now names rounds 31, 32 and 33 as the ones that trimmed comment and credits 601f567a with four lines bought back, matches the repository.
  - Fix 3, reproduced end to end: at 26598af4 a report carrying one finding is refused when its body holds a section titled "Findings I could not place" or "Other findings" beside a declared-empty list, and the identical report titled "What else I saw" reaches Done, which is what the corrected docs/manual.md now says; the false promise remains only in AGENTS.md line 136 and skills/new-project/templates/AGENTS.md line 136, and is captured in .cairn/next-iteration/the-working-agreement-promises-a-heading-the-gate-refuses.md with the manual's correction noted.
  - Fix 4: docs/manual.md line 495 now reads "metadata, but hides no finding. The prefix belongs to that list alone," - the comma splice is a full stop.
  - Fix 5, mutation-tested on a throwaway copy of scripts/pkg-lint.mjs: removing the bare-URL alternative, the node: client alternative, the @google/generative-ai client, the docker-compose alternative or the Procfile alternative each makes tests/pkg-lint.test.mjs fail, as do removing fetch and the openai client; with the bare-URL alternative removed the whole suite is 537 tests, 536 pass, 1 fail, so the fixture is observed by `node --test tests/*.test.mjs` and not only in isolation.
  - The 0.7.0 changelog entry claim by claim against the v0.6.0 tag and the kernel: PKG-004 reads 1900 at v0.6.0 and 2000 now; `documents`, the attribution policy, the defect flag and independentGap are all absent from v0.6.0:bin/cairn.mjs and present now; LOOP-059's revised text narrows the mechanism review to the current commitment; scripts/release.mjs takes an uncommitted CHANGELOG.md into the release commit; the five version files all read 0.7.0 and `cairn --version` prints 0.7.0; and I reproduced the attribution claim by committing a message carrying a co-authorship trailer in my clone, where wake named `reword 013697eb` citing PKG-045, then reset it away.
  - The three stated limits in that entry are each real at 26598af4: a defect written as ordinary prose under a heading that does not name findings reaches Done, a heading whose title names findings is refused even when every finding is listed, and an honest line that begins with the prefix outside the list is refused.
  - Repair follow-through: applying literally what the message asks reaches Done for a fence inside the examined list, a fence inside the findings list, a duplicate field below the list and an indented findings field; the one that does not is an indented examined field, where the record is answered about its commit line instead, and that case is already captured in .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md as an indented header answered about the wrong field.
  - Reproduced but not raised again because they are already captured: the working agreement's heading promise, and the stray bullet directly under the commit field that costs a whole new reviewer while the same bullet one line lower is repaired in place.
  - The review record's sentence that the v0.7.0 tag points at 96f60acb is stale rather than false: the review's own finding 3 at a48ccce4 records that the tag was deleted when the entry was re-cut at edb4460a, so the entry describes the state it examined, and as the brief directs I do not treat the tag's absence as a defect.
  - Scanned bin/cairn.mjs, bin/spec.mjs and bin/hook.mjs for declarations that are written and never read, including destructured bindings: none remain.
  - Ran the full suite at 26598af4 in a pristine clone: `node --test tests/*.test.mjs` is 537 tests, 537 pass, 0 fail, 0 skipped, 0 todo, in 56.5 seconds, with no failure to re-run. `node scripts/pkg-lint.mjs` reports 7959 tracked files, kernel 1900 lines, clean; `node scripts/spec-lint.mjs docs/spec` is clean. `node bin/cairn.mjs wake` at 26598af4 names `review reviews-carry-an-independent-report` under LOOP-058, which is the action this round is.
findings:
  - docs/manual.md lines 497 to 500 tell the reader to "Keep no fence inside either list, write each field once and at the margin, put every entry under a field, and expect a report that breaks these to be written again rather than edited", and the gate does not do that for four of those five shapes: at 26598af4 a committed report with a fence inside its examined list, a fence inside its findings list, a duplicate field below the list, or an indented findings field each makes wake name `repair .cairn/reviews/reviews-carry-an-independent-report.independent.md` with the words "keep the reviewer's own words", and applying each message literally then reaches Done, so the report is edited in place and no round is lost; only a bullet written directly under the commit field costs a new reviewer, and that asymmetry is already captured. AGENTS.md line 167 states the narrower and correct rule, that being refused rather than read in part is one thing and only a report carrying the prefix outside its list is written again, so the manual overstates the cost of the other four to the developer who reads it to learn what this commitment built. (wording)
