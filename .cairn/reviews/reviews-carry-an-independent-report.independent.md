commitment: reviews-carry-an-independent-report
commit: 09abf7f7
reviewer: a twentieth independent subagent given only the commitment, LOOP-020 with LOOP-086/LOOP-071/LOOP-032/LOOP-033/LOOP-108, the AGENTS.md record paragraphs and commit 09abf7f7, working read-only in its own scratch clone with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 and its falsifier, LOOP-086, LOOP-071, LOOP-032, LOOP-033 and LOOP-108 in docs/spec/loop.md, and the AGENTS.md paragraphs on the review record, its lists and the independent reviewer, all at 09abf7f7; AGENTS.md is byte-identical to skills/new-project/templates/AGENTS.md and unchanged since f6bdf509, so this round is code and tests only
  - the code at 09abf7f7: headerOf with ATX and SETEXT, listOf with ENTRY and FINDING, entryFix, reviewOf and independentGap in bin/cairn.mjs, withoutFences and recordFields, and the wake's order, where independentGap runs before the defect-fix, promote and Done branches, so a promotion is gated by the report too; tests/independent.test.mjs and tests/record-boundaries.test.mjs; the whole suite in the clone is 517 tests and all pass
  - the nineteenth round's six defects, each rebuilt as a concrete record at 09abf7f7 and run through `node bin/cairn.mjs wake` rather than read from the diff: all six hold. A citation of 0 or 000 now gives "the review cites (independent <short> 0), and the report holds 1 finding"; `findings:[]` with no space, and `findings:   []`, end the examined list and declare no findings on both sides, reaching Done; a stray `Findings:` line and an `Examined: also the tests` note are now told "write that line as a - entry or take it out of the list", while only a real misorder is told to reorder; a setext heading underlined with - or = ends the header on both sides, so prose beneath it reaches Done, in LF and in CRLF; a blank line before an indented continuation joins it in findings and in examined, on both sides; and a doubled `commit:` line no longer stands in for naming the commit, since every `^commit:` line is stripped
  - the four regressions the fixes could have caused, at 09abf7f7: a citation the check should accept (an earlier round's 7-hex on its own line, "01" for 1, a 40-hex whose first seven match) is still accepted, and only the same-line pair below is not; the examined boundary eats nothing in silence, since `findings:x`, `findings:[] and more` and a repeated `findings:` line are all read or named; the continuation no longer needs a preceding non-blank line but still refuses to swallow a line reading `open:`/`resolved:` in any case or a nested bullet that reads as a finding; the heading rule ends the header early only for the `---` shapes in finding 1
  - about fifty further hand-built pairs run through the wake at 09abf7f7: a bogus 7-hex and a non-hex commit: line in both records (named by LOOP-058, never Done), untracked records, a report deleted on disk but present in HEAD, an empty report file, a citation against a `findings: []` report, `findings: []` followed by a real `open:` bullet, tabs and `*` bullets, numbered entries, and the ATX hint "the fields sit under a heading", which fires for `#` headings only
  - the real committed pair at 09abf7f7 run end to end: with both commit: lines and the citations rewritten to a fixture HEAD, .cairn/reviews/reviews-carry-an-independent-report.md and its .independent.md reach "Done: ... the review at <sha> and its independent report are clean", and the review carries all six nineteenth-round findings as resolved with the citation last
  - all 68 files in .cairn/reviews at 09abf7f7 read through the reader, each installed as the current commitment's review with only its commit: line rewritten to the fixture HEAD and a clean report beside it: 61 are read (Done), and 6 are refused, the same six as the nineteenth round: command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md and skills-can-be-installed-with-the-skills-cli.md because a later `verification:` field sits inside the examined: list and is named as a line the loop cannot read, and the-answer-reaches-the-agent.md and the-wake-names-an-action-or-refuses.md because their only finding entry is the word "none", an unrecognized prefix; all six are legacy records for finished commitments that no gate reads again. The 68th file is the .independent.md report itself, which is refused only because my harness installed it as a review, where its unprefixed entries are unrecognized
  - limits I confirmed and do not report as defects, because the specification or AGENTS.md states them: the kernel cannot prove the reviewer had none of the build's context (LOOP-020's own note), it cannot tell a falsely resolved finding from a real one, LOOP-032's no-code-during-review is not mechanized, and a bullet under a heading is prose, so a report that lists findings above and notes under a heading, or declares `findings: []` and bullets text below, reaches Done with the lower bullets unread, exactly as AGENTS.md says
findings:
  - A `---`, `--` or `===` line directly after a field line is read as that field's setext underline, so the header ends one line early and the loop names a field that is plainly present: the review "commitment: first\ncommit: <sha>\nexamined:\n  - everything\nfindings: []\n---\n\n## Notes" is refused with "findings: is missing; write findings: [] when there are none (LOOP-086) (LOOP-108)", a `---` after the commit: line gives "examined: needs a nonempty list of what the review examined", the report side gives "needs findings: as a list of - entries, or findings: [] for none", none of them mentions a heading boundary because the "the fields sit under a heading" hint tests `#` headings only, and the same rule drops an indented continuation a `---` underlines without naming it.
  - A review line that carries this report's finding and also cites an earlier report, at another commit, is refused as though it double-carried: against a report holding the one finding "the gate is wrong", the review "findings:\n  - resolved: the gate is wrong, fixed (independent aaaaaaa 3) (independent <short> 1)" gives "the independent report's finding 1 is not carried: its review line cites another finding too", in either citation order, although "aaaaaaa 3" names another round's report and tests/independent.test.mjs accepts that same citation on a separate line.
