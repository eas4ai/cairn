commitment: reviews-carry-an-independent-report
commit: 5d70311f
reviewer: an independent reviewer given the commitment, its requirement texts and the commit range, and none of the build's context
examined:
  - Setup: cloned /home/shawn/workspace2/cairn-dev into my own scratch clone and checked out 5d70311f, the commit under review; every command ran as node bin/cairn.mjs inside that clone or inside throwaway fixtures built from its tests/helpers.mjs, and I wrote nothing in the checkout under review.
  - Read docs/commitments/reviews-carry-an-independent-report.md, the texts of LOOP-020, LOOP-032, LOOP-033, LOOP-058, LOOP-059 and LOOP-071, PKG-004 in docs/spec/package.md, AGENTS.md, skills/new-project/templates/AGENTS.md, docs/manual.md, docs/decisions/the-working-agreement-matches-the-gate-on-a-heading-that-names-findings.md and the other decision records the five fixes touch, and the record reader in bin/cairn.mjs: independentGap, listOf, headerOf, reviewOf and the markup-tolerant prefix sweep.
  - The gate, by experiment rather than by reading: about sixty review-and-report pairs committed in throwaway repositories and judged by what node bin/cairn.mjs wake printed, each beside a control differing in one detail; a clean pair reaches Done at these fixtures, so every refusal I report below is the variation and not the fixture.
  - Heading rule: a heading whose title names findings was refused in every shape I could write it, hashed at levels two and six, with closing hashes, underlined, singular, beside a list that declares none and beside a list that holds entries, and holding a paragraph, a bullet list, a table, a subsection or nothing at all; a heading that does not name findings was read as prose in the same positions, and a fenced example heading was correctly ignored.
  - Nested entries and blank lines: entries after one or two blank lines, blank lines between entries, wrapped entries, numbered, parenthesised, asterisk and plus bullets, task boxes, margin-level entries and entries interrupted by a rule all survived intact; a nested bullet that reads as a finding of its own is named rather than swallowed, and a nested bullet that does not is joined to the entry above it, so its words still have to be carried word for word.
  - The prefix sweep: I placed the finding prefix in twenty markup spellings in an examined entry, in body prose, under a heading, inside a fence, inside an HTML comment, behind quote markers, bold markers, a task box, a numeral and an HTML entity for the colon, and every one of them was refused in both the report and the review; a word that merely contains it, such as the unresolved one, was correctly not taken for it.
  - I could not build a report that loses a finding in silence. The only shapes that reach Done while a defect is written somewhere are the ones AGENTS.md and docs/manual.md name as an accepted limit: a defect written as ordinary prose, or as a bullet beside a declared-empty list, under a heading that does not name findings and without the prefix.
  - Fix 1, the flake entry: read the receipts themselves rather than the review's description of them. Receipt .cairn/evidence/runs/20260918T143948080Z-2616244 is at 792b8774, exits 1, records 538 tests with 537 passing, and its .out names exactly one failure, the SPEC-018 block-Status test in tests/agreement.test.mjs; receipt 20260918T151547880Z-207881 is at ebbe1360 and names the retention test in tests/scope-retention.test.mjs. Both files pass alone, 7 of 7 and 13 of 13, and the standing receipt 20260918T151720876Z-313044 is 538 of 538. Every figure and every file name in that entry is true.
  - Fix 2, listOf: bin/cairn.mjs line 977 now pushes each section's title alone and line 980 reads it; no reader of a section's level, start or body remains anywhere under bin/, and no .title survives from that structure. tests/independent.test.mjs is 35 of 35 and the kernel is 1731 lines against 1732 at 9cfbacc4, as the review says.
  - Fix 3, the test statements: the three the previous report named are corrected, but the same superseded rule survives twice more in the same file, which is finding 1 below.
  - Fix 4, AGENTS.md: the two copies are byte-identical, sha256 dca73623fc34ed438216 and 19515 bytes each; the sentence is gone from where it sat, and the findings-heading paragraph at lines 131 to 141 now says the title alone refuses such a heading, that an empty one is refused like any other and that nothing beneath it is read, which is exactly what I measured the gate doing, in both records, in every shape above.
  - Fix 5, the ceiling records: node bin/cairn.mjs reversals prints 7 of 88 and PKG 4, and lists the-complexity-ceiling-is-1600-lines to the-kernel-ceiling-rises-to-2000-lines with the cause that the stated condition occurred; PKG-004 reads 2000 in docs/spec/package.md, scripts/pkg-lint.mjs line 61 and tests/pkg-lint.test.mjs line 35. Two defects in how that fix is written up are findings 2 and 3 below.
  - The suite: node --test tests/*.test.mjs twice in the clone at 5d70311f, 538 tests, 538 pass, 0 fail both times, with no flake in either run.
  - Deliberate state left alone as the brief directs: the five version files at 0.6.0, the missing 0.7.0 changelog entry and the absent v0.7.0 tag.
findings:
  - tests/independent.test.mjs still states the superseded heading rule in two more places that the previous round's third fix did not reach and that its closing claim denies: line 456 titles a test "a findings-titled section is prose beside a list that holds entries", and line 469 comments "A findings-titled section is elaboration when the record's own list holds entries, and the silent-loss shape only when it declares none", while the loop on lines 470 and 471 immediately below asserts that the same three titles are "refused beside any list" and the gate does refuse them: I committed a report whose findings list held one entry with a "## Findings detail" heading over prose alone, and wake answered Resolvable: review first, naming that heading. Both lines are unchanged from 9cfbacc4, so the review's claim that "a search of the whole repository for either phrasing returns nothing" holds only for the two phrasings it searched, and the file every future reader learns the rule from still teaches the rule the decision record the-working-agreement-matches-the-gate-on-a-heading-that-names-findings replaced (wording)
  - docs/decisions/pkg-045-yields-to-loop-139-when-both-would-claim-the-wake.md now contradicts itself inside one line: its History opens "The PKG domain carries four reversals", names all four, and then says "each of the three reversed a rule about what Cairn ships or how much of it there is"; node bin/cairn.mjs reversals prints PKG 4, so the count the level argument rests on is the stale one this fix existed to correct, and the same line already records that an earlier draft of it said PKG carried none, making this at least its third number and still not internally consistent (wording)
  - docs/decisions/the-complexity-ceiling-is-1600-lines.md carries its new "Superseded by: the-kernel-ceiling-rises-to-2000-lines" line inside the field block, immediately above "Cause: an unforeseen condition occurred", which is that record's cause for superseding complexity-ceiling-1500-lines and not the cause of the reversal the new line announces; node bin/cairn.mjs reversals prints "the stated condition occurred" for that pair, and all six other superseded records in docs/decisions place the stamp alone on its own line directly under the title, which is where cairn decide writes it at bin/cairn.mjs line 1488, so a reader of the record is handed the wrong cause for the reversal it announces (wording)
