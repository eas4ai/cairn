commitment: reviews-carry-an-independent-report
commit: 362eb5a7
reviewer: an independent reviewer given only the commitment, the requirement texts named in the brief, the commit range and the twenty-second round's six defects, working read-only in a fresh clone
examined:
  - a scratch clone of the repository checked out at 362eb5a7, with every case committed on a test branch whose parent is 362eb5a7 and run through `node bin/cairn.mjs wake`, so the review named 362eb5a7 and the report named 362eb5a
  - the six defects of the twenty-second round, each rebuilt as a concrete record pair at 362eb5a7: further findings under a heading titled findings (named as a repair), a commitment:/commit: line quoted lower in the header (named as an unreadable list line, the real field kept), a rule under a field the kernel does not read (`scope: the whole commitment` then `---`, read through to Done), a report for another commitment or commit whose fields sit below a heading (a new reviewer, before any repair), the review's fields below a heading and below prose (both explained as the report's are), and the review's commit: joined by the line below it (named as a joined value); all six hold
  - bin/cairn.mjs at 362eb5a7: fields, recordFields, headerOf, listOf, ENTRY, FINDING, ATX, SETEXT, FIELD, entryFix, reviewOf, independentGap and the wake path from reviewOf to Done
  - docs/spec/loop.md LOOP-020 and its falsifier, LOOP-032, LOOP-033, LOOP-058, LOOP-071, LOOP-086 and LOOP-108, and AGENTS.md's paragraphs on the review record, its lists, the findings-titled heading and the independent reviewer, at 362eb5a7
  - tests/independent.test.mjs and tests/record-boundaries.test.mjs, then `node --test tests/*.test.mjs` at 362eb5a7: 520 pass, 0 fail
  - all 67 committed records in .cairn/reviews/ read through the reader at 362eb5a7, each copied to the current commitment's review path with its commit: line re-dated: 61 are accepted, and the 6 refused are command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md and skills-can-be-installed-with-the-skills-cli.md, whose `verification:` field sits between examined: and findings: and is named as a line the examined: list cannot read, and the-answer-reaches-the-agent.md and the-wake-names-an-action-or-refuses.md, whose `- none` entry is named as unrecognized under LOOP-086; both shapes are refused by the grammar AGENTS.md states, and the committed review and its .independent.md for this commitment, re-dated to 362eb5a7, read cleanly and reach Done with every citation resolved
  - attacks beyond the six: a report edited on disk or untracked (both named `commit <path>`), a report naming the commit only on its commit: line, doubled and out-of-order fields, prose and bullets under headings of either form, and a review header quoting a field name in its body
findings:
  - A list entry written at the left margin and followed by a `---` or `===` rule is read as a setext heading with the rule as its underline, so a report whose examined: entry is the unindented line `- bin/cairn.mjs at 362eb5a7` followed by `---` and then `findings: []` is refused with "names findings below a heading, where the header ends; move the record's fields to the top", though the fields already are at the top and no message names the rule; the same shape refuses a review, an indented entry before the rule is accepted, and the code's own comment at headerOf claims "a list's own line is not heading text" while FIELD never tests ENTRY (blocked).
  - A setext-underlined heading whose title holds a colon with text after it is read as a field line rather than heading text, so a report ending `findings:` with one entry, then `Note: what else I read`, an underline of dashes and a prose bullet, is refused with "findings: holds a line the loop cannot read as an entry: \"Note: what else I read\", and the entries after it are unread" and told to "put a heading above prose that follows the findings" when that heading is already there; a review with the same section is refused the same way, while a title ending in a colon is read as a heading (blocked).
  - A review whose complete header sits below a single prose line and carries no `commitment:` line is refused with "commit: names the commit the review examined" although its commit: line is present, because the below-prose explanation added in the twenty-second round fires only when `commitment:` is in the record; the named repair does not fix it, since what needs moving is the prose (blocked).
  - A review whose header is complete and readable is refused with "it does not read its commitment:, because the line above it is neither a field nor a heading" when any line of its prose below a heading begins with `commitment:`, as a review discussing the record format will; the verdict is decided by a body line that LOOP-071 says the loop must not read as metadata, and moving the fields, which the repair asks for, changes nothing (blocked).
  - A report whose header field is spelled `Commit:` with a capital C, or `commit :` with a space before the colon, or which omits only its `commitment:` line, is discarded for a whole new reviewer ("names no commit, which is not a commit", "names commitment none, not reviews-carry-an-independent-report") rather than repaired, and the message never names the spelling; the same three shapes in the review are repairs, and the loop elsewhere takes care to name a field written in another case rather than act on it (blocked).
  - A real further finding a report writes as a bullet under a heading whose title lacks the word "findings", such as "## What else I saw" holding "- the wake accepts a review whose commit line is empty, losing the gate", reaches Done with nothing named; a report's findings carry no open:/resolved: prefix, which is the form this round's reviewer was asked for, so the finding-shaped test can never fire in a report and the heading's title is the only guard the twenty-second round's fix left, though AGENTS.md at 362eb5a7 calls a bullet under a heading prose and next-iteration already holds reviews-and-reports-are-written-in-a-form-the-loop-reads for this class (silent).
  - When a record declares `findings: []` and explains under a heading titled "Findings" what it tried, in prose bullets, the refusal is correct by AGENTS.md's rule that a heading naming findings holds no list, but the repair says only "they belong in the findings: list above the first heading" and "give a finding its own - entry, put a heading above prose that follows the findings"; neither names the fix, which is to retitle the heading, and following the message as written would turn notes into fabricated findings (wording).
