commitment: reviews-carry-an-independent-report
commit: 83dec7ed
reviewer: a twelfth independent subagent given the commitment, LOOP-020, the working agreement paragraphs and the commit range a9cd734c..83dec7ed, with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 with its falsifier, and LOOP-086, LOOP-071 and LOOP-108 in docs/spec/loop.md, against the AGENTS.md paragraphs on the review record, the findings list and starting the independent reviewer
  - git log and git diff a9cd734c 83dec7ed for bin, tests and docs at commit 83dec7ed: listOf line by line and both callers, independentGap and reviewOf, plus recordFields and withoutFences, which still supply commit: and commitment:
  - the eleventh independent report at .cairn/reviews/reviews-carry-an-independent-report.independent.md and the six claimed resolutions in .cairn/reviews/reviews-carry-an-independent-report.md, each retested as a concrete record
  - the whole suite in a scratch clone at 83dec7ed with TMPDIR outside the repository: 510 tests, all pass; tests/independent.test.mjs and its new reader test for what they assert and what they leave out
  - about thirty hand-built review and report records at 83dec7ed in that clone run through node bin/cairn.mjs wake: numbered and starred lists, tab indents, blank lines between entries, entries missing their dash, nested sub-bullets, prose between entries, two-paragraph findings, notes after the list, and findings under a heading
  - this repository's own review and report read by listOf at 83dec7ed: 12 examined and 57 findings entries in the review, 6 and 6 in the report, nothing unread, so the real records are read whole
findings:
  - A findings entry that lost its "- " and sits indented under the entry above is glued onto it instead of being named: a review holding "- resolved: one, fixed" and then "  open: the cache is never invalidated" reports Done with "the review at <commit> and its independent report are clean", so the eleventh round's finding 2 is fixed only when the unreadable line is unindented or the first thing in the list, and the LOOP-086 falsifier's own shape still hides an open finding in second place; reproduced with a 2-space line, a 4-space line and a nested "- open:" sub-bullet, and the wake says nothing about the line it joined.
  - Once one entry has been read, a line the reader cannot read is named only when it begins with open: or resolved:, so any other interruption ends the review's list in silence and every entry after it is dropped: a plain sentence between entries, or a finding written as two paragraphs, makes a following "- open: <defect>" disappear and wake reports Done; reproduced both ways in a scratch clone at 83dec7ed.
  - The same silence is total in the independent report, whose findings never begin with open: or resolved:, so no unreadable line in it is ever named: a report with "- one", an unindented sentence such as "I also read the tests.", and then "- two is broken too" reaches Done with "its independent report are clean" while finding 2 is carried nowhere, which is the LOOP-020 falsifier; reproduced with and without blank lines around the sentence.
  - A report whose findings sit under a heading is neither read nor named, because the heading check matches only a bullet beginning open: or resolved:, which a report never writes: a report with an empty findings: list or findings: [] and then "## Findings" listing two numbered defects reaches Done with "its independent report are clean", so the eleventh round's finding 6 is fixed for the review only and the report side is unguarded.
  - In the review, a finding-shaped bullet under a heading is named only when nothing was read above it: a review that carries one resolved entry in its header list and then lists "- open: a defect still unfixed" under "## Open" reaches Done with no word about it, so the second half of the eleventh round's finding 6 is not resolved, and LOOP-071 does not force this, since the loop already refuses that record rather than reading it when the header list is empty.
  - The refusal for an indented line directly under a review entry still blames the wrong thing: with the note or sub-bullet joined to the entry, the message is "a review line cites (independent <commit> 1) but does not end with it; put the citation last on its line" although the citation already ends its line and the glued line is the cause, so the eleventh round's finding 5 is fixed only for the blank-line case and a developer cannot tell which line to move.
  - The working agreement was not updated for the new reader: AGENTS.md still tells the builder the findings list "is one unbroken list of `- ` entries above any heading: a blank line inside it ends it", which this kernel contradicts, says nothing about numbered entries, nested details or the line it now refuses to read, and never states that the independent report must carry commitment:, commit:, examined: and findings: above the first heading, although the kernel refuses a verbatim report that lacks them.
