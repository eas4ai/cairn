commitment: reviews-carry-an-independent-report
commit: a84cc814
reviewer: I had none of the build's context; I cloned the repository to my own scratch directory, checked out a84cc814, and never wrote in the working copy under review.
examined:
  - the gate at a84cc814: independentGap, listOf, headerOf, reviewOf and the Done branch of wake in bin/cairn.mjs, plus withoutFences in bin/spec.mjs
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020, LOOP-032, LOOP-033, LOOP-086 and LOOP-108 in docs/spec/loop.md, and the record-grammar paragraph of AGENTS.md
  - twenty-four review/report pairs built as real records in my clone, each committed and each answered by a run of `node bin/cairn.mjs wake` at a84cc814: a clean pair with one carried finding and a `findings: []` pair both reach Done, so the setup is sound
  - this round's three changes: a findings-naming heading over a table, a paragraph, an empty section, a subsection titled "Findings I set aside" and a setext title underlined with `---` (all refused); a report so headed answered with `review <slug>`; and a blank, a prose-only and a field-less report
  - the previous round's fixes, all of which hold: a bare `findings:` above a body list is told to write `findings: []`, a bare and an unreadable `examined:` are repairs, a report at 74848eb4 is replaced rather than repaired, and `node scripts/spec-lint.mjs docs/spec` names an unclosed fence as "loop.md:1764: a fenced example is never closed"
  - `node --test tests/*.test.mjs` at a84cc814: 525 tests, 525 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, exit 0
findings:
  - open: a bullet that begins `open:` or `resolved:` under a body heading is recognised only when it is the first bullet in the body; any bullet before it hides it and the wake reaches Done with nothing named. A report with `findings: []`, a `## Method` section holding `- I read bin/cairn.mjs`, and a `## What I saw` section holding `- open: a second defect the loop never names` reaches Done. Moving the `open:` bullet ahead of the other one turns the same report into a repair. The cause is `below` in listOf: `rest.matchAll(ENTRY with gm)` whose `(\S[\s\S]*)$` swallows the rest of the document into one match, so `below.some(x => /^(?:open|resolved):/i.test(x))` tests only the first bullet. The identical record as the agent's own review (`findings: []` plus `## Notes` holding a plain note then `- open: my own unresolved defect`) also reaches Done, so LOOP-086 loses the finding too. This is not the accepted prose limit: AGENTS.md promises the loop "refuses only what it can recognise: a bullet that begins `open:` or `resolved:`", and here it recognises it or not depending on position (silent)
  - open: a report whose `findings:` list the loop reads and the review carries is still refused, and a whole new reviewer demanded, for the title of an explanatory section. A report with `findings:` holding one finding, carried by the review as `resolved: ... (independent a84cc81 1)`, plus `## Findings in detail` over a paragraph, gives `review <slug>` with "writes the heading "Findings in detail", whose title names findings, where the loop reads no finding" -- and the loop did read that finding, from the header list, and accepted the same report when only the title changed to `## Detail` (which reaches Done). `## No findings` over "I found nothing." and `## Summary of findings review process` over prose are refused the same way. The heading test ignores whether the header list was read, so an honest and complete report is discarded for a section title the reviewer was never told to avoid (blocked)
  - open: the answer to a report whose findings sit under `## Findings` as `- open:` bullets is a repair the next wake throws away. A report with `findings: []` and `## Findings` holding `- open: the gate mis-reads a heading title` gives `repair <report>`: "move them into the findings: list above the first heading". Doing exactly that, and changing nothing else, gives `review <slug>` on the next wake, because the now-empty `## Findings` heading is refused for its title alone. The repair cannot be completed as written; only deleting the reviewer's heading, which the same message forbids, gets past it. Note also that "shaped" wins over "named" in listOf, so which of the two answers a findings-titled heading gets depends on whether a bullet under it happens to begin `open:` (blocked)
  - open: a report that carries some but not all of the four fields gets a repair first and a new-reviewer demand second. A report holding `examined:` with one entry and `findings: []`, but neither `commitment:` nor `commit:`, gives `repair <report>`: 'add "commitment: reviews-carry-an-independent-report" above its fields'. Adding exactly that line gives `review <slug>` on the next wake: "names no commit, which is not a commit". A report holding only `findings: []` behaves the same. The `bare` test requires all four fields to be absent, so a report short of the one field the kernel can never supply is still sent round the repair first (blocked)
  - open: a report or review that opens with an ordinary Markdown title is refused, and the repair asks for the reviewer's heading to be removed while claiming the opposite. `# Independent report` above `commitment:`, `commit:`, `examined:` and `findings: []` gives `repair <report>`: "names examined and findings below a heading, where the header ends ... keep the record's fields at the top, each on its own line, put prose after a heading, and change none of the reviewer's words". The only fix is deleting the title. The kernel reads the record half-way: recordFields skips one `# ` title and reads `commitment:` and `commit:` past it, while headerOf and listOf end the header at the same line and lose `examined:` and `findings:`. The same file as the agent's own review gives the matching `repair` under LOOP-108 (blocked)
  - open: the "carries no record" message states something false whenever one of the four fields is present but empty. A report holding only `examined:` with no entries gives `review <slug>`: "carries no record: none of commitment:, commit:, examined: or findings: is a field at its top" -- `examined:` is a field at its top, and it is the empty list, not the missing field, that the reviewer must be told about. The `bare` test uses `!early.examined.entries.length`, so a present-but-empty `examined:` counts as absent in the test and is denied in the message (wording)
  - open: the fix clause attached to a report repair is generic and, in one case, self-contradictory. A bare `examined:` gives "needs a nonempty examined: list; keep the reviewer's own words: give a finding its own - entry, put a heading above prose that follows the findings", none of which concerns `examined:`; an unreadable `examined:` line gets the same findings-shaped advice because entryFix returns null for it. The findings-under-a-heading repair says "keep the reviewer's own words" and "reword a note that begins open: or resolved:" in one sentence, which are opposite instructions about the same text (wording)
  - open: every `review <slug>` message tells the agent to brief the replacement reviewer with "the commitment, its requirement texts and the commit range", and AGENTS.md says to give it "only the commitment file, its requirement texts and the commit range". Neither names the record form the gate enforces, so a reviewer refused for a section titled "Findings in detail", for omitting `commit:`, or for opening with a title, is replaced by a reviewer able to fail in exactly the same way, at the cost of a full round each time. The brief should name the form, or say what to tell the new reviewer differently (wording)
