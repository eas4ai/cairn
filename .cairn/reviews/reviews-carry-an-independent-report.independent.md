commitment: reviews-carry-an-independent-report
commit: 74848eb4
reviewer: an independent reviewer with none of the build's context, given the commitment file, the requirement texts named in the brief, the commit 74848eb4 and the twenty-fifth round's outcome, working read-only in its own scratch clone
examined:
  - a scratch clone checked out at 74848eb4, every case written as a review/report pair and committed on a branch off 74848eb4 that touches only .cairn/reviews (102 commits, `git diff --name-only 74848eb4 HEAD` lists nothing else), each run through `node bin/cairn.mjs wake`; a clean pair with one carried finding and a `findings: []` pair both reach Done at 74848eb4, so every refusal below is the case and not the setup
  - the six fixes of the twenty-fifth round, each rebuilt as a concrete record at 74848eb4 and run, never read from the diff: a table and a paragraph under `## Findings`, `## FINDINGS`, `##Findings` and an underlined `Findings` are all named as "has content under the heading"; a bulleted `## What else I read` with no findings-naming heading is Done and a distant `## Findings I could not place` is now named by its own title; a bare `findings:` above a list is told to write `findings: []`; a bare `examined:` and an `examined:` holding `verification: ...` are named as needing entries rather than as a field the parser never reached; a report at 8efe6c02 and short of its `commitment:` line is replaced, while the same report at 74848eb4 is repaired; and `node scripts/spec-lint.mjs` reports "loop.md:1762: a fenced example is never closed" with exit 1 on a copy of docs/spec with an unterminated fence, while this repository's own docs/spec is clean. All six hold, with the two gaps in the heading rule recorded below
  - what the fixes could have broken: the scalar-field branches of independentGap after the reordering (prose above `commit:`, `Commit:` at the margin, fields under an H1 title and under a later heading, a report with no `findings:` line and one with no `examined:` line), the findings-naming heading in capital, plural, no-space, underlined and fenced forms, a section separated by a rule rather than a heading, subsections under a findings-naming heading, a citation followed by a full stop, an uncommitted report, an empty and a whitespace-only report, and a report short of only its `commit:` line
  - the kernel at 74848eb4: fields, recordFields, headerOf with ATX/SETEXT/ENTRY/OWN, listOf with its new section scan and its shaped/named/undeclared cases, entryFix, reviewOf, independentGap with its wanted, hidden, blocked and spelled branches and its carry and citation rules, withoutFences in bin/spec.mjs, and the unclosed-fence scan in scripts/spec-lint.mjs
  - the same shapes run against the kernel at 70292c06^ in a second clone, to separate a regression from a limit: the subsection case was refused there and is Done at 74848eb4, and the honest records named below were Done there
  - docs/spec/loop.md LOOP-020 and its falsifier, LOOP-032, LOOP-033, LOOP-058, LOOP-071, LOOP-086 and LOOP-108; AGENTS.md on the review record, its lists, the findings-titled heading, the stated limit and the independent reviewer; the independent-report paragraph of docs/manual.md; .cairn/escalations/loop-020-loop-086.md; the commitment file; and the committed review and report
  - tests/independent.test.mjs including the new round-25 case, tests/record-boundaries.test.mjs and tests/spec-lint.test.mjs, then `node --test tests/*.test.mjs` at 74848eb4: 524 pass, 0 fail
  - all 68 files in .cairn/reviews/ at 74848eb4 read through the reader, each copied to this commitment's review path with its first `commit:` line re-dated to 74848eb4 beside a clean report: 60 accepted, 8 refused -- the four with a `verification:` line inside `examined:` (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, skills-can-be-installed-with-the-skills-cli) as "examined: holds a line the loop cannot read as an entry"; the two listing `- none` under `findings:` (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses) as 'finding 1 is unrecognized: "none"'; this commitment's own .independent.md, whose prefixless findings are unrecognized when it is read as a review; and freshness-and-scope-guidance-agree.md, newly refused for its `## Documentation review finding` section
findings:
  - A heading whose title names findings still hides content in its subsections: a report with `findings: []`, `## Findings`, `### 1. The gate drops an entry` and one prose line reaches Done at 74848eb4 with nothing named, and the same shape with a bullet under `### Detail` was refused by the kernel at 70292c06^ ("holds findings under a heading") and is Done now, because the section scan gives a findings-naming heading only the lines before its first subheading; a rule after the first content line does the same, since `text` over `---` becomes a heading of its own, and both shapes pass in the builder's own review too (silent).
  - A heading whose title merely mentions findings is refused although every finding is in the list: a report with one carried finding plus `## How I reached the findings above` and a single method sentence is refused with "has content under the heading", and this repository's own committed review .cairn/reviews/freshness-and-scope-guidance-agree.md, whose four findings are all in its `findings:` list, is refused for its `## Documentation review finding` prose section; both reach Done on the kernel at 70292c06^, and the only repairs named, moving the findings or retitling the heading, change the reviewer's words, which the same verdict ("keep the reviewer's own words", "never edit a reviewer's report to fit") and AGENTS.md ("Keep its report verbatim") forbid (blocked).
  - AGENTS.md and docs/manual.md still say the loop "refuses only what it can recognise: a bullet that begins open: or resolved:, and any list under a heading that names findings", and AGENTS.md that such a heading "holds no list", while the kernel at 74848eb4 refuses a table row or a paragraph there as well; 70292c06 changed neither document, so an agent or reviewer writing to the agreement cannot predict the refusal it gets (wording).
  - An empty report is told to add a line and then thrown away: a committed zero-byte or whitespace-only .cairn/reviews/reviews-carry-an-independent-report.independent.md is answered with 'carries no commitment: line; add "commitment: reviews-carry-an-independent-report" above its fields', and once that line is the file's whole content the next wake answers "names no commit, which is not a commit; start a new reviewer", so the named repair does not make the record acceptable -- the repair-then-discard pattern the twenty-fifth round's second finding closed only for a report at another commit (blocked).
