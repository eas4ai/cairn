commitment: reviews-carry-an-independent-report
commit: d88e131a
reviewer: a fresh subagent with none of the build's context, given the commitment file, LOOP-020 and its falsifier, LOOP-086, LOOP-071, LOOP-032, LOOP-033, LOOP-058 and LOOP-108, the working agreement's paragraphs on the review record and the reviewer, and the commit d88e131a; it built every record by hand in a scratch clone and judged only what `node bin/cairn.mjs wake` printed
examined:
  - the reader and the gate at d88e131a: recordFields, headerOf, listOf, entryFix, reviewOf and independentGap in bin/cairn.mjs, read line by line against LOOP-020, LOOP-086, LOOP-108 and LOOP-071 and against the AGENTS.md paragraphs on the review record, its lists and starting the reviewer
  - each of the twenty-first round's six defects retested at d88e131a as a built record run through wake in a fixture repository, never by reading the diff: an underlined "Attacked:" title in the review and in the report (both Done); a report whose fields sit below a `# ` title, below a setext title, below a prose line, and with prose between commitment: and commit: (all four now repairs, the last two naming the line above and the joined value); the report's repair now naming "move the record's fields to the top, each on its own line, above any heading or prose"; the kernel's advice for an unrecognized entry now reading "or write findings: [] when there are none"; README.md's reviewed(c) and done(c) now naming report(c) and docs/manual.md carrying both the "The review alone is not enough" paragraph and a Get unstuck row for `review <slug>`; and `---`, `===`, `--`, `====`, `-----` between commit: and examined: in the review and in the report (all ten Done), plus a rule inside a findings list above a real open finding (still named)
  - what those fixes could have broken, at d88e131a: an underlined title whose text is one of the five whitelisted field names; a horizontal rule under a field the whitelist does not name, in the review and in the report; an indented rule inside a findings list; a well-formed record led by a `# ` title or by a field-shaped prose line; findings and prose under hashed and underlined headings with and without an open:/resolved: prefix; a stale and a foreign report with a title above the fields; a report missing only its commitment: line and one missing only its commit: line; a report untracked, and a committed report deleted from the tree; findings: written above examined:; and a comparison of recordFields at d88e131a with recordFields at 49012dc0, the commit that opened this commitment, to separate a new behavior from an old one
  - every committed record in .cairn/reviews/ at d88e131a read through the real reviewOf, imported from a copy of bin/cairn.mjs with only its two entry-point lines removed, and the same 68 files read through the kernel at 49012dc0: six reviews are refused and none of them by this commitment's work - command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn and skills-can-be-installed-with-the-skills-cli each carry a margin-level "verification:" field inside examined: ('examined: holds a line the loop cannot read as an entry: "verification:", and the entries after it are unread'), and the-answer-reaches-the-agent and the-wake-names-an-action-or-refuses each list "- none" under findings: ('finding 1 is unrecognized: "none"'); all six are refused by the kernel at 49012dc0 too, the other 61 read clean with no open finding, and reviews-carry-an-independent-report.independent.md is the report, never read as a review by reviewOf
  - the shipped documents against what the kernel does: README.md's verdict block and the paragraph on what the definitions leave out, the Review finding format and Decide what Done means for you and Get unstuck sections of docs/manual.md, and AGENTS.md against skills/new-project/templates/AGENTS.md (byte-identical at d88e131a); and the whole suite at d88e131a, `node --test tests/*.test.mjs`, 519 tests, all passing, plus tests/independent.test.mjs and tests/record-boundaries.test.mjs read against the behavior I reproduced
findings:
  - A report that lists at least one finding above the first heading and more findings under a heading, with no open:/resolved: prefix, reaches Done and the ones under the heading are never named: a report whose findings: list held "the gate accepts a record it must refuse" and whose "## Findings in full" section held "- wake exits 3 on an empty report" and "- the message names the wrong file" printed "Done: first ... and the review at 0f4063d and its independent report are clean" with only finding 1 carried, while the identical report with those two written "open: ..." is refused with "holds findings under a heading, where the loop does not read them" - and since a report's findings need no prefix (independentGap's norm makes it optional and tests/independent.test.mjs writes bare text), the heading check is inert for exactly the shape the kernel expects. (silent)
  - The report's commitment: and commit: are read by recordFields, which takes the last margin-level field-shaped line above the first heading, so a complete honest report whose prose quotes one is sent back for a whole new reviewer rather than repaired: a report ending "---" then "commitment: someone-else" gives "names commitment someone-else, not first; start a new reviewer ... never edit a reviewer's report to fit", and one ending "commit: 0000000" gives "names 0000000, which is not a commit", while the review with the same quoted line gets the precise repair 'findings: holds a line the loop cannot read as an entry: "commit: 0000000"; write each finding as a - entry, and put prose after a heading'. (blocked)
  - headerOf treats a rule as an underlined heading unless the line above it is one of commitment, commit, examined, findings or reviewer, so a record that carries any other field above a rule reads as having its fields below a heading: a report with "range: aaaaaaa..{short}" above "---" is refused with "names examined and findings below a heading ... move the record's fields to the top, each on its own line, above any heading or prose" although they already are, and the review with that same line is refused with "examined: needs a nonempty list of what the review examined", while the whitelisted "reviewer: r" above the same rule reaches Done; the fix is to delete the rule or rename the field, which neither message says. (blocked)
  - The new fields-below-a-heading repair runs before the commitment and commit checks, so a report that a new reviewer really should replace is edited first: a report at an earlier real commit with "# Independent report" above its fields gives "repair .cairn/reviews/first.independent.md ... move the record's fields to the top ... and change none of the reviewer's words", and only after that edit does wake say "names commit 3f32bab and the review names a9bffcd ... start a new reviewer", so the agent spends a cycle rewriting a report that is then discarded. (wording)
