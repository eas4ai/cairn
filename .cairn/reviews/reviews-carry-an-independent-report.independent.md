commitment: reviews-carry-an-independent-report
commit: 538ba9a0
reviewer: a ninth independent subagent given the commitment, LOOP-020, the working agreement paragraph and the commit range 03961bb1..538ba9a0, with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 in docs/spec/loop.md with its falsifier and the Revised 2026-09-17 note, the AGENTS.md "Review before Done" paragraph (byte-identical in skills/new-project/templates/AGENTS.md), and the review step in docs/walkthrough.md
  - git log and git diff 03961bb1..538ba9a0 for bin/cairn.mjs and tests/independent.test.mjs, the whole of independentGap as it stands at 538ba9a0, where wake calls it (after the staleness and LOOP-033 checks), and the fields, recordFields, asList and reviewOf code it reads both findings lists through
  - the eighth independent report and the builder's claimed resolution, in a clone at 538ba9a0: both of its cases are fixed. An uncommitted edit that adds a finding to a committed clean report, and a new report for a review redone at a later commit, are now named "commit .cairn/reviews/<slug>.independent.md" instead of Done or "start a new reviewer". An untracked first report, a staged-only report and a report deleted on disk are named the same way. The finding is resolved.
  - a scratch clone at 538ba9a0 with TMPDIR outside the repository: tests/independent.test.mjs is 7 tests, all pass; the whole suite is 508 tests, all pass
  - a simulated ninth round on the real review record at 538ba9a0: a long finding with quotes, backticks, colons and parentheses carried as open gave "resolve", the same finding carried as resolved gave Done; nine realistic carrying forms (em dash, semicolon, colon, square brackets, a parenthesis before "Resolved:", a capitalised first word, a trailing sha) all carried, and only a finding wrapped in quotation marks was refused, with the accepted form named
findings:
  - A blank line between two entries of the independent report's findings list silently drops every entry after it, so wake reports Done with a finding the review never carries, which is the LOOP-020 falsifier. Reproduced on the real report at 538ba9a0: two findings with one blank line between them and only the first carried gave "Done: reviews-carry-an-independent-report ... its independent report are clean", and removing the blank line refused the second finding correctly. A loose Markdown list is ordinary in a .md file, and nothing in AGENTS.md or the refusals tells the reviewer or the builder that a blank line loses findings. The same loss happens when further findings follow a Markdown heading, and a "*" bullet after a "-" entry is refused as "has a finding that wraps onto a second line", which names a cause the reader cannot match to the file.
  - The same blank line inside the review's own findings list drops every line after it, and the report gate reads that list, so an honest review is wrongly refused and an open finding can reach Done. Reproduced at 538ba9a0: a review carrying finding 1 and, after a blank line, finding 2 with the correct citation is refused with "no review line cites (independent <commit> 2)" although that line is present; and a review whose whole findings list follows a blank line reaches Done while holding "open: the gate never reads this". The report side of this exact case was fixed earlier in the commitment by naming it a repair; the review side was left unfixed.
