commitment: reviews-carry-an-independent-report
commit: 0735b118
reviewer: I had none of the build's context, read no diff and no session history, and worked read-only in my own clone of the repository at 0735b118; I wrote nothing in the project checkout.
examined:
  - the gate at 0735b118, read as source (bin/cairn.mjs: independentGap, reviewOf, listOf, headerOf, entryFix and the prefix test; withoutFences in bin/spec.mjs) and then judged only by what `node bin/cairn.mjs wake` actually did in my own clone
  - setup first: a clean pair with one carried finding reaches Done at 0735b118, and a pair whose two lists are both declared empty reaches Done, so the harness can tell acceptance from refusal
  - about thirty review-and-report pairs built, committed and put through `node bin/cairn.mjs wake` at 0735b118; every silent case below is paired with a control that differs in one detail and is refused
  - about 3,600 report shapes and about 1,100 review shapes screened through the kernel's own list reader before the survivors were confirmed by wake: a finding-shaped line in 42 markup spellings inserted at every line position of seven report templates, and in 20 spellings at every position of five review templates
  - the prefix test itself, in 42 spellings: emphasis, underscores, strike-through, code spans, HTML tags, a tag split across the word, entity-encoded colons in three forms, link and bracket forms, a backslash escape, capitals, numbered entries, a quote marker, a table row, and a gap of zero to five characters that are not letters. All are caught outside the lists except the two in finding 1; a gap of five is left alone, which matches the stated rule
  - the list boundaries: a bullet nested under a finding, a rule at the margin, blank lines, a second field below the list, the field written in capitals or with a space before the colon, the field indented or emphasised, a fence in either list, a fence that is never closed, a CRLF record, a record with a hashed title and one with an underlined title. Each is read or named; inside the header I found no unread region apart from finding 3
  - the carry and citation rules: a citation not last on its line, two review lines citing one finding, one line citing two, a number past the report's count, and a finding no line cites are each refused and named precisely; sound, with the one ASCII-only edge in finding 7
  - `node --test tests/*.test.mjs` at 0735b118 in the clone: 535 tests, 535 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo
  - all 68 records in .cairn/reviews/ at 0735b118 put through the review reader one at a time: 58 accepted, 10 refused. One of the ten is the independent report itself, which that reader is not meant to read; of the 67 review records, 58 are accepted and 9 refused, which matches the limit accepted on 2026-09-18
findings:
  - The prefix test loses the finding word when an HTML comment, or a character reference containing letters, stands between it and the colon. The comment markers are blanked before the tags are stripped, so the comment's own letters fill the gap and no reading of the line matches. A report bullet under a plain heading reading the finding word, then an HTML comment holding two ordinary words, then a colon, then a defect, reaches Done at 0735b118 with nothing named; the identical bullet without the comment is refused. The same bullet in the review record reaches Done although the review's own words say the defect stands, and its control is refused. The named reference for a non-breaking space and the numeric reference for a space are lost the same way, while a comment holding only a digit is caught, which is what shows the cause. (silent)
  - A report whose findings field carries one leading space is answered with a repair whose own words lose the reviewer's findings. The indented field joins the last examined entry and the findings beneath it become further examined entries, so the gate says the report needs that field as a list of entries, or the empty-list form for none, and tells the agent to add the empty-list form and change none of the reviewer's words. Doing exactly that reaches Done at 0735b118, with the reviewer's defect filed as something examined and no carry required. The control, the same report with the field at the margin, is refused because that finding is not carried. Nothing checks that a record the gate is about to let declare itself empty holds no findings elsewhere. (silent)
  - A bullet written in the header above the examined list is dropped with nothing named. In a report whose fields are followed by an entry-shaped line placed before the examined field, that line joins the preceding field's value in the record reader, appears in neither list, and the pair reaches Done at 0735b118. The same line carrying the prefix is refused, so the sweep does reach that position. The header above the first heading has no rule for a bullet belonging to no declared list, where below the first heading such a bullet is at least named while no findings are declared; the limit accepted on 2026-09-18 covers a heading's prose, not the header. (silent)
  - A fence inside the findings list is named as a fence inside the examined list. The examined test spans from the examined field to the end of the header, so it fires for a fence sitting after the findings entries, and it is tried before the findings test, in the report gate and in the review reader alike. Both messages then tell the writer to take the fence out of a list it is not in. (wording)
  - The rule that a report carrying the prefix outside its list is written again, never repaired, wherever the line sits, does not hold in two places. A prefixed bullet in the report's examined list, and a prefixed bullet nested under a finding, are both answered with the repair action on the report file and told to move the line into the findings list, because the examined test and the unread test run before the test that asks for a new report. For the same line one position higher the gate says that moving or rewording it would change the reviewer's words and asks for a fresh reviewer, so the two answers contradict each other. (wording)
  - Two values computed on every list read are never used: the test for a bullet, a claim or a table row under a heading that names findings, and the span of each parsed section. The first is the only remaining trace of a rule the gate no longer applies, which is why a table of defects under a heading not naming findings is prose today. (wording)
  - The word boundary added to the carry uses the ASCII word class, so a review line may continue the finding's last word with a letter outside ASCII and still carry it. A report finding ending in the word ok is carried at 0735b118 by a review line reading oke with an accent that then denies the defect. The reviewer's words stay on the page and the project ships ASCII, so this is narrow. (wording)
