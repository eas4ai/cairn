commitment: reviews-carry-an-independent-report
commit: a48ccce4
reviewer: I had none of the build's context, read no session transcript and no diff of the build's working history, and worked read-only in my own clone; I wrote nothing in the project checkout.
examined:
  - the released state at a48ccce4 in my own clone, read as source (bin/cairn.mjs independentGap, reviewOf, listOf, headerOf, entryFix and the line test for the finding prefix; scripts/release.mjs; scripts/pkg-lint.mjs; bin/hook.mjs and bin/hooks/) and then judged only by what node bin/cairn.mjs wake and the shipped scripts actually did
  - the five version files at a48ccce4: package.json, .claude-plugin/marketplace.json, .claude-plugin/plugin.json, .codex-plugin/plugin.json and .muse-plugin/plugin.json all carry 0.7.0, and node bin/cairn.mjs --version prints "cairn 0.7.0" (PKG-039)
  - the tag: v0.7.0 is an annotated tag whose target commit is 96f60acb, the release commit; git show --stat 96f60acb changes only the five version files and CHANGELOG.md; the tag message carries the whole 0.7.0 entry with its heading marker stripped, matching scripts/release.mjs
  - the CHANGELOG 0.7.0 entry claim by claim against the kernel: the documents rule, the defect rule, the attribution rule, the revised-requirement rule, and the independent-report gate with each of its three named limits
  - the documents claim end to end: from a Done state at a48ccce4, appending a line to README.md (a documents entry of both node-test and pkg-lint) then running cairn check --stale and committing returns Done, while the control, appending a line to tests/fold.test.mjs (an inputs entry), returns a review round naming LOOP-032. The claim holds
  - the defect claim end to end: cairn backlog --defect --from LOOP-086 makes wake name "fix .cairn/backlog/a-probe-defect-two.md" (LOOP-140), and adding a Fixed by line with no passing evidence at or after that commit keeps it named; the control, the same capture --from LOOP-020, a requirement this commitment includes, is instead sent to the capture gate (LOOP-092)
  - the attribution claim: with .cairn/policy carrying attribution forbidden, a probe commit whose message carries a Co-Authored-By Claude trailer makes wake name "reword <sha>" (PKG-045), and the control, the same commit with a clean message, does not
  - the full test suite at a48ccce4: node --test tests/*.test.mjs gave tests 537, pass 537, fail 0, cancelled 0, skipped 0, todo 0, exit 0, in 59.7 s, with nothing to re-run in isolation. The thirty-ninth report at 3833b02a recorded 528, and no test file changed between 3833b02a and a48ccce4
  - about 87 review-and-report pairs built, committed and put through wake at a48ccce4, each read beside a control that differs in one detail; the harness first, so acceptance can be told from refusal: a pair whose two lists are both declared empty reaches Done, the same empty review beside a report holding one finding is refused by name, and the same report beside a review that cites it reaches Done
  - 42 report shapes an honest reviewer would write, each beside a review carrying nothing, so that reaching Done would mean the finding was lost: five bullet markers, two numbering forms, four indents including a tab, capitals, emphasis, a task box, a space before the colon, a number inside the prefix, an emoji marker, a footnote marker, a wrapped entry, a nested detail, a leading hashed title and a leading underlined title, a rule and a blank line inside the list, fields out of order, a CRLF record, a record with no final newline, the value written on the key line, an entry below the declared-empty form, a second key line, a table row, a fence, an HTML comment, a block quote, a details block, a nested prefixed bullet, a prefixed line in the examined list, a prefixed line in a reviewer field, and a prefixed line above the fields. Every one was named but the three below
  - 24 review shapes holding a real unfixed finding beside a clean report, in the same vocabulary. Every one was named but the two below
  - the three shapes that reached Done are the limit the developer accepted on 2026-09-18 and the 0.7.0 entry records: a defect written as a plain bullet under a heading titled "Defects", the same defect written as prose under a heading titled "Notes", and the same bullet written with a fullwidth colon in place of the prefix, all three beside a declared-empty list. No shape outside that limit lost a finding
  - the record identity checks: a report naming another commitment, a report naming 3833b02a while the review names a48ccce4, and a report that names its commit nowhere but on its own key line were each refused by name; a bullet stranded above the lists at four positions was repaired in place at three of them
  - the kernel size at a48ccce4: wc -l over the five tracked files under bin/ gives 1900, and node scripts/pkg-lint.mjs prints "kernel 1900 lines, clean", exactly the PKG-004 ceiling
  - I found no silent loss at a48ccce4. Every shape I built either kept the finding or named something, and the five below are about documents and messages
findings:
  - The working agreement promises an acceptance the gate does not give. AGENTS.md lines 135 to 138, and the shipped copy at skills/new-project/templates/AGENTS.md lines 135 to 138, say that when the findings field holds entries a heading whose title names findings "is read as elaboration and may hold prose", and bin/cairn.mjs line 982 carries the same sentence as a comment, contradicted by the comment on the next line. At a48ccce4 a report whose findings list holds one carried entry and whose body has a section titled "Findings I could not place" holding only prose is refused, and so is the same report titled "How I reached the findings above"; the control, the identical report titled "What else I saw", reaches Done. The same asymmetry holds for the review record: a section titled "The findings in context" is refused where "Notes" is accepted. The 0.7.0 changelog states the real rule, that such a heading is refused even when every finding is listed, so it is the agreement a reviewer is briefed from that is wrong, and a reviewer who follows it costs a whole round. (wording)
  - Two records committed at a48ccce4 state a kernel size that is wrong by 28 lines and use it as the reason to defer work. .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md line 146 says the files under bin/ "stand at 1872 now" and line 156 says PKG-004 is "not violated at 1872 of 1900". At a48ccce4 wc -l over the five tracked files under bin/ gives 1900 and pkg-lint prints 1900, so the headroom named there does not exist; the sibling item .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md says in the same directory that the kernel "sits at exactly 1900". The conclusion still holds, since 1900 does not exceed 1900, but the margin a later reader would plan against does not. (wording)
  - The leading bullet of the CHANGELOG 0.7.0 entry describes behaviour that did not change in 0.7.0. It says the stop hook no longer traps a session, judges with the kernel that wrote the latest evidence, opens after three refusals with no progress, writes a record under .cairn/stops/ and is named by the next wake. git diff v0.6.0 v0.7.0 -- bin/hooks/ and -- bin/hook.mjs are both empty, and stopVerdict in bin/cairn.mjs is unchanged between the two tags, while the 0.6.0 entry already describes all five behaviours under PKG-018, PKG-033, PKG-021, PKG-044, PKG-043 and LOOP-139. The same text is the v0.7.0 tag message, so it is what a reader upgrading from 0.6.0 sees first. (wording)
  - PKG-045 and LOOP-139 both claim the slot ahead of every action but a live check, and the kernel can honour only one, so PKG-045's falsifier is met. At a48ccce4, with .cairn/policy forbidding attribution, one probe commit that both adds an unexplained record under .cairn/stops/ and carries a Co-Authored-By Claude trailer makes wake name "explain .cairn/stops/<name>.md", which PKG-045's falsifier calls another action. The controls: the same commit without the stop record names reword, and the same stop record with a clean message names explain. The attribution is still caught one action later, so nothing escapes, but one of the two Agreed texts cannot be satisfied as written. (wording)
  - An honest report with a stray bullet on the line below its commit field is sent back for a whole new reviewer, where the same bullet one line lower is repaired in place. At a48ccce4 the field then reads "a48ccce4,a stray bullet" and the gate answers that the report "names a48ccce4,a stray bullet, which is not a commit" and asks for a new reviewer, although bin/cairn.mjs carries a repair for a commit line that "carries more than the commit"; that repair is missed because the joined value has no space after the identifier, so its first whitespace-separated word is not a commit either. The control is the same bullet written below a reviewer field and above the examined field, which is answered with a repair that keeps every word the reviewer wrote. (blocked)
