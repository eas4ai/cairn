commitment: reviews-carry-an-independent-report
commit: 26455bd1
reviewer: an independent reviewer given the commitment, its requirement texts and the commit range, and none of the build's context
examined:
  - Setup: cloned /home/shawn/workspace2/cairn-dev into my own scratch clone and checked out 26455bd1, the commit under review; every command ran as node bin/cairn.mjs inside that clone or inside throwaway fixtures built from its tests/helpers.mjs, and I wrote nothing in the checkout under review.
  - Read docs/commitments/reviews-carry-an-independent-report.md, docs/releasing.md, the texts of LOOP-020, LOOP-032, LOOP-033, LOOP-058, LOOP-059, LOOP-071, LOOP-092, LOOP-139, LOOP-140, PKG-004, PKG-039, PKG-040 and PKG-045, AGENTS.md, skills/new-project/templates/AGENTS.md, docs/manual.md, CHANGELOG.md, the escalations loop-020, loop-020-2, loop-020-loop-086, pkg-039 and pkg-040, and the record reader in bin/cairn.mjs: independentGap, listOf, headerOf, reviewOf, captureVerdict and the markup-tolerant prefix sweep.
  - The gate by experiment rather than by reading: about 800 review-and-report pairs committed in throwaway repositories and judged by what node bin/cairn.mjs wake printed, each beside a control differing in one detail; a clean pair reaches Done at these fixtures, so every refusal I report is the variation and not the fixture.
  - Position and markup sweep of the report: a finding-shaped line in 14 markup spellings inserted at every one of the 19 line positions of a rich report, 266 committed pairs in all, and every one was named rather than reaching Done.
  - Position and markup sweep of the review: the same in 12 spellings at every one of the 15 line positions of a rich review, 180 committed pairs, and every one was named.
  - Generated carry test: 300 pairs whose report holds one to three findings written in seven bullet spellings with six body decorations, and whose review carries a random subset of them; zero reached Done with a finding uncarried, and the 46 pairs that carried everything yet were refused all fall under rules the working agreement states, namely a bullet nested beneath an entry joining it, and a fence or a paragraph left inside the list.
  - Hand-built shapes: a defect placed in the findings list, in an examined entry, in body prose, in a table cell, behind quote markers, bold markers, a task box, an HTML comment, a hashed heading, a numeral, an entity, and inside a fence both terminated and unterminated; the file states of both records, being missing, empty, whitespace only, deleted on disk, edited on disk, CRLF and with a byte-order mark; the commit comparison; and the second-list, indented-field, orphan-bullet, findings-naming-heading and undeclared-list rules.
  - Shapes AGENTS.md promises the loop accepts: 14 of them, and all 14 reached Done, including a leading title hashed at levels one and six and underlined, blank lines between entries, numbered, margin-level, task-box and wrapped entries, an extra unknown field, a fence in the body outside any list, a word that merely contains the prefix, and the prefix described in words rather than written.
  - The release against PKG-039 and PKG-040: all five version files read 0.7.0, node bin/cairn.mjs --version prints cairn 0.7.0 and exits 0 from a directory that is not a repository, the annotated tag v0.7.0 points at 2d11e7a5, and that single commit changes exactly the five version files and adds the 53-line 0.7.0 changelog entry; the tag message body and the entry body are byte-identical, and docs/releasing.md's step 4 is what 26455bd1 records.
  - The 0.7.0 entry claim by claim: bin/ stood at 1900 lines at edb4460a where PKG-004 rose to 2000 and stands at 1898 now; a change to a declared input listed under documents: costs a check and not a review round while a plain input change costs both; a requirement revised outside the commitment leaves wake at Done while one inside names a mechanism review; with attribution forbidden an unpushed Co-Authored-By or Claude-Session commit is named for rewording and a clean commit and an unset policy are not; an unexplained stop record is named first and the attribution on the next wake; and bin/hook.mjs and bin/hooks/ are byte-unchanged between v0.6.0 and v0.7.0, so the corrected entry rightly carries no stop-hook bullet.
  - End to end on the repository itself at 26455bd1: with a clean pair committed, wake reads Done and names only the five next-iteration items; with one uncarried finding against the real 46-round review record it names that finding by its number; and once that line is carried it reads Done again, so nothing but the review blocks the release at this commit.
  - The suite: node --test tests/*.test.mjs twice in the clone at 26455bd1, 538 tests, 538 pass, 0 fail, exit 0 both times, with no flake in either run, so no file needed a run on its own.
findings:
  - AGENTS.md line 243 and the project template say a defect an Agreed requirement already forbids is captured with cairn backlog --defect --title ... --body ... --from REQ, and the 0.7.0 changelog entry says wake then names fix ITEM and the item carries its fixing commit, but when REQ is one the current commitment includes, which is the case the entry calls worked under the current commitment, wake instead answers Resolvable: escalate on the item under LOOP-092 and offers as its repair an Outside because: line, whose meaning AGENTS.md defines two paragraphs later as the idea is not this commitment's work, which is false of an item the same wake will name fix for once the line is added; reproduced in a fixture at 26455bd1 where the identical capture from a requirement outside the commitment is named fix at once, where the capture from a requirement inside it is named escalate, and where adding the line turns it into fix, and the shipped tests/defect.test.mjs only ever captures with --outside, so the documented path is never exercised (blocked)
  - docs/manual.md lines 334 to 336 tell a reviewer that writing findings: [] says there are none and leaves the record's other bullets, such as an attacked list, as prose, and a review written exactly that way is refused unless those bullets sit under a heading: with findings: [] followed by an Attacked: label and two bullets, wake at a fixture answers Resolvable: repair .cairn/reviews/first.md naming the label as a line it cannot read as an entry, and with the bullets placed directly after the empty list it names the first bullet the same way, while the control with the same bullets under a plain heading reaches Done; AGENTS.md says the body's bullets rather than the record's other bullets, and the manual's own review section further down does add the heading qualifier, so only this sentence misleads (blocked)
