commitment: reviews-carry-an-independent-report
commit: 1ca39d93
reviewer: I had none of the build's context, never read its diff, and worked read-only in my own clone of the repository at 1ca39d93 under /home/shawn/workspace2/scratchpads/tmp/claude-1000/, building every case as a committed review and report pair and running the kernel's wake on it
examined:
  - the gate at commit 1ca39d93, reached through 30 committed review and report pairs and a wake run on each, never by reading the diff
  - my setup first, with a clean pair whose one finding is carried and a pair whose lists are both written as the empty marker; both reached Done, so every later Done is a result and not a broken harness
  - the new exempt boundary, with a fence between two entries, a fence before the first entry, a fence before an entry's indented continuation, a margin rule inside the list, a wrapped entry, a list ending at the record's end, a list followed at once by a divider, the empty marker with a body, and a second findings line; the margin rule, the divider, the second field line, the last line of a record with no closing newline, and a fence that never closes are all read and named, so those five are sound
  - the label rule in both directions, with one label, three labels, four labels, a label that carries no colon of its own, a bracketed tag, and honest prose and scope entries that mention the two prefixes after a colon
  - every message a refusal printed in these runs, and whether the record is acceptable after the repair it names is carried out
  - the carry and citation rules, which are sound: an uncited finding, a citation that is not last on its line, a citation numbered past the report's length, and a review line that does not begin with the finding's words are each refused and named
  - the commit comparison, which is sound: a report at another commit of this repository, and a report naming a hex string that is no commit, are both refused and told a new report is needed
  - a report modified on disk, deleted from disk, committed empty, and committed as a deletion; each is named, the first two as a commit action and the last two as a new reviewer, so this is sound
  - freshness, which is sound: a review naming the commit before the one that revised the requirement is refused under LOOP-058, and so is a review naming a hex string that is no commit, because its reviewed text cannot be read; a review naming an older commit whose declared inputs have since changed is refused under LOOP-032, which is what the wake said before I wrote anything
  - the findings-naming section rule over its whole span, which is sound in that direction: a bullet in a subsection of a heading that names findings, and a bullet under a subheading that names findings, are both refused
  - one leading title in each parser, which is sound: a record opening with a hashed title and a record opening with an underlined title are both read, and their fields below it are read
  - the whole suite at 1ca39d93, node --test over tests/*.test.mjs, which reports 532 tests, 532 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo
  - all 68 files of .cairn/reviews read through the review reader, of which 62 are accepted and 6 refused; four are refused because a verification field line sits inside their examined list, where the reader cannot read it as an entry and drops the entries after it, those being command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md and skills-can-be-installed-with-the-skills-cli.md; two are refused because their first findings entry reads none, which is not a recognized entry, those being the-answer-reaches-the-agent.md and the-wake-names-an-action-or-refuses.md
findings:
  - a fence standing between the findings line and the last line the list consumed escapes the sweep entirely, so a real finding inside it disappears with nothing named; the report case fencemid, a fence holding a prefixed line between two carried entries, reached Done, while fenceafter, the same fence moved one line lower so it follows the last entry, was refused; fencefirst with the fence above the first entry, fencecont2 with the fence above an entry's indented continuation, and swallow with the fence holding a heading, two prefixed lines, a bullet and a table row, four findings in all, each reached Done; rvfence hid an open finding the same way inside the review's own list and reached Done, so an unresolved defect vanishes from the resolution gate too, where rvfenceafter was refused; the cause is that the walk measures how far the list reaches on the fence-stripped text, where a fence's lines are blank and so neither end the list nor advance the reach, while the exempt span is then cut from the original text, where those same lines carry their words; the three messages saying that no fence hides such a line are false for every fence inside the list (silent)
  - a label that carries no colon of its own swallows the prefix, because the label strip removes everything up to the first colon in the line and that colon is the prefix's own when no colon precedes it; the report case labeldash, a line reading Finding 3 then a dash then the prefix and a defect under a heading, reached Done, while labelcolon, the same line with the label's own colon after Finding 3, was refused; noteword, the word Note then a space then the prefix, reached Done, and rvlabel, the same shape in the review record, reached Done (silent)
  - four labels before the prefix hide it, although the rule means to strip up to four, because the test runs on the text after nought, one, two and three labels are removed and the fourth removal is assigned just as the loop ends; the report case fourlabel with four one-letter labels reached Done and threelabel with three was refused, and fourreal, a plainly written line reading Round 34 then reviewer 2 then item 3 then severity high then the prefix and a defect, reached Done (silent)
  - a heading whose title names findings and holds prose only is accepted whenever the findings list holds an entry, so prose findings under it disappear with nothing named, although the same heading is fatal when the list is empty; the report case headprose, one carried entry plus a section titled Further findings holding two sentences of real defect, reached Done, while headprose0, the same section with the list written as the empty marker, was refused as a heading that names findings and cannot be retitled, and headbullet, the same section with its prose written as one bullet, was refused as well (silent)
  - honest text is refused whenever the line's first colon is followed by the prefix, which is neither the start of a line nor markup; the report case honestexamined, whose scope entry reads the sweep outside the list then a colon then the two prefixes then the words lines under a heading, is refused as a line the loop cannot read as an entry, and honestprose, the same sentence as body prose under a neutral heading, is refused as a finding under a heading; the repair named for the first, to move the line into the findings list, then makes the reviewer's scope note a finding the review must carry, as case honestmoved shows (blocked)
  - a repair the message names leaves the record refused for a second reason, so the writer is sent somewhere that is not acceptable; case unclosed, a fence that never closes inside the findings list, is refused with the advice to put a heading above the prose that follows the findings, and case advice, that very record with a heading put above it, is refused again because the fenced line under the heading is read as a finding (blocked)
  - the advice given when the examined list holds nothing but a finding-shaped bullet asks for a change the record already carries, so it names no repair at all; case exonly, whose only examined entry is a bullet beginning with the prefix, is refused with the advice to write each as a bullet entry under examined, which is exactly what that line is, while the same line beside a second honest entry gets the usable advice to move it into the findings list (blocked)
