commitment: reviews-carry-an-independent-report
commit: 3b6dc3ce
reviewer: an independent reviewer given the commitment, its requirement texts and the commit range, and none of the build's context
examined:
  - Setup: cloned /home/shawn/workspace2/cairn-dev into my own scratch clone, checked out 3b6dc3ce, ran the kernel only as `node bin/cairn.mjs` inside the clone, never the `cairn` on PATH, and wrote nothing inside the project checkout; `git status --porcelain` in the clone at 3b6dc3ce is empty.
  - Read docs/commitments/reviews-carry-an-independent-report.md and the requirement texts for LOOP-020, LOOP-032, LOOP-033, LOOP-058, LOOP-059 in docs/spec/loop.md and PKG-004, PKG-039, PKG-040 in docs/spec/package.md, plus AGENTS.md, docs/manual.md, docs/walkthrough.md, docs/releasing.md and the four LOOP-020 escalations.
  - Measured the kernel myself at every first-parent commit from the twenty-fourth round to 3b6dc3ce, summing the tracked files under bin/ exactly as scripts/pkg-lint.mjs counts them (newlines plus an unterminated last line), and cross-checked four of those commits by running the project's own lint in a detached worktree: 601f567a and 209773d3 report "kernel 1896 lines", f0b5bf54 and 1376ec71 report "kernel 1900 lines".
  - The measured timeline: 1899 from the twenty-ninth round's fix 7c35392b; 1900 first reached at the thirtieth round's fix 1376ec71 and held through rounds 31 to 33; 1896 from the thirty-fourth round's fix 601f567a for eleven consecutive commits (601f567a, c95f83ef, 09ad875e, dfd7097d, e4f0fc37, 7e3fe0de, 5a198efe, 9b3dcda5, d54a3db2, eb899953, 209773d3); 1900 again from the thirty-fifth round's fix f0b5bf54, which added a net four lines, through to 3b6dc3ce, where the lint reports kernel 1900 lines of 2000.
  - The next-iteration paragraph in .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md: every commit and direction it names is right against my measurement, including 1376ec71 reaching 1900, 601f567a taking bin/ to 1896, and the rounds after it spending that again; .cairn/escalations/loop-020-2.md independently records the same 1896 dip.
  - Mutated a throwaway copy of scripts/pkg-lint.mjs as briefed: narrowing the scanned set on line 20 to direct children of bin/ fails tests/pkg-lint.test.mjs at line 51 (the PKG-012 fixture), and narrowing the counted set on line 60 fails it at line 35 (PKG-004); both mutations are killed.
  - Ran fifteen further mutations of scripts/pkg-lint.mjs against tests/pkg-lint.test.mjs to find what it still does not observe; killed: the .cairn/ exclusion of the ASCII scan, the evidence-output probes, the kernel comment scan for PKG-003, the 2000 threshold, and the unterminated-last-line term. Survived: the bare-URL and node:http(s) alternatives of PKG-012, the @google/generative-ai client of PKG-012, the docker-compose and Procfile alternatives of PKG-001, the extensionless-file branch and the reference/ exclusion of the PKG-008 file set, the backtick decision form of PKG-003, and an independent narrowing of the PKG-009 and PKG-003 kernel scans to direct children of bin/.
  - Confirmed two of those survivors against the whole suite, not just one file: with the bare-URL and node:http(s) alternatives removed from line 80, `node --test tests/*.test.mjs` is 537 tests, 537 pass, 0 fail; with @google/generative-ai removed, likewise 537 pass, 0 fail.
  - Read the whole 0.7.0 changelog entry claim by claim against the kernel and against the v0.6.0 tag: the backlog defect item, the documents: field, the revision-scope narrowing, the attribution policy and the independent-report gate are all absent from v0.6.0:bin/cairn.mjs and present at 3b6dc3ce; bin/hooks/ and bin/hook.mjs are byte-identical between v0.6.0 and HEAD, which is why the stop-hook bullet was rightly dropped; the five version files and `cairn --version` all read 0.7.0; scripts/release.mjs does take an uncommitted CHANGELOG.md into the release commit; LOOP-071 and LOOP-141 say what the entry says they say. Every claim held except the kernel-size sentence in the first bullet.
  - Verified the PKG-004 rise history in the decision record against the specification's own history: 1500 present from the drafting commit, 1600 first at 8d83d666 on 2026-09-15, 1900 first at 6985d190 on 2026-09-17, 2000 first at edb4460a on 2026-09-18, so "this is its third rise" is right.
  - Built review-and-report pairs in the clone and judged by what `node bin/cairn.mjs wake` actually does, each acceptance paired with a control differing in one detail: 13 hand-written shapes, 120 randomly generated shapes placing a real finding in one of four slots in eight host contexts with twenty markup spellings, and duplicate-field shapes (a second findings field in the header and below a heading, a declared-empty list followed by a bullet, two examined lists). None reached Done except the documented prose limit; the control carrying the finding reached Done in every case.
  - Tested six repair messages end to end by applying literally what the message asked and waking again: a missing commitment line, a capitalised field name, an indented field, a stray bullet above the lists, a commit line carrying more than the commit, and a fence inside the examined list. All six reached Done after the repair. One further message, for a declared-empty list followed by a bullet, names the line but its first clause asks for an entry the line already has; its other clauses do reach Done, so I did not count it.
  - Reproduced two defects that are already captured and so are not raised again here: the working agreement's promise that a findings-naming heading beside a list holding entries may hold prose, which the gate refuses (captured in .cairn/next-iteration/the-working-agreement-promises-a-heading-the-gate-refuses.md); and the stray-bullet asymmetry below the commit field (captured in the record-format item).
  - Scanned bin/cairn.mjs for dead declarations by name-occurrence count, including destructured bindings: none, so the two the earlier rounds found are gone.
  - Checked what is committed: 7947 tracked files, the bulk being evidence receipts that PKG-002 requires; no editor backups, scratch files or reports; assets/notation.png is unreferenced but was already ruled the developer's choice in .cairn/reviews/record-integrity.md; every output path named by a receipt exists. As the brief directs, I did not treat the absent v0.7.0 tag as a defect, and I have left the review record's now-stale sentence about that tag out of my report for the same reason.
  - Ran the full suite at 3b6dc3ce: `node --test tests/*.test.mjs` is 537 tests, 537 pass, 0 fail, 0 skipped, in 56 seconds, with no failure to re-run in isolation. `node scripts/pkg-lint.mjs` and `node scripts/spec-lint.mjs docs/spec` are both clean. `node bin/cairn.mjs wake` at 3b6dc3ce names `review reviews-carry-an-independent-report` because a requirement changed since the recorded review, which is the action this round is.
findings:
  - The claim that the kernel "had stood at exactly 1900 ... since the thirtieth round of the independent-report gate, so every fix after it had to remove as much as it added" is false as stated, in all three records that carry it: the PKG-004 rationale in docs/spec/package.md, docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md, and the first 0.7.0 bullet in CHANGELOG.md. Measured from the tracked files under bin/ exactly as scripts/pkg-lint.mjs counts them, the total was 1896, not 1900, from the thirty-fourth round's fix 601f567a for eleven consecutive commits ending at 209773d3, and the thirty-fifth round's fix f0b5bf54 then added a net four lines rather than removing as much as it added; the project's own lint reports "kernel 1896 lines" at 601f567a and at 209773d3 and "kernel 1900 lines" at f0b5bf54. The package.md text asserts the figure is measured rather than copied, and .cairn/escalations/loop-020-2.md and .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md, both committed in this repository, record the 1896 dip that these three deny. (wording)
  - .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md says "Rounds 31, 32, 33 and 34 of reviews-carry-an-independent-report each paid for their fixes by trimming comment", and round 34 did not: its fix 601f567a took bin/ from 1900 to 1896, the first headroom in four rounds, as .cairn/escalations/loop-020-2.md says in the same repository. The item was captured at d07e574f, before that fix existed, so the round-34 clause was asserted in advance and the repository now contradicts it; the sibling item's paragraph was corrected for exactly this class in the previous two rounds and this one was left. (wording)
  - docs/manual.md, in the paragraph that explains this commitment's own rule, tells the reader that a heading whose title names findings "beside a list that holds entries ... may hold prose but not a list", and the gate does not do that: with one finding in the report's list and only the prose sentence "I read the kernel twice." under the heading, wake refuses a report headed "How I reached the findings above" and refuses one headed "Findings I could not place", while the identical report headed "What else I saw" reaches Done. The captured next-iteration item for this mismatch names only AGENTS.md and skills/new-project/templates/AGENTS.md, which are contract text; docs/manual.md carries the same false promise, is not contract text, is listed under documents: in .cairn/mechanisms/node-test so correcting it costs a check and not a review round, and is the document that contradicts the 0.7.0 changelog entry's statement of the real rule. (wording)
  - docs/manual.md line 495 ends one sentence with a comma and starts the next with a capital in the same line: "A fence keeps a quoted field out of the metadata, but hides no finding, The prefix belongs to that list alone, so describe it rather than writing it". The sentence break is missing, in the paragraph a developer reads to learn what this commitment built. (wording)
  - tests/pkg-lint.test.mjs still does not observe most of the PKG-012 check it covers. The new fixture proves the scanned file set, but not what is scanned for: deleting the bare-URL and node:http(s) alternatives from line 80 of scripts/pkg-lint.mjs leaves the whole suite green at 537 tests, 537 pass, 0 fail, and deleting the @google/generative-ai client does the same, so the lint could stop recognising a kernel that embeds an API URL or imports Google's client and no test would name it. The test observes only fetch and two of the three client names. The same mutation sweep shows tests/pkg-lint.test.mjs also does not observe the docker-compose and Procfile alternatives of PKG-001, the extensionless-file branch and the reference/ exclusion of the PKG-008 file set, or the backtick decision form of PKG-003. (wording)
