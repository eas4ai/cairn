commitment: reviews-carry-an-independent-report
commit: c7c18041
reviewer: a fourth independent subagent given the commitment, LOOP-020, the working agreement paragraph and the commit range ef70db45..c7c18041, with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 in docs/spec/loop.md with its falsifier and the Revised 2026-09-17 note, and the AGENTS.md "Review before Done" paragraph on the independent reviewer
  - the diff ef70db45..c7c18041 for bin/cairn.mjs (independentGap), tests/helpers.mjs and tests/independent.test.mjs, and the wake code around independentGap, including reviewOf and dirtyInputs
  - the third independent report and the builder's review with its eight claimed third-round resolutions, each checked against the code at c7c18041
  - tests/independent.test.mjs, run in a scratch clone at c7c18041 with TMPDIR set to a scratch directory: 4 tests, all passed
  - probe scripts in the scratch clone that commit crafted reviews and reports and run wake. Cases tried: after the review is redone, the report's commit: line edited along with a trailing blank line or a one-character change; the report deleted in one commit and re-added with only its commit: line changed; a new report with the same wording as the previous one; carried forms for a report finding that ends with a period; unrelated review lines that begin with a short report finding, followed by a comma, " (" or " - "
  - the claimed fixes that held when probed: a report whose only change is its commit: line is refused; the four hyphen, path and colon cases from the third report are refused; one review line no longer carries two findings (the longest-first matching cannot refuse a valid set of lines); an unindented wrapped finding is named repair; an uncommitted review is named commit; a commit mismatch names both commits; capitals, "!" and a bracket are accepted
findings:
  - The fix for editing a stale report's commit: line only compares the report with its previous committed version, so an agent can still reach Done without a new independent report. Reproduced, each reaching Done after the review was redone at a later commit: the old report with a new commit: line and one extra blank line at the end; with a new commit: line and one character added to the reviewer: line; and deleted in one commit, then re-added unchanged except for the commit: line.
  - A report from a genuinely new reviewer is refused when its wording matches the previous report apart from the commit: line. This is likely when the reviewer finds nothing and gets the same prompt. Reproduced: a report with the same reviewer: and examined: lines and findings: [] was refused as "edited, not written again". Starting another reviewer, as the refusal says, can produce the same words and the same refusal. The test helper avoids this only by putting the commit into the examined line.
  - A clause break still lets an unrelated review line carry a short report finding. Reproduced, each reaching Done: report "a defect" carried by "resolved: a defect, unrelated walkthrough typo fixed"; "wake" carried by "resolved: wake (the help text typo), fixed"; "the report" carried by "resolved: the report - writer skill typo fixed".
  - A report finding that ends with a period is refused in the usual ways of carrying it. Reproduced for the finding "The parser drops wrapped lines.": refused were "resolved: The parser drops wrapped lines. Fixed in abc1234", "resolved: The parser drops wrapped lines, fixed in abc1234" and "resolved: The parser drops wrapped lines; fixed in abc1234". To follow the "resolved: <its text>, and how" form that AGENTS.md and the refusal describe, the builder must write a period followed by a comma (".,"), and the refusal does not say so.
  - tests/independent.test.mjs covers only the exact case where nothing but the commit: line changed. It does not test a commit: line edit combined with another small change, a report deleted and re-added, a new report with the same wording, a short finding carried by an unrelated line at a comma or parenthesis, or a finding that ends with a period.
