commitment: reviews-carry-an-independent-report
commit: 62471010
reviewer: a fresh subagent given only the commitment file, LOOP-020 and its falsifier, LOOP-086, LOOP-071, LOOP-032, LOOP-033, LOOP-058, LOOP-108, the working agreement's record and reviewer paragraphs, and the commit 62471010; it built every record by hand in scratch clones and judged only what wake printed
examined:
  - the reader and the gate at 62471010: recordFields, headerOf, listOf, entryFix, reviewOf and independentGap in bin/cairn.mjs, read line by line against LOOP-020, LOOP-086, LOOP-108 and LOOP-071, and against the AGENTS.md paragraphs on the review record, its lists and starting the reviewer
  - the four defects the twentieth round and the builder named, each retested at 62471010 as a built record run through `node bin/cairn.mjs wake` in a fixture repository, never by reading the diff: a rule under commit: or examined: (`---`, `===`, `--`, `====`, `-----`), the same rule in the report's header, a carrying review line beside an earlier round's citation at another commit (both orders, and with a real earlier sha), the manual's findings advice with a bulleted body, and the genuine double carry as a control
  - what those fixes could have broken, at 62471010 and again on a worktree at 09abf7f7 for comparison: an underlined heading whose title is field-shaped, a hashed heading with the same title, a rule inside a findings list above a real open finding, a rule between fields, an indented rule, prose above and between the header fields of both records, a hashed and an underlined title above the fields, the double carry with two citations of this report on one line, and citations of another report's commit
  - every committed record in .cairn/reviews/ at 62471010 read through the real reader (reviewOf, imported from an unmodified copy of bin/cairn.mjs with main() not invoked), and the same 68 records read through the kernel at 49012dc0, the commit that opened this commitment, to tell a new refusal from an old one
  - the shipped documents against what the kernel does with records: the review-finding paragraph and the "Decide what Done means for you" and "Get unstuck" sections of docs/manual.md, the review step and printf records of docs/walkthrough.md, and README.md's "The verdicts in plain characters" block with its reviewed(c) and done(c) definitions
  - the whole suite at 62471010: `node --test tests/*.test.mjs`, 518 tests, all passing, plus tests/independent.test.mjs and tests/record-boundaries.test.mjs read against the behavior I reproduced
findings:
  - All four named defects hold at 62471010: a `---`, `===`, `--`, `====` or `-----` line between commit: and examined: now reaches Done in the review and in the report (it gave "examined: needs a nonempty list" and an unreadable commit before), the manual's `findings: []` advice with an "## Attacked" bullet list reaches Done, and a carrying line beside an earlier round's citation reaches Done in both orders while the genuine double carry is still refused.
  - A regression from the same fix: an underlined heading whose title ends with a colon is no longer heading text, so honest prose under it is read as part of the findings list; the review "commitment: first / commit: <sha> / examined: / - the work / findings: [] / (blank) / Attacked: / --------- / (blank) / - the reader, line by line" is refused with 'findings: holds a line the loop cannot read as an entry: "Attacked:", and the entries after it are unread', the same shape in the report is refused with the reviewer-facing "give a finding its own - entry ... or ask the reviewer again", and both reach Done at 09abf7f7 and with "## Attacked:" or a colon-free "Attacked" underlined at 62471010.
  - A report whose header fields sit below a title is not repaired but sent to a new reviewer: with "Independent report / ================== " or any plain prose line above commitment:, wake says ".cairn/reviews/first.independent.md names commitment none, not first; start a new reviewer", and a prose line between commitment: and commit: swallows it into the value ("names commitment first I was given the commitment and the commit range., not first"), so a complete, honest report is discarded although AGENTS.md forbids editing it; the identical mistake in the review gets the repair "the fields sit under a heading and the header ends at the first heading, hashed or underlined".
  - The report's repair never names the heading boundary that the review's does: a report with "# Independent report" above its fields is refused with "needs a nonempty examined: list; keep the reviewer's own words: give a finding its own - entry, put a heading above prose that follows the findings, or ask the reviewer again", none of which applies, while the same review gets "the fields sit under a heading and the header ends at the first heading, hashed or underlined".
  - bin/cairn.mjs still carries the wording the twentieth round fixed in docs/manual.md: a review with an unrecognized entry is told to "use list entries ... or leave findings empty when there are no findings (LOOP-086)", and a record written that way, with findings: naming no entries above any bulleted section, is refused a second time with "a finding sits under a heading, where the loop does not read it", while the same record written findings: [] reaches Done.
  - No shipped developer document says Done now needs the independent report: README.md's reviewed(c) and done(c) definitions and its paragraph on "what the definitions leave out" name only the review, its examined commit and its findings, and the manual's "Decide what Done means for you" lists "a current review with no open finding" while its "Get unstuck" table has rows for resolve, repair, record and scope but none for `review <slug>`; wake refuses Done with "no independent report is committed at .cairn/reviews/first.independent.md; start a new reviewer", so the documents state a Done condition the kernel no longer has.
  - Six committed reviews are refused by the reader at 62471010, and none of them by this commitment's work: command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn and skills-can-be-installed-with-the-skills-cli carry a "verification:" field inside examined: ('examined: holds a line the loop cannot read as an entry: "verification:", and the entries after it are unread'), and the-answer-reaches-the-agent and the-wake-names-an-action-or-refuses list "- none" under findings: ('finding 1 is unrecognized: "none"'); all six are refused identically by the kernel at 49012dc0, the remaining 61 read clean with no open finding, and this commitment's own .independent.md is not a review record and is never read by reviewOf.
