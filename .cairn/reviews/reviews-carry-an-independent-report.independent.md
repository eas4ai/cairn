commitment: reviews-carry-an-independent-report
commit: 896cea21
reviewer: a seventeenth independent subagent given the commitment, LOOP-020, the working agreement paragraphs and the commit range ff0e9e56..896cea21, with no build context
examined:
  - docs/commitments/reviews-carry-an-independent-report.md, LOOP-020 with its falsifier, and LOOP-086, LOOP-071 and LOOP-108 in docs/spec/loop.md, against the AGENTS.md paragraphs on the review record, its lists and the independent reviewer
  - git log and git diff ff0e9e56 896cea21 for bin, tests, docs, AGENTS.md and skills: at commit 896cea21 the removal of the FIELD rule, the new plain boundary in listOf, the rewritten heading condition, the report's repair message, and the new test in tests/independent.test.mjs
  - each of the sixteenth round's five findings retested as a concrete record at 896cea21 rather than read: findings 1, 2, 4 and 5 hold, finding 3 is the stated limit and the next-iteration capture exists and says what it narrows
  - the whole declared suite in a scratch clone at 896cea21 with TMPDIR outside the repository: 514 tests, all pass; node bin/cairn.mjs wake there names a new review at 896cea21
  - about twenty hand-built review and report records run through node bin/cairn.mjs wake in that clone, each also run against a second clone at ff0e9e56 to separate this commit's behaviour from what it inherited
  - all 68 committed records in .cairn/reviews read through the reader extracted from 896cea21 and from ff0e9e56, comparing which records each version refuses
findings:
  - A review or report whose findings list is declared empty with "findings: []" is refused as "a finding sits under a heading" as soon as its body holds any bullet or numbered line, because 0dc44b2c dropped the empty guard from the heading rule, so LOOP-086's "an empty findings list MUST remain valid" no longer holds.
  - Six committed reviews in .cairn/reviews (the-hooks-and-gates-judge-by-records, the-contract-says-what-the-kernel-does, the-kernel-refuses-bad-records-and-leaves-no-trap, the-next-iteration-starts-from-the-specification, this-repository-declares-and-lints-what-it-reads, consumers-can-run-the-lint-and-open-the-skills) have exactly that shape, findings: [] above an "Attacked:" bullet list; every one is clean at ff0e9e56 and refused at 896cea21, so the regression hits this repository's own house style, not a constructed record.
  - The refusal a no-findings record now gets cannot be acted on: it says a finding sits under a heading and to keep the findings in the header's list when the record declares no findings, and the repair named for the same bullets before a heading ("write each finding as a - entry, and put prose after a heading") produces the other refusal when followed, so no no-findings review with a bulleted body is accepted in any form.
  - The working agreement's new sentence, "a bullet under a heading is read as prose unless it reads as a finding or the list above it is empty", states the same conflation of a declared "findings: []" with a list that has no entries, so the agreement now contradicts LOOP-086 and the kernel's own comment ("the record's only list") and has to be corrected with the kernel.
  - The regression is untested: the new assertions in tests/independent.test.mjs exercise a findings-titled heading only beside a nonempty findings list, and no test covers a body bullet or numbered line beside "findings: []" in either record.
