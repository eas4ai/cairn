commitment: reviews-carry-an-independent-report
commit: 29b7f211
reviewer: a fresh agent with none of the build's context, which read only the commitment file, the seven requirement texts and the record-grammar paragraph of AGENTS.md, and worked read-only in two scratch clones of its own; /home/shawn/workspace2/cairn-dev was never written to
examined:
  - the gate at 29b7f211, as about thirty concrete review/report pairs committed on a probe branch of each clone and judged by `node bin/cairn.mjs wake`; every claim below is a wake run, never a reading of the diff
  - the setup first: at 29b7f211 a pair with one carried finding and a `findings: []` pair both reach Done, and every silent case below is paired with a control that differs in one detail and is refused
  - the new seam, by position sweep: the kernel's own listOf, headerOf, bare, label and saysFinding lifted verbatim out of bin/cairn.mjs lines 902-989 into a throwaway module, then a finding-shaped line inserted at every line position of nine record templates in eleven markup shapes, asking for a position that is neither read as an entry nor swept; every survivor was then confirmed by wake in the report and in the review
  - the two headers in disagreement: a fence in the header, a fenced heading before the real one, a quoted `findings:` field inside a fence, a quoted `examined:` field inside a fence, a setext title and its underline, a record with no heading at all, a record whose last line has no newline, and a report whose commit line is last
  - the label rule: twenty-one label shapes through saysFinding, then four survivors confirmed by wake in the report body, in the review body and in the examined list
  - the carry and citation rules: no citation, a citation not last on its line, two review lines citing one finding, one line citing two, a number beyond the report, and a review line that does not begin with the finding's words - all six named, each saying which finding and why
  - the commit comparison: a report at another commit, a report naming the commit only on its commit line, a report for another commitment, fields pushed below a heading, and two leading titles - all five named
  - a record out of step with its commit: the report modified on disk, deleted from disk, committed as a deletion and committed empty, and the review modified on disk - all five named, the disk cases as `commit <path>` so both records are judged in one state
  - freshness: LOOP-032 with the review at 36411289 against a tree at 29b7f211, and LOOP-058 with LOOP-020's falsifier text edited after the review, which names the mechanism review under LOOP-059
  - the findings-naming section over its span: a section headed "Findings in detail" holding only prose beside a nonempty list reaches Done, while one bullet in its subsection is named, so the span is judged and not just the first level; one leading title, hashed or underlined, is accepted by both parsers
  - `node --test tests/*.test.mjs` at 29b7f211: 531 tests, 531 pass, 0 fail, 0 cancelled, 0 skipped, 43.1s
  - all 68 files in .cairn/reviews/ at 29b7f211 read through the kernel's own reviewOf: 62 accepted, 6 refused - four (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, skills-can-be-installed-with-the-skills-cli) put a `verification:` field inside the examined list, named as a line the loop cannot read as an entry; two (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses) write `- none` as finding 1, named as unrecognized; every refusal is older than this commitment, and this commitment's own independent report now reads cleanly as a review too
findings:
  - open: the region the sweep exempts runs from the `findings:` line to the record's first heading, not to the end of the findings list, so any fence in that span hides a real finding and nothing is named. The same fenced block is Done before the first heading and refused after it: a report whose list holds one carried finding plus a fenced `- open: the second real defect` before `## Notes` reaches Done, and moving that identical block below `## Notes` is refused with "says open: or resolved: under a heading". A record with no heading at all - which the form encourages, since the fields sit above the first heading, and which my clean base pair proves reaches Done - exempts everything after its list, so the thirty-second round's decorative divider is only half fixed: two `~~~~~~~~~~~~~~~~~~~~~~~~` rules with a real finding between them are Done in the report, and are Done in the review too, where the hidden line is `- open: the fence seam is not fixed, and I have not repaired it`, so LOOP-033 never fires and the wake says the review is clean. A report that writes `findings: []` and puts its only finding inside that span is also Done. Adding a heading above the divider refuses the identical span, and removing the fence markers refuses it as not carried (silent)
  - open: the exempt span starts at the first `findings:` line the raw text shows, so a fence that quotes the `findings:` field moves the start above itself and un-sweeps its own contents, while LOOP-071 keeps that quoted field out of the metadata. A report whose header carries a fenced block reading `findings:` then `  - open: the second real defect` above its real list reaches Done; deleting just the quoted `findings:` line from that same block is refused as saying the finding above the findings list. So whether a fenced example of the record shape is hidden or refused turns on whether the example happens to include the field line, which is not a distinction any reviewer can be told (silent)
  - open: the label rule strips one label of at most forty characters from the already-stripped text, so a second label, a longer label, or a bracketed tag still hides the prefix everywhere the sweep looks, and a real finding is lost. Under `## Notes` in the report, `- Note: Blocked: open: ...`, `- Blocked, and the message is also wording and long: open: ...` and `- [silent] open: a real finding disappears from the gate with nothing named` all reach Done, where the same bullet with one short label is refused; the same two-label bullet in the review body reaches Done while the review carries an unresolved defect; and in the examined list `  - Note: Blocked: open: the second real defect` is silently accepted as a thing examined, where the one-label version is named as a line the loop cannot read. A reviewer asked to rate or tag findings writes exactly these shapes (silent)
  - open: the repair for a finding under a heading tells the reviewer to do what the refused record already did, and doing it again does not help. The message asserts "The loop hides an example only inside a fence" and advises "quotes an example of the shape inside a fence", yet the record it refused had the example inside a fence, below a heading; the only placement that is hidden is above the first heading, and no message says so. Following the advice as written leaves the record refused for the same reason, so the repair is unreachable (blocked, and the message is wording)
  - open: in a review, an honest examined entry that names the prefix after a label is refused with advice that leads to a second refusal. `  - LOOP-086: open: and resolved: are the two prefixes the reader classifies` under examined is refused with "move it into the findings: list, and change none of the reviewer's words"; moved there, it is refused as "finding 2 is unrecognized". The line is a description of the reader, not a defect, so neither list will take it and the writer is left to guess that the only fix is to reword a sentence the same message forbids changing (blocked)
  - resolved: the carry and citation rules, the commit comparison, an uncommitted, modified, deleted, empty or deleted-and-committed record, LOOP-032 and LOOP-058 freshness, the findings-naming section judged over its whole span, and one leading title in each parser are all sound at 29b7f211, each refusal naming the finding or the field and what to write; the position sweep found no unread region outside the exempt span, so findings 1 and 2 are the whole of that seam and finding 3 is the whole of the label seam (wording)
