commitment: reviews-carry-an-independent-report
commit: 3ba15a82
reviewer: an independent reviewer given only the commitment file, the requirement texts named in the brief, the commit and the twenty-third round's outcome, working read-only in its own clone
examined:
  - a scratch clone checked out at 3ba15a82, with every case written as a review/report pair, committed on a test branch whose parent is 3ba15a82 and run through `node bin/cairn.mjs wake`; a baseline pair (review citing one report finding, and a findings: [] pair) reaches Done at 3ba15a82, so every refusal below is the case and not the setup
  - the six fixes of the twenty-third round, each rebuilt as a concrete record at 3ba15a82: a margin entry above a `---` rule (report and review, both Done); an underlined title `Note: what else I read` above a prose bullet (both Done); a complete header below one prose line with no commitment: line (both named as "does not read commit:, although the line is there"); a field name quoted in prose below a heading (both Done); `Commit:` and `commit :` in a report (both named as "spells commit: another way" and repaired); and findings under a `## Findings` heading (both repairs offer retitling); all six hold
  - bin/cairn.mjs at 3ba15a82: fields, recordFields, headerOf with ENTRY/SETEXT/ATX/OWN, listOf, entryFix, reviewOf, independentGap, the wake path from reviewOf through independentGap to promote and Done, and withoutFences in bin/spec.mjs
  - docs/spec/loop.md LOOP-020 and its falsifier, LOOP-032, LOOP-033, LOOP-058, LOOP-071, LOOP-086, LOOP-108; AGENTS.md's paragraphs on the review record, its lists, the findings-titled heading, the stated limit and the independent reviewer; the independent-report paragraph in docs/manual.md; the committed review, its .independent.md and .cairn/escalations/loop-020-loop-086.md
  - tests/independent.test.mjs and tests/record-boundaries.test.mjs, then `node --test tests/*.test.mjs` at 3ba15a82: 521 pass, 0 fail; the new underline, unread-field, spelling and retitle cases are all tested with hashed headings and with a header that carries every field, which is why the gaps below are uncovered
  - all 68 committed records in .cairn/reviews/ at 3ba15a82 read through the reader, each copied to this commitment's review path with its commit: line re-dated to 3ba15a82: 61 accepted, and 7 refused, which are command-help-is-always-available.md, host-paths-are-part-of-the-contract.md, humans-can-understand-and-use-cairn.md and skills-can-be-installed-with-the-skills-cli.md, whose `verification:` line between examined: and findings: is named as a line the examined: list cannot read; the-answer-reaches-the-agent.md and the-wake-names-an-action-or-refuses.md, whose `- none` entry is named as unrecognized under LOOP-086; and reviews-carry-an-independent-report.independent.md, which is a report and whose findings carry no open:/resolved: prefix, so read as a review its finding 1 is unrecognized; no committed review holds an open finding, and this commitment's own review and report, re-dated, reach Done with every citation resolved
  - attacks beyond the six: fields below an H1 title (named in both records, repair fixes it), a bare `commit:` line, a fenced example findings list in a review body (Done, correct), an indented example findings list, an underlined section titled `Findings: the ones I could not place`, a report naming the commit only on a trailing commit: line with no final newline, a review that omits the optional commitment: field (Done), and unterminated fences inside a findings list
findings:
  - The findings-titled heading rule reads only hashed headings, so a report whose `findings: []` is followed by the underlined heading `Findings` over `--------` and the bullet `- the wake accepts a review whose commit line is empty, losing the gate` reaches Done with nothing named, while the identical record with `## Findings` is refused; the same asymmetry holds in the review, and AGENTS.md's rule that "a heading whose title names findings holds no list" is what the hashed form enforces, so this loss is not the limit the developer accepted (silent).
  - An unterminated code fence at the margin inside a findings list blanks every line after it, so the report `findings:` / `  - the wake does something wrong` / three backticks / `  - the wake accepts a review whose commit line is empty` reaches Done carrying one finding, and the review `findings:` / `  - resolved: ... (independent 3ba15a8 1)` / three backticks / `  - open: a real defect nobody has resolved` reaches Done with "the review at 3ba15a82 and its independent report are clean" while the same review without the fence line gives "resolve" for that open finding (silent).
  - A review that omits the optional `commitment:` field reaches Done, but the same review with one margin prose line below a heading that begins `commitment: <slug> is the line a review may also carry.` is refused with "it names commitment below a heading, where the header ends; move the record's fields to the top", although its fields are at the top and LOOP-071 says a body line is not metadata; the named repair does not fix it, since what must move is the prose (blocked).
  - The new spelling check matches indented lines inside the header's own lists, so a complete review whose examined entry continues `    commitment: the slug the record names` is refused with "it spells commitment: another way; write each field name in lower case, with no space before the colon" although it carries no commitment: line and reaches Done without that note, and a record that simply omits `findings:` while an indented note mentions it gets the same spelling repair instead of "findings: is missing; write findings: [] when there are none" (blocked).
  - A bullet under a heading whose title does not name findings is still read as a finding when it begins open: or resolved:, so a review with `## Notes` over `- open: is the prefix a finding carries, and this line is a note about it`, or with a four-space indented example of a malformed findings list under `## What I attacked`, is refused with "keep the findings in the header's list, or, if those lines are notes rather than findings, retitle the heading so it does not name findings"; neither option fixes either record, whose real fixes are to reword the note and to fence the example (blocked).
  - docs/manual.md states the accepted limit as "A bullet under a heading is prose, so a finding written there is read as a note and nothing names it", and AGENTS.md adds "a bullet under a heading is prose, whatever it says, so a finding written there is read as a note and nothing names it", which contradicts the sentence above it and the two refusals reproduced above, where a bullet under a hashed findings-titled heading and a bullet beginning open: under any heading are both named (wording).
  - A report that omits only its `commitment:` line is still discarded for a whole new reviewer, "names commitment none, not reviews-carry-an-independent-report; start a new reviewer with none of the build's context", rather than repaired like the `Commit:` and `commit :` spellings fixed this round, so one missing header line throws away a report's findings and costs another independent round, and the agreement's "never edit a reviewer's report to fit" forbids adding the line (blocked).
