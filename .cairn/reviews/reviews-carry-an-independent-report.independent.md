commitment: reviews-carry-an-independent-report
commit: 356e94fc
reviewer: a fresh session with none of the build's context, working read-only in its own clone of the repository at 356e94fc, which was never written to
examined:
  - the gate at 356e94fc: about thirty review-and-report pairs, each written into .cairn/reviews/, committed, and judged by running node bin/cairn.mjs wake in the clone
  - the setup first, so a pass means something: a pair with one carried finding, and a pair whose report declares an empty findings list, both reached Done at 356e94fc
  - the newest reader: every line outside the list read for the prefix, a findings-naming section judged over its span, a prefix line inside examined:, and one leading title in each parser
  - the carry and citation rules: a finding no line cites, a citation that is not last on its line, two findings cited by one line, a line that does not begin with the finding's words, a number the report does not have, an earlier round's citation, and a full 40-character sha; each behaved as the requirement says
  - the commit comparison: a report naming another commit is refused; a report modified on disk, deleted from the tree, or written and not committed is named for commit before it is judged; the same for the review
  - LOOP-032 freshness: at 356e94fc the repository's own review, which examined 7f3032b2, is named stale because a declared input changed since; committing a change to bin/cairn.mjs, README.md or docs/spec/loop.md after a clean pair makes the evidence stale before the review gate is reached
  - node --test tests/*.test.mjs at 356e94fc: 528 tests, 528 pass, 0 fail, 0 skipped, exit 0
  - all 68 .md files in .cairn/reviews/, each put in turn at the current commitment's review path and read by the kernel's own reader: 62 accepted, 6 refused; four of them (command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn, skills-can-be-installed-with-the-skills-cli) for an unbulleted "verification:" line inside examined:, with the entries after it unread, and two (the-answer-reaches-the-agent, the-wake-names-an-action-or-refuses) for a findings value of "none", read as one unrecognized finding
  - the limits already accepted were used as controls, not reported as defects: a prose finding under a heading that does not name findings, an indented code block, a table cell beginning with the prefix, a resolved line with no account, a heading that merely mentions findings, the cost of a version bump, and old runtimes
findings:
  - the helper that reads a line for the finding prefix strips at most one list marker and a fixed set of lead characters, so a line any reader sees as a finding still passes the sweep: "- - open: X", "- 1. open: X", "(2) open: X", "- (warning emoji) open: X" and "<!-- open: X -->" all reached Done under a heading titled Notes, with the finding lost and nothing named; the same shapes in the builder's own review carried an unresolved open finding through to Done, so LOOP-033 and LOOP-086 lose by the same hole, not only LOOP-020 (silent)
  - a table row counts as a row only when the line begins with a pipe, in both the prefix test and the test for what a findings-naming section holds, so a GitHub-style table written without outer pipes is invisible to the whole gate: a report whose declared list held only "see the table below" and whose two real findings sat in such a table under a heading titled Findings reached Done, while the identical table with outer pipes is refused and the identical table beside an empty declared list is refused; a pipe-less row inside examined: is joined in silence to the entry above it, and in the builder's own review such a row hid an open finding through to Done (silent)
  - a field the reviewer writes after the findings list, including the reviewer: line the reviewers of this round are told to carry, is refused as an unreadable findings entry, and the repair advice offers only bulleting the line or asking the reviewer again, never moving the field above examined: (blocked)
  - a report that is committed but missing from the working tree is named as commit .cairn/reviews/<slug>.independent.md, and committing that deletion throws the report away and sends the next wake for a new reviewer; the message never says to restore the file (wording)
  - a prefix line in the record's own header, above the findings list, is refused with a message saying it sits under a heading, which is not where it is, and the fix offered is a whole new report rather than moving that one line into the list (wording)
