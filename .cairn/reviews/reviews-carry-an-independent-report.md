commitment: reviews-carry-an-independent-report
commit: a48ccce4
examined:
  - mechanism review of PKG-004 after the developer raised the ceiling to 2000 (LOOP-059). I compared scripts/pkg-lint.mjs with the revised text and falsifier and changed no code. The lint counts every tracked file under bin/ as wc -l counts it, plus an unterminated last line, and reports when the total exceeds 2000, which is the falsifier's words. I demonstrated both sides in a scratch repository outside this checkout: a kernel of 2001 lines is reported as "PKG-004: kernel is 2001 lines", and the same kernel at exactly 2000 is not reported at all. No mismatch. The real kernel stands at 1900 of 2000.
  - re-examined at a48ccce4, the 0.7.0 release. The release commit 96f60acb changes five version strings and the changelog and no kernel logic; a48ccce4 records the checks at it, and every requirement passes there. I verified the release state myself: the five version files all read 0.7.0, the kernel prints cairn 0.7.0, and the tag v0.7.0 points at 96f60acb. The full suite at the release commit is 537 tests, 537 pass, 0 fail, with no flake in that run.
  - a correction to the thirty-ninth report, which I could not reproduce: it reports the suite as 528 tests passing twice in a pristine clone, and the suite at that commit is 537. I cloned the repository afresh, checked out 3833b02a and ran the same command: 537 tests, 537 pass, 0 fail. The 43 test files are identical between 3833b02a and the release commit, all tracked and all present, so nine tests were missing from the report's count rather than from the suite. No finding of that report depends on the number, and both runs agree there are no failures. The report is kept verbatim, since editing a reviewer's words is what this commitment forbids; the correction belongs here, in my own record.
  - re-examined at 3833b02a, the release round, with a thirty-ninth independent report from a new subagent working read-only in its own clone. It built about 360 record pairs and put each through wake: 50 report shapes an honest reviewer would write plus 200 generated from the same vocabulary, each beside a review carrying nothing, and every one was named; a position sweep inserting a plainly prefixed bullet at each of the 47 line positions of a rich report and a rich review, and every one was named, so outside a fence it found no region the reader neither reads nor sweeps; 20 review shapes holding a real unfixed finding beside a clean report, all named but the one limit accepted on 2026-09-18; the file states of both records, the commit comparison in six spellings, and the carry and citation rules in 15 shapes. It reports no silent loss: every shape either kept the finding or named something. It found five, all wording, and one of the five is dead code rather than a message. Carried below.
  - re-examined at 962a52fa after the comment, indented-field and stray-bullet fixes, with a thirty-eighth independent report from a new subagent working read-only in its own clone. It ran the widest verification of the commitment: 240,000 generated record shapes through the kernel's own list reader, 120,000 reports and 120,000 reviews, each with a finding-shaped line inserted at a random position in one of up to nineteen spellings, and 21,450 spellings of the prefix itself across 22 wrappings and thirteen gaps at fifteen positions. No shape lost a finding without naming something except the one class in its first finding. It found three: one silent and narrow, two wording. Carried below.
  - my own probes at 962a52fa: thirteen shapes against the three-way reading, twelve correct, and one silent miss of my own, a tag pair with content between the word and the colon. The report's first finding is the same class from the other side, an element round the word with markup in the gap, so both are closed by the same change.
  - re-examined at 0735b118 after the numbered-prefix fix, with a thirty-seventh independent report from a new subagent working read-only in its own clone. It screened about 3,600 report shapes and 1,100 review shapes through the kernel's own list reader, inserting a finding-shaped line in 42 markup spellings at every line position of seven report templates and 20 spellings at every position of five review templates, then confirmed the survivors by wake with a control for each. It reports the carry and citation rules sound and no unread region in the header but one. It found seven: three silent, four wording. Carried below.
  - my own probes at 0735b118: fifteen shapes against the widened predicate, all correct, including a digit with no gap, a bracketed number and a hashed number caught, and a year, a hyphenated word and a sentence left as prose. I also dry-ran the release in a throwaway clone: it refused in one line because the loop is not at Done and wrote nothing, which is what the release document promises.
  - re-examined at d68b89da after the punctuation and fence fixes, with a thirty-sixth independent report from a new subagent working read-only in its own clone. It built 52 record pairs, paired every silent case with a control, and reported the carry and citation rules, the commit comparison, a modified, deleted or emptied record, freshness, the findings-naming heading, the examined list and the fence rules all sound. It found five: one silent, one blocked, three wording. Carried below.
  - my own probes at d68b89da: eleven honest sentences against the widened prefix rule, nine of which stay prose because at most four characters may stand between the word and its colon, and six record shapes end to end. One silent miss of my own, a fence inside the examined list, which the report did not reach but which loses a reviewer's stated scope the same way the findings list lost entries.
  - re-examined at 5a198efe after the rule that gave the prefix to the findings list alone, with a thirty-fifth independent report from a new subagent working read-only in its own clone. It took the prefix rule apart one detail at a time, walked the list's own lines through ten shapes, and re-verified the carry and citation rules, the commit comparison, a modified, deleted, emptied or blanked record, and freshness under LOOP-032 and LOOP-058, reporting each sound. It found five: three silent, one blocked, one wording. Carried below.
  - my own probes at 5a198efe: twenty-five markup shapes through the reader's own predicate, all correct, and nine record shapes end to end. Three apparent failures were my own expectations being wrong, which I record rather than dress up: a wrapped carrying line does carry, because the continuation joins it and the citation ends up last; a wrapped report entry changes the finding's words, so a review line that no longer begins with them is refused; and an indented rule after the list joins the entry and pushes the citation off the end, which is visible rather than silent. No silent loss of my own.
  - re-examined at 1ca39d93 after the thirty-third round's fixes, with a thirty-fourth independent report from a new subagent working read-only in its own clone. It built thirty record pairs, named every case, and paired each silent one with a control: a fence between two entries against the same fence one line lower, a label without its own colon against the same label with one, four stacked labels against three, and a findings-naming section holding prose against the same section beside an empty list. It re-verified the carry and citation rules, the commit comparison, a modified, deleted or empty record, freshness under LOOP-032 and LOOP-058, the findings-naming span and one leading title in each parser, reporting all sound. It found seven: four silent, three blocked. Carried below.
  - my own probes at 1ca39d93: eleven cases against the same boundary. Six hold, one apparent failure was my own test being wrong, since a wrapped carrying line moves its citation off the end and is refused for that, and one was a silent miss of my own: five stacked labels, where the loop capped at four. Removing the cap is safe against hostile input, 5000 labels in 15ms, and the report's third finding covers the same rule.
  - re-examined at 29b7f211 after the thirty-second round's structural fix, with a thirty-third independent report from a new subagent working read-only in its own clones. It lifted the reader's own helpers into a throwaway module, inserted a finding-shaped line at every line position of nine record templates in eleven markup shapes, and confirmed every survivor by wake, pairing each silent case with a control. It reports no unread region outside the exempt span, so its findings are the whole of that seam and of the label seam. It found five defects and one entry confirming the rest: three silent, two blocked, one wording. Carried below.
  - my own probes at 29b7f211: nine cases against the same seam. A fenced heading before the real one, a fence inside the header, a quoted findings: line inside a fence, a CRLF record, a record with no trailing newline and the real findings list inside a fence are all named, and two honest shapes stay prose. One silent miss of my own, a label longer than forty characters, which the report's third finding covers in full.
  - an audit of the record itself: 146 carrying citations across 30 rounds, all distinct, 188 resolved entries and no open finding. My first count found four duplicates and was wrong: it counted citations quoted inside a finding's own words, which the gate treats as words rather than as a carry, so the numbering is sound.
  - re-examined at 36411289 after the thirty-first round's fixes, with a thirty-second independent report from a new subagent working read-only in its own clones. It screened about a hundred wrapper shapes through the kernel's own helper before confirming the survivors by wake, swept seventeen opener and closer shapes through withoutFences in both directions, and re-verified the carry and citation rules, the commit comparison, a modified, deleted and empty record, freshness, and the findings-naming span, reporting all of them sound. It found four defects and recorded one entry confirming the rest: three silent, one blocked whose message is also wording, one wording. Carried below.
  - my own probes at 36411289: nine fence cases in both directions. A language word, an indented fence, a closer longer than its opener, a fence inside a list item and a tilde fence all hide their examples; a prose line beginning with the marker, a decorative tilde rule used twice, and an unterminated fence hide nothing. One blocked case of my own, the priced cost of that rule, recorded below and then overtaken by the report's second finding.
  - re-examined at 606d046c after the thirtieth round's fixes, with a thirty-first independent report from a new subagent working read-only in its own clone. It built about forty record pairs and, instead of guessing shapes, swept a finding-shaped line through every line position of two rich records for nine markup shapes, to look for a region neither read nor exempt: outside a fence there is none, so its two silent findings are reader rules rather than unread regions. It also re-verified the carry and citation rules, the commit comparison, a deleted, modified and empty record, freshness, and the three wording fixes of the previous round. It found four: two silent, one blocked, one wording. Carried below.
  - my own probes at 606d046c: sixteen region cases. A finding written as the findings: value, as a second word on the commitment: line, as the examined: value, after an unterminated fence, after a tilde fence, below the review's last heading, below a report's appendix heading, as the report's own title, in a continuation joined across a blank line, in a second findings: list, in a CRLF record and with a non-breaking space after the prefix are all named; a finding inside a terminated fence stays hidden, as it must; a review whose carrying line sits inside a fence or an HTML comment is refused, because a hidden line carries nothing. One silent gap of my own, recorded below.
  - re-examined at 356e94fc after the twenty-ninth round's rewrite, with a thirtieth independent report from a new subagent working read-only in its own clone, briefed to the developer's ruling and to use the accepted limits as controls. It built about thirty record pairs, re-verified the carry and citation rules, the commit comparison, an uncommitted and a deleted record, and LOOP-032 freshness, and read all 68 files in .cairn/reviews/ (62 accepted, 6 refused, every refusal older than this commitment). It found five: two silent, one blocked, two wording. Carried below.
  - my own probes at 356e94fc: thirteen further markup shapes. A bullet nested three deep, a hard line break before the prefix, an HTML comment ahead of it, a link wrapped round it, a details block and a bullet after a bare rule are all named, and a sentence using the word mid-line, a heading titled "Open questions", an ordinary table and a fenced example all stay prose. One marginal silent gap of my own, recorded below; a full-width colon is left alone deliberately, since the project ships ASCII only. I also measured the kernel against PKG-004: 1899 lines of 1900, so every fix this round is line-neutral or pays for itself in trimmed comment.
  - re-examined at 7f3032b2 after the twenty-eighth round's fixes, with a twenty-ninth independent report from a new subagent working read-only in its own clone, briefed to the developer's ruling that only a silent loss is fixed. It built twenty-six record pairs, read all 68 files in .cairn/reviews/ through the reader (62 accepted, 6 refused, every refusal older than this commitment), and recorded a control beside each silent case. It found eight: six silent, one blocked, one wording. Carried below.
  - my own probes at 7f3032b2: eleven markup shapes through the new helper. Capitals, a doubly nested quote marker, underscores and a numbered entry are named; "- opened: the file" and "- resolved issues: none of them mattered" are correctly left as prose. Three silent losses of my own, recorded below, and two exotic misses not worth a rule: an escaped prefix, which renders as text, and a definition-list line whose colon sits on the next line.
  - re-examined at 35a9ca09 after the twenty-seventh round's fixes, with a twenty-eighth independent report from a new subagent working read-only in its own clone. The developer had ruled that only a silent loss would be fixed from this round, so the reviewer was asked to hunt silent losses first and report the rest briefly. It built thirty-eight record pairs, confirmed all five of the round's changes where it could reach them, and found eleven: four silent, three blocked, four wording. Carried below.
  - my own probes at 35a9ca09: a prefixed bullet nested two levels deep behind earlier bullets, and as a numbered entry, both named; a bullet under a findings heading beside a list that holds entries, refused, and prose there, accepted; two leading titles, a title after prose, a setext title and a title above a deeper heading, each refused; a title inside a fence, invisible; a report short of commit: only and of examined: only. One finding of my own, blocked, recorded below.
  - a regression sweep at 35a9ca09: all 67 committed reviews read through the current reader, 61 accepted and 6 refused, every refusal for a reason that predates this commitment (four with a verification: line inside examined:, two listing - none under findings:). freshness-and-scope-guidance-agree.md, refused at a84cc814 for its findings-naming section, is accepted again, which confirms the twenty-seventh round's second fix on a real record.
  - re-examined at a84cc814 after the twenty-sixth round's fix, with a twenty-seventh independent report from a new subagent working read-only in its own clone, rating each finding silent, blocked or wording. It built twenty-four record pairs, confirmed the previous round's fixes and this round's three changes, and found eight: one silent, four blocked, three wording. Carried below.
  - my own probes at a84cc814: a findings heading inside a closed fence and inside a four-space code block, both invisible; "## What I found" with a bulleted finding, Done, which is the accepted limit; a "- Findings:" list item, not a heading; the review's own "## Review at <sha>" section; and a report whose only body is a rule. One finding of my own, wording, recorded below. I also read every committed review: only freshness-and-scope-guidance-agree.md carries a heading whose title names findings, and none carries an underlined one.
  - re-examined at 74848eb4 after the twenty-fifth round's fixes, with a twenty-sixth independent report from a new subagent working read-only in its own clone, rating each finding silent, blocked or wording. It rebuilt all six fixes as records and confirmed them, then read every one of the 68 committed reviews through the reader: 60 accepted, 8 refused. It found four: one silent, two blocked, one wording. Carried below.
  - my own probes at 74848eb4: a singular "Finding" and an all-caps "FINDINGS" title, both named; a findings heading inside a fence, correctly invisible; bullets after a rule with no heading, named as unreadable; and an empty findings-titled section, accepted. No finding of my own this round.
  - re-examined at 8efe6c02 after the twenty-fourth round's fixes, with a twenty-fifth independent report from a new subagent working read-only in its own clone, rating each finding silent, blocked or wording. All seven fixes hold. It found five: one silent, four blocked. Carried below.
  - my own probes at 8efe6c02: the rewritten fence stripper against specification parsing, where a closed fence still hides its example requirement and an unterminated one no longer hides every line after it, and a fenced example inside a review body, still hidden. One finding of my own, recorded below.
  - re-examined at 3ba15a82 after the twenty-third round's fixes and the developer's ok on the buried-finding limit, with a twenty-fourth independent report from a new subagent working read-only in its own clone, rating each finding silent, blocked or wording. All six fixes hold. It found seven: two silent, four blocked, one wording. Carried below.
  - my own probes at 3ba15a82: a capitalised Commitment: line, an underlined heading whose title carries a colon and a value, a heading titled with the record's own field name, a reviewer: line in the review's header, and an examined entry that reads like a field. All five behaved, and I found nothing of my own this round.
  - re-examined at 362eb5a7 after the twenty-second round's fixes, with a twenty-third independent report from a new subagent working read-only in its own clone, again rating each finding silent, blocked or wording. All six fixes hold. It found seven: five blocked honest records, one the silent case the loop cannot close, one wording. Carried below.
  - my own probes at 362eb5a7: an honest method section with bullets, a findings entry that mentions the findings list, and a repeated examined: line, all handled; and the residual silent case, which I reproduced and added to the captured next-iteration item with the round counts.
  - re-examined at d88e131a after the twenty-first round's fixes, with a twenty-second independent report from a new subagent working read-only in its own clone. I asked it to rate each finding silent, blocked or wording. It confirmed all six fixes and found four defects: one silent, two blocked, one wording. Carried below.
  - my own probes at d88e131a: a bullet after a rule inside the list, a report quoting a field name in prose below a heading, a report whose fields sit inside a fence, a report missing findings: while naming it mid-line, and the review side of every malformed shape the report now handles. Two findings of my own, both the same asymmetry, are recorded below.
  - the history of this record: eighteen rounds carried before this one, 79 findings from the reports and 111 entries once my own and the reports' confirmations are counted, with no downward trend in the per-round count. The structural remedy, Cairn writing and reading its own records, waits in next-iteration for the developer.
  - re-examined at 62471010 after the twentieth round's fixes, with a twenty-first independent report from a new subagent working read-only in its own clone. It confirmed all four fixes, found one regression in my underline rule, two defects in how a malformed report is answered, one stale message in the kernel, and one gap between the shipped documents and what Done now requires. Carried below.
  - my own probes at 62471010: a wrapped field value beside a rule, an indented underlined heading, a single-dash underline, two citations of this report on one line, the commitment file's own requirements and done-when, and the kernel's error handling for a report that is a directory, binary, empty or a symlink, and for a missing review; every one gives a Resolvable verdict and no crash. One finding of my own is recorded below.
  - re-examined at 09abf7f7 after the nineteenth round's fixes, with a twentieth independent report from a new subagent working read-only in its own clone. It verified all six of the nineteenth round's shapes and the four regressions those fixes could have caused, and found two defects, both introduced by those fixes. Carried below as open.
  - my own probes at 09abf7f7: a title underlined above the fields, an indented command block and a dashed rule under a findings entry, a margin sentence beginning findings: inside examined:, a citation naming another report's commit, a finding-shaped entry under an underlined heading, a fenced example between entries, an unterminated fence, nested numbering, and the shipped walkthrough and skills tests, which pass. My probes found the missing hint for an underlined heading, which the report names inside its first finding, and one defect of my own in the shipped manual, recorded below.
  - re-examined at f6bdf509 after the eighteenth round's fixes, with a nineteenth independent report from a new subagent working read-only in its own clone. It verified all five of the eighteenth round's shapes and found six defects, two of them in the fixes themselves. Carried below as open.
  - my own probes at f6bdf509: a report finding whose words begin with a field name, records written with CRLF line endings, entries indented with a tab, a unicode bullet, a citation written with the full 40-character commit, a horizontal rule inside a list, twelve findings carried in order and in reverse, an empty examined list, a six-character commit: line, an upper-case commit, and LOOP-141's document rule, where a documents-only change keeps the review current and a code change makes it stale. One of my probes found the reorder advice defect the report names third, independently.
  - re-examined at 69ec524c after the seventeenth round's fix, with an eighteenth independent report from a new subagent working read-only in its own clone. It verified all five of the seventeenth round's shapes as concrete records run through wake, and found four defects, two of them silent losses. Carried below as open.
  - my own probes at 69ec524c, each a built record run through wake: the carry rule against a review that declares findings: [] over a report with two findings, crossed citations, two lines citing one finding, and a finding whose words extend another's; the committed-state reads for a report never committed and a report edited after it was committed; a report that copies the review; a report naming the commit only in an indented block
  - re-examined at 896cea21 after the sixteenth-round resolutions, with a seventeenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range ff0e9e56..896cea21. Four resolutions hold and the fifth is the stated limit; it found one regression I introduced, in five shapes: a declared findings: [] with any bulleted body is refused, which six of this repository's own committed reviews would hit. Carried below as open.
  - re-examined at ff0e9e56 after the fifteenth-round resolutions, with a sixteenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range 6ef294ba..ff0e9e56. It found that the heading rule I added refuses honest records, that a Reviewer: line still ends a review's list in silence, and three more; carried below as open.
  - re-examined at 6ef294ba after the fourteenth-round resolutions, with a fifteenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range b0f53f96..6ef294ba. It confirmed five of six resolutions and half of the sixth, and found three defects: a repair that destroys a finding when followed, a field name repeated inside a list, and a report's findings under a heading beside a list; carried below as open.
  - re-examined at b0f53f96 after the thirteenth-round resolutions, with a fourteenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range 52010049..b0f53f96. It found that any margin line shaped like a field still ends a list in silence, in six shapes; carried below as open.
  - re-examined at 52010049 after the twelfth-round resolutions, with a thirteenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range 83dec7ed..52010049. It read all 68 committed records through the reader and found five defects, all from the concession that a trailing unreadable line is prose; carried below as open.
  - re-examined at 83dec7ed after the eleventh-round resolutions, with a twelfth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range a9cd734c..83dec7ed. It read this repository's own records through the reader (12 examined and 57 findings entries in the review, 6 and 6 in the report, nothing unread) and found seven defects in what the reader treats as continuation and in what it names; carried below as open.
  - re-examined at a9cd734c after the tenth-round resolutions, with an eleventh independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range 13c512de..a9cd734c. It compared the new reader with the previous kernel on about twenty records and on this repository's own, and found that reading loose Markdown without refusing what it cannot read trades named repairs for silent drops, in six shapes; carried below as open.
  - re-examined at 13c512de after the ninth-round resolutions, with a tenth independent report from a new subagent given the commitment, LOOP-020, LOOP-086, LOOP-071, LOOP-108, the working agreement paragraphs and the range 538ba9a0..13c512de. It found the same silent drop with the blank line before the first entry, which my detector could not see, and three more; all four reproduced on this repository's own records. Carried below as open.
  - re-examined at 538ba9a0 after the eighth-round resolution, with a ninth independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range 03961bb1..538ba9a0. It confirmed the eighth finding resolved and found that a blank line inside a findings list silently drops every entry after it, in the report and in the review, the second of which lets an open finding reach Done and predates this commitment; carried below as open.
  - re-examined at 03961bb1 after the seventh-round resolutions, with an eighth independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range 1313f32b..03961bb1. It confirmed both resolutions, ran a simulated round on this review record, and found one defect; carried below as open, citing this report's commit and number.
  - re-examined at 1313f32b after the sixth-round resolutions, with a seventh independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range e69a3c83..1313f32b. It confirmed both resolutions hold for the documented forms, with a swap of the older kernel, and found two refinements; carried below as open, citing this report's commit and number.
  - re-examined at e69a3c83 after the fifth-round resolutions, with a sixth independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range f4f4e27d..e69a3c83. It confirmed all six fifth-round resolutions, including a swap of the older kernel, and found two edge cases; carried below as open, citing this report's commit and number.
  - re-examined at f4f4e27d after the fourth-round resolutions, with a fifth independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range c7c18041..f4f4e27d. It confirmed the two new rules held against re-dated reports and unrelated lines, and found that citation numbers collide across rounds, among six findings; carried below as open, each citing this report's commit and number.
  - re-examined at c7c18041 after the third-round resolutions, with a fourth independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range ef70db45..c7c18041. It confirmed seven of eight resolutions held and found five remaining problems, all from matching and forgery checks built on heuristics; carried below as open, each marked with its number in the report.
  - re-examined at ef70db45 after the second-round resolutions, with a third independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range 92d5e828..ef70db45. It found that my fix for the stale-report bypass changed only the message, that the looser matching lets unrelated findings carry, and six more; its findings are carried below as open, in its own words.
  - re-examined at 92d5e828 after the thirteen resolutions, with a second independent report from a new subagent given the commitment, LOOP-020, the working agreement paragraph and the range b2a1ea27..92d5e828. It confirmed which resolutions held and reproduced eight new defects, one a regression the fix introduced: a report left at an earlier commit is now named repair, which lets an agent edit its commit: line and reach Done with no new reviewer. Its findings are carried below as open, in its own words.
  - LOOP-020 as revised: tests/independent.test.mjs failed on the kernel before the build and passes now; the full suite passed at 502 after tests/robustness.test.mjs and the walkthrough learned the independent report. The walkthrough shows the step, and says a report the builder writes is not independent.
  - the independent report, written by a subagent given only the commitment, LOOP-020 and the commit range b365c2c..b2a1ea2, saved verbatim beside this review. It reproduced every defect below in a scratch clone; I had tested only well-formed reports. Each of its thirteen findings is carried here as open, in its own words.
findings:
  - resolved: A report whose `examined:` list holds no readable entry is refused with a false diagnosis: `examined:` bare, or `examined:` followed by `verification: node --test tests/independent.test.mjs` and then a bullet, both give "does not read examined:, although the line is there, because a line above it is neither a field nor a heading; keep the record's fields at the top", although the line is read and the fields are at the top, and independentGap's own correct messages sit below the branch that pre-empts them, while the review reader names the identical record correctly as "examined: needs a nonempty list of what the review examined" (blocked).; resolved at 70292c06: the report's list messages come first, so a bare examined: list is named as needing entries and a verification: line inside it is named as unread; the branch about a field the parser never reached now judges only the scalar fields (independent 8efe6c0 1)
  - resolved: A report short of only its `commitment:` line but naming an older commit, `commit: 3ba15a82` while the review names 8efe6c02, is told to add the commitment line, and the very next wake discards the report it just had edited with "names commit 3ba15a82 and the review names 8efe6c02 ... start a new reviewer", so the named repair does not make the record acceptable and the earlier branch's guard that a report for another commit is replaced rather than repaired is not applied to the new repair (blocked).; resolved at 70292c06: the commit identity is read before the repair, so a report at another commit is replaced rather than asked to add a line and then discarded (independent 8efe6c0 2)
  - resolved: A heading that names findings holds no bullet list but does hold a table: a report with `findings: []` and a `## Findings` section whose defect is a Markdown table row, and the same section written as one prose paragraph, both reach Done with nothing named, while the identical bullet is refused, so a recognisable finding disappears under a heading whose title does name findings, which is outside the limit the developer accepted in loop-020-loop-086 and contrary to AGENTS.md's rule that such a heading holds no list (silent).; resolved at 70292c06: a heading whose title names findings may hold no content of any shape, so a table row or a paragraph under it is named as a bullet already was (independent 8efe6c0 3)
  - resolved: A `findings:` line carrying neither `[]` nor any entry is read as no findings and reaches Done when nothing follows it, yet the same line plus an unrelated `## What else I read` bullet list is refused with "holds findings under a heading" in the report and "a finding sits under a heading" in the review, and none of the three named repairs applies, since no heading names findings, no bullet begins open: or resolved: and nothing is an example, while the fix that works, writing `findings: []`, is not named (blocked).; resolved at 70292c06: a findings: line that declares neither entries nor [] above a list is told to write findings: [] when there are none, or to move the findings into the list (independent 8efe6c0 4)
  - resolved: A report with `findings: []`, a bulleted `## What else I read` section and a later prose-only heading titled `## Findings I could not place` is refused as holding findings under a heading although no list sits under the findings-naming heading, because the check pairs every bullet below the first heading with every heading's title anywhere in the record; removing only the distant heading makes the identical record Done, and the refusal is wider than the documented rule (blocked).; resolved at 70292c06: each heading is judged on the lines of its own section, so bullets under a heading that does not name findings no longer pair with a findings-naming heading elsewhere (independent 8efe6c0 5)
  - resolved: an unterminated fence in a specification file now exposes what it holds: at 8efe6c02 parseSpec on a file whose fenced example requirement is never closed returns the example beside the real requirement, where the old stripper hid the example and every line after it. Hiding was worse, since it could hide real requirements in silence, but a fenced draft can now be read as a requirement and nothing names the unclosed fence; the specification lint should name it. (blocked); resolved at 70292c06: the specification lint names a fence a file never closes, with its line, so an unclosed fence cannot leave a draft requirement in the text unnoticed
  - resolved: The findings-titled heading rule reads only hashed headings, so a report whose `findings: []` is followed by the underlined heading `Findings` over `--------` and the bullet `- the wake accepts a review whose commit line is empty, losing the gate` reaches Done with nothing named, while the identical record with `## Findings` is refused; the same asymmetry holds in the review, and AGENTS.md's rule that "a heading whose title names findings holds no list" is what the hashed form enforces, so this loss is not the limit the developer accepted (silent).; resolved at 32102569: a heading that names findings holds no list whether its title is hashed or underlined, in both records (independent 3ba15a8 1)
  - resolved: An unterminated code fence at the margin inside a findings list blanks every line after it, so the report `findings:` / `  - the wake does something wrong` / three backticks / `  - the wake accepts a review whose commit line is empty` reaches Done carrying one finding, and the review `findings:` / `  - resolved: ... (independent 3ba15a8 1)` / three backticks / `  - open: a real defect nobody has resolved` reaches Done with "the review at 3ba15a82 and its independent report are clean" while the same review without the fence line gives "resolve" for that open finding (silent).; resolved at 32102569: only a fence that closes hides its block, so an unterminated fence leaves every line below it readable and the open finding after it is named (independent 3ba15a8 2)
  - resolved: A review that omits the optional `commitment:` field reaches Done, but the same review with one margin prose line below a heading that begins `commitment: <slug> is the line a review may also carry.` is refused with "it names commitment below a heading, where the header ends; move the record's fields to the top", although its fields are at the top and LOOP-071 says a body line is not metadata; the named repair does not fix it, since what must move is the prose (blocked).; resolved at 32102569: a field counts as missing only when the gate lacks it, so a body line below a heading that begins with a field name decides nothing, and commitment: stays optional in a review (independent 3ba15a8 3)
  - resolved: The new spelling check matches indented lines inside the header's own lists, so a complete review whose examined entry continues `    commitment: the slug the record names` is refused with "it spells commitment: another way; write each field name in lower case, with no space before the colon" although it carries no commitment: line and reaches Done without that note, and a record that simply omits `findings:` while an indented note mentions it gets the same spelling repair instead of "findings: is missing; write findings: [] when there are none" (blocked).; resolved at 32102569: a spelling is judged at the record's own margin, so an indented note that mentions a field name is a note, and a record that omits findings: is told findings: is missing (independent 3ba15a8 4)
  - resolved: A bullet under a heading whose title does not name findings is still read as a finding when it begins open: or resolved:, so a review with `## Notes` over `- open: is the prefix a finding carries, and this line is a note about it`, or with a four-space indented example of a malformed findings list under `## What I attacked`, is refused with "keep the findings in the header's list, or, if those lines are notes rather than findings, retitle the heading so it does not name findings"; neither option fixes either record, whose real fixes are to reword the note and to fence the example (blocked).; resolved at 32102569: the repair names every fix that keeps a note a note: retitle a heading that names findings, reword a note that begins open: or resolved:, and put an example inside a fence (independent 3ba15a8 5)
  - resolved: docs/manual.md states the accepted limit as "A bullet under a heading is prose, so a finding written there is read as a note and nothing names it", and AGENTS.md adds "a bullet under a heading is prose, whatever it says, so a finding written there is read as a note and nothing names it", which contradicts the sentence above it and the two refusals reproduced above, where a bullet under a hashed findings-titled heading and a bullet beginning open: under any heading are both named (wording).; resolved at 32102569: the agreement and the manual now say the loop refuses what it can recognise, a bullet beginning open: or resolved: and any list under a heading that names findings, and reads the rest as prose (independent 3ba15a8 6)
  - resolved: A report that omits only its `commitment:` line is still discarded for a whole new reviewer, "names commitment none, not reviews-carry-an-independent-report; start a new reviewer with none of the build's context", rather than repaired like the `Commit:` and `commit :` spellings fixed this round, so one missing header line throws away a report's findings and costs another independent round, and the agreement's "never edit a reviewer's report to fit" forbids adding the line (blocked).; resolved at 32102569: a report short of its commitment: line is repaired by adding that line, which is the record's own field and not the reviewer's words, instead of being discarded for a new reviewer (independent 3ba15a8 7)
  - resolved: A list entry written at the left margin and followed by a `---` or `===` rule is read as a setext heading with the rule as its underline, so a report whose examined: entry is the unindented line `- bin/cairn.mjs at 362eb5a7` followed by `---` and then `findings: []` is refused with "names findings below a heading, where the header ends; move the record's fields to the top", though the fields already are at the top and no message names the rule; the same shape refuses a review, an indented entry before the rule is accepted, and the code's own comment at headerOf claims "a list's own line is not heading text" while FIELD never tests ENTRY (blocked).; resolved at 6e97a863: an underline makes a heading of the line above it only when that line is neither a list entry nor one of the record's own fields, so a margin entry followed by a rule stays an entry (independent 362eb5a 1)
  - resolved: A setext-underlined heading whose title holds a colon with text after it is read as a field line rather than heading text, so a report ending `findings:` with one entry, then `Note: what else I read`, an underline of dashes and a prose bullet, is refused with "findings: holds a line the loop cannot read as an entry: \"Note: what else I read\", and the entries after it are unread" and told to "put a heading above prose that follows the findings" when that heading is already there; a review with the same section is refused the same way, while a title ending in a colon is read as a heading (blocked).; resolved at 6e97a863: a field-shaped title with a value is heading text when an underline follows it, so the section's prose is prose; the record's own five fields are never heading text (independent 362eb5a 2)
  - resolved: A review whose complete header sits below a single prose line and carries no `commitment:` line is refused with "commit: names the commit the review examined" although its commit: line is present, because the below-prose explanation added in the twenty-second round fires only when `commitment:` is in the record; the named repair does not fix it, since what needs moving is the prose (blocked).; resolved at 6e97a863: a field that sits in the header but the parser never reached is named where it sits, for the review and the report alike, instead of being reported missing (independent 362eb5a 3)
  - resolved: A review whose header is complete and readable is refused with "it does not read its commitment:, because the line above it is neither a field nor a heading" when any line of its prose below a heading begins with `commitment:`, as a review discussing the record format will; the verdict is decided by a body line that LOOP-071 says the loop must not read as metadata, and moving the fields, which the repair asks for, changes nothing (blocked).; resolved at 6e97a863: the check reads only the header, so a line of prose below a heading that begins with a field name decides nothing (independent 362eb5a 4)
  - resolved: A report whose header field is spelled `Commit:` with a capital C, or `commit :` with a space before the colon, or which omits only its `commitment:` line, is discarded for a whole new reviewer ("names no commit, which is not a commit", "names commitment none, not reviews-carry-an-independent-report") rather than repaired, and the message never names the spelling; the same three shapes in the review are repairs, and the loop elsewhere takes care to name a field written in another case rather than act on it (blocked).; resolved at 6e97a863: a field spelled in another case, or with a space before its colon, is named and repaired in both records, and the report is no longer discarded for it (independent 362eb5a 5)
  - resolved: A real further finding a report writes as a bullet under a heading whose title lacks the word "findings", such as "## What else I saw" holding "- the wake accepts a review whose commit line is empty, losing the gate", reaches Done with nothing named; a report's findings carry no open:/resolved: prefix, which is the form this round's reviewer was asked for, so the finding-shaped test can never fire in a report and the heading's title is the only guard the twenty-second round's fix left, though AGENTS.md at 362eb5a7 calls a bullet under a heading prose and next-iteration already holds reviews-and-reports-are-written-in-a-form-the-loop-reads for this class (silent).; resolved at 396eb722 as a stated limit, on the developer's answer: no rule over prose can tell a buried finding from a note, so the escalation loop-020-loop-086 put the choice to the developer, who answered ok. The working agreement and the manual now state the limit and tell the agent to say it when briefing a reviewer, and the record format change that would end it waits in next-iteration as reviews-and-reports-are-written-in-a-form-the-loop-reads (independent 362eb5a 6)
  - resolved: When a record declares `findings: []` and explains under a heading titled "Findings" what it tried, in prose bullets, the refusal is correct by AGENTS.md's rule that a heading naming findings holds no list, but the repair says only "they belong in the findings: list above the first heading" and "give a finding its own - entry, put a heading above prose that follows the findings"; neither names the fix, which is to retitle the heading, and following the message as written would turn notes into fabricated findings (wording).; resolved at 6e97a863: both repairs now offer retitling the heading when its lines are notes, so following the message cannot turn notes into findings (independent 362eb5a 7)
  - resolved: A report that lists at least one finding above the first heading and more findings under a heading, with no open:/resolved: prefix, reaches Done and the ones under the heading are never named: a report whose findings: list held "the gate accepts a record it must refuse" and whose "## Findings in full" section held "- wake exits 3 on an empty report" and "- the message names the wrong file" printed "Done: first ... and the review at 0f4063d and its independent report are clean" with only finding 1 carried, while the identical report with those two written "open: ..." is refused with "holds findings under a heading, where the loop does not read them" - and since a report's findings need no prefix (independentGap's norm makes it optional and tests/independent.test.mjs writes bare text), the heading check is inert for exactly the shape the kernel expects. (silent); resolved at d7ec978c: a heading whose title names findings may hold no list, in the review and in the report, so a section titled findings can no longer hide real findings; a heading that does not name findings keeps its bullets as prose, and AGENTS.md and its template state both (independent d88e131 1)
  - resolved: The report's commitment: and commit: are read by recordFields, which takes the last margin-level field-shaped line above the first heading, so a complete honest report whose prose quotes one is sent back for a whole new reviewer rather than repaired: a report ending "---" then "commitment: someone-else" gives "names commitment someone-else, not first; start a new reviewer ... never edit a reviewer's report to fit", and one ending "commit: 0000000" gives "names 0000000, which is not a commit", while the review with the same quoted line gets the precise repair 'findings: holds a line the loop cannot read as an entry: "commit: 0000000"; write each finding as a - entry, and put prose after a heading'. (blocked); resolved at d7ec978c: in a record header the first line of a field is its value, so a field line quoted lower cannot replace it; a quoted line inside a list is named as the stray it is, and one below a heading is prose (independent d88e131 2)
  - resolved: headerOf treats a rule as an underlined heading unless the line above it is one of commitment, commit, examined, findings or reviewer, so a record that carries any other field above a rule reads as having its fields below a heading: a report with "range: aaaaaaa..{short}" above "---" is refused with "names examined and findings below a heading ... move the record's fields to the top, each on its own line, above any heading or prose" although they already are, and the review with that same line is refused with "examined: needs a nonempty list of what the review examined", while the whitelisted "reviewer: r" above the same rule reaches Done; the fix is to delete the rule or rename the field, which neither message says. (blocked); resolved at d7ec978c: a rule is a heading's underline only under heading text, which is any line that is not a field with a value and not one of the record's own fields, so a record carrying any other field above a rule is read (independent d88e131 3)
  - resolved: The new fields-below-a-heading repair runs before the commitment and commit checks, so a report that a new reviewer really should replace is edited first: a report at an earlier real commit with "# Independent report" above its fields gives "repair .cairn/reviews/first.independent.md ... move the record's fields to the top ... and change none of the reviewer's words", and only after that edit does wake say "names commit 3f32bab and the review names a9bffcd ... start a new reviewer", so the agent spends a cycle rewriting a report that is then discarded. (wording); resolved at d7ec978c: the report's identity is read from its fields wherever they sit, so a report for another commitment or commit is replaced before any repair is asked for (independent d88e131 4)
  - resolved: the review gets none of the help the report gets when its fields sit below prose: a review whose first line is a sentence before commitment: is refused with only "commit: names the commit the review examined", where the same mistake in the report names the line above and the move. (blocked); resolved at d7ec978c: reviewOf names a field that sits below a heading and a commitment: line blocked by prose, with the same instruction to move the fields to the top
  - resolved: a prose line between the review's commit: and examined: joins the commit value, and the verdict is LOOP-058's "a requirement or falsifier changed since the review, or its reviewed text is unavailable", which names neither the line nor the cause, while the report in the same shape is told that the line below its commitment: joined the field. (blocked); resolved at d7ec978c: a commit: value the line below it joined is named as such, before any freshness message
  - resolved: All four named defects hold at 62471010: a `---`, `===`, `--`, `====` or `-----` line between commit: and examined: now reaches Done in the review and in the report (it gave "examined: needs a nonempty list" and an unreadable commit before), the manual's `findings: []` advice with an "## Attacked" bullet list reaches Done, and a carrying line beside an earlier round's citation reaches Done in both orders while the genuine double carry is still refused. This entry is the report's confirmation of the twentieth round, not a defect; I reproduced the same five shapes at 62471010 (independent 6247101 1)
  - resolved: A regression from the same fix: an underlined heading whose title ends with a colon is no longer heading text, so honest prose under it is read as part of the findings list; the review "commitment: first / commit: <sha> / examined: / - the work / findings: [] / (blank) / Attacked: / --------- / (blank) / - the reader, line by line" is refused with 'findings: holds a line the loop cannot read as an entry: "Attacked:", and the entries after it are unread', the same shape in the report is refused with the reviewer-facing "give a finding its own - entry ... or ask the reviewer again", and both reach Done at 09abf7f7 and with "## Attacked:" or a colon-free "Attacked" underlined at 62471010.; resolved at 704ae301: only the record's own fields (commitment, commit, examined, findings, reviewer) are excluded from heading text, so a title ending in a colon is a heading again and the prose under it is prose (independent 6247101 2)
  - resolved: A report whose header fields sit below a title is not repaired but sent to a new reviewer: with "Independent report / ================== " or any plain prose line above commitment:, wake says ".cairn/reviews/first.independent.md names commitment none, not first; start a new reviewer", and a prose line between commitment: and commit: swallows it into the value ("names commitment first I was given the commitment and the commit range., not first"), so a complete, honest report is discarded although AGENTS.md forbids editing it; the identical mistake in the review gets the repair "the fields sit under a heading and the header ends at the first heading, hashed or underlined".; resolved at 704ae301: a report whose fields sit below a heading is repaired in place, naming the fields and the move, and a report whose first field is blocked by prose, or whose value a following line joined, is named the same way; neither asks for a new reviewer, so an honest report is never discarded (independent 6247101 3)
  - resolved: The report's repair never names the heading boundary that the review's does: a report with "# Independent report" above its fields is refused with "needs a nonempty examined: list; keep the reviewer's own words: give a finding its own - entry, put a heading above prose that follows the findings, or ask the reviewer again", none of which applies, while the same review gets "the fields sit under a heading and the header ends at the first heading, hashed or underlined".; resolved at 704ae301 by the same repair: the report's verdict now names the heading boundary and what to move, in place of advice about findings entries that did not apply (independent 6247101 4)
  - resolved: bin/cairn.mjs still carries the wording the twentieth round fixed in docs/manual.md: a review with an unrecognized entry is told to "use list entries ... or leave findings empty when there are no findings (LOOP-086)", and a record written that way, with findings: naming no entries above any bulleted section, is refused a second time with "a finding sits under a heading, where the loop does not read it", while the same record written findings: [] reaches Done.; resolved at 704ae301: the repair says to write findings: [] when there are none, the form the kernel reads (independent 6247101 5)
  - resolved: No shipped developer document says Done now needs the independent report: README.md's reviewed(c) and done(c) definitions and its paragraph on "what the definitions leave out" name only the review, its examined commit and its findings, and the manual's "Decide what Done means for you" lists "a current review with no open finding" while its "Get unstuck" table has rows for resolve, repair, record and scope but none for `review <slug>`; wake refuses Done with "no independent report is committed at .cairn/reviews/first.independent.md; start a new reviewer", so the documents state a Done condition the kernel no longer has.; resolved at 704ae301: README's reviewed(c) gains the report clause and defines report(c) and carried, the manual's Done section explains the independent report and what Cairn can and cannot check about it, and the unstuck table has a row for a review verdict (independent 6247101 6)
  - resolved: Six committed reviews are refused by the reader at 62471010, and none of them by this commitment's work: command-help-is-always-available, host-paths-are-part-of-the-contract, humans-can-understand-and-use-cairn and skills-can-be-installed-with-the-skills-cli carry a "verification:" field inside examined: ('examined: holds a line the loop cannot read as an entry: "verification:", and the entries after it are unread'), and the-answer-reaches-the-agent and the-wake-names-an-action-or-refuses list "- none" under findings: ('finding 1 is unrecognized: "none"'); all six are refused identically by the kernel at 49012dc0, the remaining 61 read clean with no open finding, and this commitment's own .independent.md is not a review record and is never read by reviewOf. This entry is the report's survey, not a defect: the six are legacy records for finished commitments, refused for reasons they carried before this commitment, as my own reading of the same 68 records found (independent 6247101 7)
  - resolved: the kernel's two readers disagree about a horizontal rule: the header parser skips a line of dashes or equals, so a record whose fields are wrapped in front-matter rules is read, while the list reader names the closing rule, refusing the record with "findings: holds a line the loop cannot read as an entry" and offering a repair that describes neither a finding nor prose.; resolved at 704ae301: a rule at the margin separates in the list reader too, so a record whose fields are wrapped in front-matter rules is read by both readers alike
  - resolved: A `---`, `--` or `===` line directly after a field line is read as that field's setext underline, so the header ends one line early and the loop names a field that is plainly present: the review "commitment: first\ncommit: <sha>\nexamined:\n  - everything\nfindings: []\n---\n\n## Notes" is refused with "findings: is missing; write findings: [] when there are none (LOOP-086) (LOOP-108)", a `---` after the commit: line gives "examined: needs a nonempty list of what the review examined", the report side gives "needs findings: as a list of - entries, or findings: [] for none", none of them mentions a heading boundary because the "the fields sit under a heading" hint tests `#` headings only, and the same rule drops an indented continuation a `---` underlines without naming it.; resolved at 1fa3d06b: a setext underline now needs heading text at the margin, so a rule under a field line is not that field's underline and the field is read; the hint says the header ends at the first heading, hashed or underlined; and the rule is no longer absorbed into the field above it, which had made the commit unreadable (independent 09abf7f 1)
  - resolved: A review line that carries this report's finding and also cites an earlier report, at another commit, is refused as though it double-carried: against a report holding the one finding "the gate is wrong", the review "findings:\n  - resolved: the gate is wrong, fixed (independent aaaaaaa 3) (independent <short> 1)" gives "the independent report's finding 1 is not carried: its review line cites another finding too", in either citation order, although "aaaaaaa 3" names another round's report and tests/independent.test.mjs accepts that same citation on a separate line.; resolved at 1fa3d06b: only citations of this report count toward a double carry, so an earlier round's citation on the same line is history, as it already was on a line of its own (independent 09abf7f 2)
  - resolved: the manual's advice for a review with no findings produces a refused record: docs/manual.md says "Leave findings: empty when the review found no issues", and a record written that way with any bulleted section under a heading is refused with "a finding sits under a heading, where the loop does not read it; keep the findings in the header's list", while the same record written findings: [] reaches Done, so the shipped manual names a form the kernel refuses and the refusal does not name the form it reads.; resolved at 1fa3d06b: docs/manual.md now says to write findings: [] when the review found no issues, and says that form leaves the record's other bullets as prose
  - resolved: A review line citing finding number 0 reaches Done, so the eighteenth round's citation rule still lets a review claim support the report never gave: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n  - resolved: my own, fixed (independent <short> 0)" against a report holding one finding gives "Done: first ... the review at <sha> and its independent report are clean", while the identical record with 2 in place of 0 gives "the review cites (independent <short> 2), and the report holds 1 finding"; the cause is that `beyond` is computed with Array.prototype.find and 0 is also the sentinel for another round's citation, so `if (beyond)` is false for a real citation 0, and "(independent <short> 000)" behaves the same.; resolved at 0c0fd5f1: the citation numbers are collected and the out-of-range one is tested against undefined, not truth, so a citation of 0 or 000 is named like any other number the report does not hold (independent f6bdf50 1)
  - resolved: A findings field written `findings:[]` with no space is read as an empty findings list by the field reader but does not end the examined: list, so the record is refused: "examined:\n  - x at <sha>\nfindings:[]" gives "examined: holds a line the loop cannot read as an entry: \"findings:[]\"", which contradicts AGENTS.md's "An `examined:` list ends at `findings:`", and the report side gives the same refusal.; resolved at 0c0fd5f1: the examined list ends at the findings: field however it is spaced, so findings:[] is read as the declared empty list it is (independent f6bdf50 2)
  - resolved: The reorder advice is offered for any unread line that begins `examined:` or `findings:` in any case, so it names a fix the record already has: "examined:\n  - x at <sha>\nFindings:\n  - open: a real defect that nobody resolved\nfindings: []" is refused with "write examined: above findings:, both above the first heading" although examined: is already above findings: and both are above the first heading, and the same advice is given for an "Examined: also the tests" note inside a findings list and for `findings:[]`; the diagnosis is right but the only fix, bulleting or removing the line, is named nowhere.; resolved at 0c0fd5f1: the reorder advice is given only when the record really has findings: above examined:; any other field name inside a list is told to write that line as a - entry or take it out, and that the fields are examined: then findings:, each named once (independent f6bdf50 3)
  - resolved: A setext-underlined heading does not end the header, so an honest record whose prose sits under one is refused and told to do what it has already done: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n\nAttacked\n--------\n\n- the reader" gives "findings: holds a line the loop cannot read as an entry: \"Attacked\", and the entries after it are unread; write each finding as a - entry, and put prose after a heading", and the report side gives "put a heading above prose that follows the findings"; AGENTS.md says only "heading", and tests/record-boundaries.test.mjs itself treats "Example\n-------" as one of the section forms records use.; resolved at 0c0fd5f1: the header ends at a heading of either form, hashed or underlined with = or --, so prose under a setext heading is prose (independent f6bdf50 4)
  - resolved: A blank line before an indented continuation breaks the join the agreement promises: the review "findings:\n  - resolved: one, and how (independent <short> 1)\n\n    I reproduced it in a scratch clone." is refused with "findings: holds a line the loop cannot read as an entry: \"I reproduced it in a scratch clone.\"", the same shape in a report and in examined: is refused the same way, and the identical record without the blank line reaches Done, while AGENTS.md says "a line indented under an entry joins it" and "Blank lines between entries are nothing" and Markdown reads that line as the entry's own paragraph.; resolved at 0c0fd5f1: an indented continuation joins the entry above it across a blank line, as the agreement says, and the joined words are what must be carried (independent f6bdf50 5)
  - resolved: A report whose `commit:` line is duplicated passes the check that is meant to catch a re-dated report: "commitment: first\ncommit: <sha>\ncommit: <sha>\nreviewer: r\nexamined:\n  - the kernel\nfindings: []" reaches Done, although the report names the commit nowhere but its commit: lines, because only the first `^commit:` line is stripped before the text is searched; the same report with one commit: line gives "does not name commit <short> anywhere but its commit: line, so it was not written for this review".; resolved at 0c0fd5f1: every commit: line is stripped before the report is searched, so a doubled commit: line no longer stands in for naming the commit the reviewer examined (independent f6bdf50 6)
  - resolved: A review that carries an open finding under a capital "Findings:" line above a lowercase "findings: []" reaches Done with that finding invisible and no line named, because the examined list's boundary test is case-insensitive while the findings field match is case-sensitive: the record "examined:\n  - x at 69ec524c\nFindings:\n  - open: a real defect that nobody resolved\nfindings: []" gives "Done: reviews-carry-an-independent-report ... the review at 69ec524c and its independent report are clean", and "FINDINGS:" behaves the same, which breaks LOOP-086's "rather than disappearing from the gate" and this commitment's own rule that every other line inside a list is named.; resolved at 0b44d887: the examined list now ends only at the lowercase findings: the kernel reads, so a Findings: or FINDINGS: line inside the list is named as a line the loop cannot read and the open finding under it reaches the gate (independent 69ec524 1)
  - resolved: The same boundary silently truncates the examined list: "examined:\n  - the record reader at 69ec524c\nFindings: none yet\n  - the independent gate, which I also examined\nfindings: []" reaches Done with the second examined entry read past and no line named, although the loop names every other unreadable line inside a list.; resolved at 0b44d887 by the same boundary: the capital field name is named, with the entries after it reported unread (independent 69ec524 2)
  - resolved: A record whose findings: list sits above its examined: list is refused with a repair that cannot be carried out: the review "commit: ...\nfindings:\n  - resolved: a thing, and how (independent 69ec524 1)\nexamined:\n  - x" gives "findings: holds a line the loop cannot read as an entry: \"examined:\" ... write each finding as a - entry, and put prose after a heading", but the finding already is a - entry and the record holds no prose, and the only fix, moving examined: above findings:, is named nowhere; the report side gives the same message with "give a finding its own - entry ... or ask the reviewer again", which also does not fix it.; resolved at 0b44d887: when the line a list cannot read is the other field's name, the repair says to write examined: above findings:, both above the first heading, in the review and in the report (independent 69ec524 3)
  - resolved: AGENTS.md says "a line indented under an entry joins it", but a line indented under a finding that begins with "open:" or "resolved:" is not joined: the review "findings:\n  - resolved: a thing, and how (independent 69ec524 1)\n    open: another defect the loop should not lose" is refused with "findings: holds a line the loop cannot read as an entry: \"open: another defect the loop should not lose\"", while the same shape with any other first word joins and reaches Done, so the agreement describes a shape the kernel refuses.; resolved at 0b44d887: the agreement now says a line indented under an entry joins it, and so does a bullet nested beneath it, unless it reads as a finding of its own, in AGENTS.md and the byte-identical template (independent 69ec524 4)
  - resolved: a review line may cite a finding number the report does not have, and the commitment still reaches Done: with a report holding one finding, a review that carries finding 1 correctly and adds a line ending "(independent <sha> 7)" is accepted, so a record can claim independent support the report never gave.; resolved at 0b44d887: a citation whose number the report does not hold is now named, and my own findings go on their own lines with no citation
  - resolved: A review or report whose findings list is declared empty with "findings: []" is refused as "a finding sits under a heading" as soon as its body holds any bullet or numbered line, because 0dc44b2c dropped the empty guard from the heading rule, so LOOP-086's "an empty findings list MUST remain valid" no longer holds.; resolved at 923c91e0: the heading rule's empty-list guard is back, so a record that declares findings: [] is taken at its word and the bullets below a heading stay prose (independent 896cea2 1)
  - resolved: Six committed reviews in .cairn/reviews (the-hooks-and-gates-judge-by-records, the-contract-says-what-the-kernel-does, the-kernel-refuses-bad-records-and-leaves-no-trap, the-next-iteration-starts-from-the-specification, this-repository-declares-and-lints-what-it-reads, consumers-can-run-the-lint-and-open-the-skills) have exactly that shape, findings: [] above an "Attacked:" bullet list; every one is clean at ff0e9e56 and refused at 896cea21, so the regression hits this repository's own house style, not a constructed record.; resolved at 923c91e0 by the same guard; all six read clean through the fixed reader, and the four records that the reader still refuses for a verification: line inside examined: are refused identically at ff0e9e56 and at 0dc44b2c^, so they are older and untouched by this work (independent 896cea2 2)
  - resolved: The refusal a no-findings record now gets cannot be acted on: it says a finding sits under a heading and to keep the findings in the header's list when the record declares no findings, and the repair named for the same bullets before a heading ("write each finding as a - entry, and put prose after a heading") produces the other refusal when followed, so no no-findings review with a bulleted body is accepted in any form.; resolved at 923c91e0: no no-findings record produces that refusal any more, so there is no unactionable repair to word (independent 896cea2 3)
  - resolved: The working agreement's new sentence, "a bullet under a heading is read as prose unless it reads as a finding or the list above it is empty", states the same conflation of a declared "findings: []" with a list that has no entries, so the agreement now contradicts LOOP-086 and the kernel's own comment ("the record's only list") and has to be corrected with the kernel.; resolved at 923c91e0: the sentence now says a bullet under a heading is prose unless it reads as a finding or the record names findings: and lists none above it, and adds that findings: [] says there are none, in AGENTS.md and the byte-identical template (independent 896cea2 4)
  - resolved: The regression is untested: the new assertions in tests/independent.test.mjs exercise a findings-titled heading only beside a nonempty findings list, and no test covers a body bullet or numbered line beside "findings: []" in either record.; resolved at 923c91e0: a test writes findings: [] above an Attacked: bullet list in the review and in the report and expects Done, red on the kernel before the fix, and keeps the paired case where findings: names no entries above such a list refused (independent 896cea2 5)
  - resolved: A review whose findings list holds a margin line beginning "Reviewer:" still ends in silence and an open finding after it reaches Done, because reviewer: is the one of the five field names a review never carries above its lists, so the new "already above" rule never covers it; the same line inside the examined list drops every entry after it with no word in the verdict. Resolved: the boundary is plain: an examined: list ends at findings:, a findings: list ends at a heading or the record's end, and every other line inside either, a Reviewer: line included, is named with what it hid (0dc44b2c) (independent ff0e9e5 1)
  - resolved: The new heading rule refuses honest records: any bullet or number anywhere in the body, together with any body heading whose title contains the word "finding" (for example "## Findings from earlier rounds", or a report's "## How I reproduced the findings"), makes wake refuse the review with "a finding sits under a heading" or the report with "holds its findings under a heading" although every finding sits in the header list and is read; the same records report Done at 6ef294ba, so this is new at ff0e9e56. Resolved: the heading rule no longer reads a heading's title: a bullet under a heading is prose unless it reads as a finding or the list above it is empty, so a record with a findings-titled section is read (0dc44b2c) (independent ff0e9e5 2)
  - resolved: The report side still misses findings under a heading whose title does not contain the word "finding": a report with "- one" and then "## Further defects", "## Defects" or "## What I found" holding "- two is broken" reports Done saying "its independent report are clean" with finding 2 carried nowhere, so the fifteenth round's third finding is resolved only for headings named after findings. Resolved: not resolved as claimed at 6ef294ba, and narrowed here to a stated limit: a report's bullet under a heading beside a read list is prose, because nothing distinguishes it from a reviewer's own notes. The fix is a canonical record the loop writes and reads, captured in next-iteration as reviews-and-reports-are-written-in-a-form-the-loop-reads (0dc44b2c) (independent ff0e9e5 3)
  - resolved: The report's repair for findings that genuinely sit under a heading offers two fixes that do not apply, "give a finding its own - entry" and "put a heading above prose that follows the findings", and never the one that does, moving the entries above the first heading as the review's own message says, so a developer following the refusal cannot repair the report. Resolved: the report's repair for findings under a heading says they belong in the findings: list above the first heading (0dc44b2c) (independent ff0e9e5 4)
  - resolved: AGENTS.md still says each list "ends at the next field of the record (commitment:, commit:, examined:, findings: or reviewer:)" and that "every other line inside a list is named by the loop rather than read past", and the kernel now does neither: a repeated name is named instead of ending the list, and a reviewer: line ends a review's list in silence. Resolved: the working agreement states the rules the kernel follows, boundary by boundary, including what a bullet under a heading means (0dc44b2c) (independent ff0e9e5 5)
  - resolved: The independent report's repair message names only the heading fix, and following it destroys the finding: a report whose second finding lost its "- " is refused with "keep the reviewer's own words, and put a heading above prose that follows its findings", and putting that heading above the line makes wake report Done with "its independent report are clean" and finding 2 carried nowhere, which is the LOOP-020 falsifier; the b0f53f96 message named the repair that actually fits ("put the reviewer's own words in this form"), and that half was dropped. Resolved: the report's repair offers both fixes, a - entry for a finding and a heading above prose, so following it cannot hide a finding (1533e16e) (independent 6ef294b 1)
  - resolved: A margin line named after one of the record's own five fields still ends a list in silence: a review holding one resolved entry, then "Examined: also the tests" or "Commitment: first", then "- open: a real defect" reports Done with the open finding gone, and a report with "- one", "Commit: 1234567 is what I read" or "Findings: two", then "- two is broken" reports Done with finding 2 uncarried; the verdict says nothing in either case. Resolved: a list ends only at a field that has not already appeared above it, so Examined:, Commitment: or Commit: repeated inside a list is named with the entries it hid (1533e16e) (independent 6ef294b 2)
  - resolved: The report's findings under a heading are read and named only when its header list is empty, so entries beside a nonempty list are silently ignored: a report with "- one" and then "## Findings" listing "- two is broken", as a bullet or numbered, reports Done saying "its independent report are clean" while finding 2 is carried nowhere, although the same shape in the review is refused. Resolved: a heading about findings names the report's list whatever its header list holds, and prose under another heading stays prose (1533e16e) (independent 6ef294b 3)
  - resolved: A margin line shaped like a field silently ends a findings list and drops every entry after it, so a review holding one resolved entry, then "Status: in progress", then "- open: a real defect I found" reports Done with the open finding gone, which is LOOP-086's Status clause and its open-findings clause; reproduced in a scratch clone at b0f53f96. Resolved: a list ends only at commitment:, commit:, examined:, findings: or reviewer:, so a Status line between entries is named with the entries it hid rather than ending the list (e1dffcf6) (independent b0f53f9 1)
  - resolved: The same break hides a review finding whose bullet and indent were lost: "open: the cache is never invalidated" at the margin ends the list and reports Done, alone or with a further valid "- open:" entry after it, so the thirteenth round's finding 2 is resolved only for lines a field pattern does not match. Resolved: a finding at the margin without its bullet is named by its own words, since it is no longer taken for a field (e1dffcf6) (independent b0f53f9 2)
  - resolved: In the independent report the same break carries findings nowhere: "- one", "Note: I also read the tests.", "- two is broken too" reports Done with "its independent report are clean" and finding 2 uncarried, which is the LOOP-020 falsifier; reproduced also with "Status:", "Reproduced:" and "Also examined:" on that middle line. Resolved: the same rule serves the report: Note:, Status:, Reproduced: and Also examined: between entries are named, and the entries after them are read as dropped (e1dffcf6) (independent b0f53f9 3)
  - resolved: The examined: list of either record truncates the same way with no word in the verdict: a field-shaped line between entries keeps only the entries above it and wake reports Done, although a plain sentence in the same place is now named, so the thirteenth round's finding 4 is resolved only in part. Resolved: examined: is read by the same rule in both records, and its unread line is named (e1dffcf6) (independent b0f53f9 4)
  - resolved: AGENTS.md says a list "ends at the next field" without saying what a field is, and the kernel treats any margin line of letters, spaces, hyphens or underscores plus a colon as one, so "open:", "resolved:" and "Status:" all end the list; the builder's review claims "every line inside a list is named wherever it sits", which is not what the kernel does, and a builder following the agreement cannot tell such a line ended the list in silence. Resolved: the working agreement names the fields that end a list, and the reader's rules are the ones the kernel follows (e1dffcf6) (independent b0f53f9 5)
  - resolved: The refusal for a note inside a list never names the fix the agreement allows: the review is told "write each finding as a - entry" and the report "put the reviewer's own words in this form without changing them, or ask the reviewer again", while the only correct repair for a verbatim report ending in a closing sentence is a heading above that sentence, which no message mentions. Resolved: both refusals name the fix the agreement allows: the review writes each finding as a - entry and puts prose after a heading, and the report keeps the reviewer's words with a heading above prose that follows its findings (e1dffcf6) (independent b0f53f9 6)
  - resolved: findings: [] followed by list entries is read as no findings and nothing names the entries: a review whose header says findings: [] and then lists "- open: a defect still unfixed" reports Done, and a report with findings: [] and its findings beneath it reports Done with "its independent report are clean" while none of them is carried, which is both falsifiers; reproduced in a scratch clone at 52010049. Resolved: findings: [] with entries beneath it names the first of them, in the review and in the report, since a list that says it is empty and then lists findings is read in part (c55a4a3d) (independent 5201004 1)
  - resolved: A line the reader cannot read is still ignored when nothing follows it, so the LOOP-086 falsifier's own line placed second ("REM-002: Historical agent checks remain inside role-input JSON rather than readable sections." with "Status: in progress" after it, under one valid resolved entry) reports Done, and a report whose last finding merely lost its "- " reports Done with "its independent report are clean" and that finding carried nowhere; the twelfth round's finding 1 is resolved only for lines that begin with open: or resolved:, and its finding 3 only when another entry follows. Resolved: every line inside a list is named wherever it sits, so a trailing line that lost its bullet is named rather than taken for prose; an indented line still joins its entry, which is a wrapped line, and the joined words are then what the review must carry (c55a4a3d) (independent 5201004 2)
  - resolved: The new AGENTS.md paragraph states two rules the kernel does not follow: it says any other line "ends the list, and the loop names it rather than read past it", while the kernel names it only when nothing was read, an entry follows, or it reads as a finding, and it says a bullet nested beneath an entry belongs to that entry, while the kernel refuses a nested "- open:" or "- resolved:" bullet as unreadable; a builder following the agreement cannot know its trailing stray line was dropped in silence. Resolved: the working agreement states the rules the kernel follows: where a list ends, what joins an entry, that a nested finding-shaped bullet is named, and that prose goes after a heading (c55a4a3d) (independent 5201004 3)
  - resolved: The examined: list drops every entry after a line the reader cannot read and no verdict says so: a review or report whose examined: list has a plain sentence between entries keeps only the entries above it and wake reports Done, although the same interruption in findings: is now named. Resolved: examined: is read by the same rules and its unread line is named, in the review and in the report (c55a4a3d) (independent 5201004 4)
  - resolved: Two refusal messages send the developer to the wrong line: when examined: has no entries the repair adds 'this line is not an entry: "findings:"', naming the correct following field, and every LOOP-108 repair on a review that has any heading appends "the fields sit under a heading and the header ends at the first heading" although the fields were read from above it, which is every real review record in this repository. Resolved: a list ends at the next field, so the following field is never named as a stray line; the heading hint is added only when a field really sits below the first heading (c55a4a3d) (independent 5201004 5)
  - resolved: A findings entry that lost its "- " and sits indented under the entry above is glued onto it instead of being named: a review holding "- resolved: one, fixed" and then "  open: the cache is never invalidated" reports Done with "the review at <commit> and its independent report are clean", so the eleventh round's finding 2 is fixed only when the unreadable line is unindented or the first thing in the list, and the LOOP-086 falsifier's own shape still hides an open finding in second place; reproduced with a 2-space line, a 4-space line and a nested "- open:" sub-bullet, and the wake says nothing about the line it joined. Resolved: a line indented under an entry joins it only when it does not read as a finding; a plain open: line or a nested open: bullet is named by its own text (bbb8696b) (independent 83dec7e 1)
  - resolved: Once one entry has been read, a line the reader cannot read is named only when it begins with open: or resolved:, so any other interruption ends the review's list in silence and every entry after it is dropped: a plain sentence between entries, or a finding written as two paragraphs, makes a following "- open: <defect>" disappear and wake reports Done; reproduced both ways in a scratch clone at 83dec7ed. Resolved: a line the reader cannot read is named whenever an entry follows it, whatever it says, so a sentence between entries or a second paragraph no longer drops the entries after it in silence (bbb8696b) (independent 83dec7e 2)
  - resolved: The same silence is total in the independent report, whose findings never begin with open: or resolved:, so no unreadable line in it is ever named: a report with "- one", an unindented sentence such as "I also read the tests.", and then "- two is broken too" reaches Done with "its independent report are clean" while finding 2 is carried nowhere, which is the LOOP-020 falsifier; reproduced with and without blank lines around the sentence. Resolved: the same rule serves the report, whose findings carry no prefix: an interrupting sentence with entries after it is a repair naming that line (bbb8696b) (independent 83dec7e 3)
  - resolved: A report whose findings sit under a heading is neither read nor named, because the heading check matches only a bullet beginning open: or resolved:, which a report never writes: a report with an empty findings: list or findings: [] and then "## Findings" listing two numbered defects reaches Done with "its independent report are clean", so the eleventh round's finding 6 is fixed for the review only and the report side is unguarded. Resolved: the report's findings under a heading are named: a list below the first heading with nothing read above it, when the header list is empty or the heading is about findings (bbb8696b) (independent 83dec7e 4)
  - resolved: In the review, a finding-shaped bullet under a heading is named only when nothing was read above it: a review that carries one resolved entry in its header list and then lists "- open: a defect still unfixed" under "## Open" reaches Done with no word about it, so the second half of the eleventh round's finding 6 is not resolved, and LOOP-071 does not force this, since the loop already refuses that record rather than reading it when the header list is empty. Resolved: an open finding-shaped bullet under a heading is named even when the header list was read, and a resolved decoy beside a real list stays unread, which LOOP-071 requires; an open finding the loop did read is still named first, so a decoy paragraph cannot turn resolve into repair (bbb8696b) (independent 83dec7e 5)
  - resolved: The refusal for an indented line directly under a review entry still blames the wrong thing: with the note or sub-bullet joined to the entry, the message is "a review line cites (independent <commit> 1) but does not end with it; put the citation last on its line" although the citation already ends its line and the glued line is the cause, so the eleventh round's finding 5 is fixed only for the blank-line case and a developer cannot tell which line to move. Resolved: the citation refusal adds that a line indented under a finding joins it, so notes belong out of the list (bbb8696b) (independent 83dec7e 6)
  - resolved: The working agreement was not updated for the new reader: AGENTS.md still tells the builder the findings list "is one unbroken list of `- ` entries above any heading: a blank line inside it ends it", which this kernel contradicts, says nothing about numbered entries, nested details or the line it now refuses to read, and never states that the independent report must carry commitment:, commit:, examined: and findings: above the first heading, although the kernel refuses a verbatim report that lacks them. Resolved: the working agreement states the reader's rules for examined: and findings:, any bullet or number, nested details, the line it refuses to read, prose after a heading, and the fields an independent report carries (bbb8696b) (independent 83dec7e 7)
  - resolved: A findings list written as a numbered list (1., 2.), as plain indented lines, or as a prose paragraph is read as an empty list, so wake reports Done saying "its independent report are clean" while the report's findings are carried nowhere; reproduced on this repository's own records at a9cd734c, where the tenth report's four findings renumbered 1. to 4. gave Done, and every one of those shapes was refused with an actionable repair by the 13c512de kernel. Resolved: listOf reads any bullet or number as an entry, and names a line it cannot read as an entry instead of reading the list as empty; a scalar findings value is named too, so the shapes that reached Done are repairs again (c3c24497) (independent a9cd734 1)
  - resolved: The same silent drop in the review's own list lets an open finding reach Done: the LOOP-086 falsifier's own shape, an un-bulleted "REM-002: Historical agent checks remain inside role-input JSON rather than readable sections." line under findings: together with "Status: in progress", reports Done at a9cd734c and was "Resolvable: repair .cairn/reviews/first.md" at 13c512de, so the non-list findings value now disappears from the gate instead of producing a verdict naming the file and the supported format. Resolved: the same reader serves the review, so the LOOP-086 shape, an unbulleted line under findings:, is a repair naming that line, and no open finding disappears (c3c24497) (independent a9cd734 2)
  - resolved: A review or report whose examined: list begins after a blank line is refused with "examined: needs a nonempty list of what the review examined" or "needs a nonempty examined: list", naming a field that is present and correctly written, with nothing naming the blank line as the cause; this is the same shape as the tenth report's finding 3, which the review marks resolved but which was fixed only for findings:. Resolved: examined: is read by the same reader in the review and the report, so a blank line before its first entry loses nothing (c3c24497) (independent a9cd734 3)
  - resolved: Any extra bullet under findings: in the report becomes a finding the builder is told to carry: a nested detail sub-bullet ("  - one" then "    - reproduced in a scratch clone") and a trailing prose bullet ("- I also read the tests and the walkthrough.") both give "the independent report's finding 2 is not carried", while the identical prose under findings: [] reaches Done, so the refusal still depends on whether the reviewer found anything and the only ways out are carrying prose as a finding or editing a report the agreement says never to edit. Resolved: a bullet nested under an entry belongs to that entry, so a detail sub-bullet is not a second finding; a bullet at the list's own level is still a finding, and the working agreement says prose goes in examined: or after a heading (c3c24497) (independent a9cd734 4)
  - resolved: An indented line after the findings list, even across a blank line, is glued onto the last entry: a report note "  Reproduced in a scratch clone." makes the refusal read "its review line does not begin with its words: one Reproduced in a scratch clone.", and an indented review note makes it read "a review line cites (independent X 1) but does not end with it; put the citation last on its line" when the citation already ends the line, so neither message names the indented line a developer has to move. Resolved: an indented line joins its entry only when it follows the entry directly; after a blank line the list ends, so a note below it is prose and no message blames the entry (c3c24497) (independent a9cd734 5)
  - resolved: A findings entry that sits under a heading is ignored with no word about it: a review with "findings:" in the header and "- open: a real defect I found" under "## Findings" reaches Done, and so does a review that carries finding 1 above the heading and lists a second, unfixed defect below it, so an open finding in the record reaches Done while the loop reads the record only in part and says nothing. Resolved: a finding-shaped bullet under a heading, with nothing read above it, is a repair naming the heading; a decoy under a heading beside a real list stays unread, which LOOP-071 requires (c3c24497) (independent a9cd734 6)
  - resolved: A review whose findings list begins after one blank line (findings: then an empty line then the entries) has every entry silently dropped and reaches Done with the words "the review at 13c512de and its independent report are clean", although the record holds "open: the blank line above hides this finding from the gate"; deleting the blank line correctly gives "resolve". Reproduced on the real review record at 13c512de. findingsBreak only reports a break when its matched block already contains an entry, so the blank line before the first entry is invisible to it. The ninth report's finding 2 named exactly this case ("a review whose whole findings list follows a blank line reaches Done while holding open: the gate never reads this") and the review marks it resolved; that half is not fixed, and the same shape also refuses an honest review with "finding 1 is not carried: no review line cites (independent <commit> 1)" when the citing line is present after the blank. Resolved: the review's findings are read from the record's text by one reader, findingsOf: every bullet after findings: up to the first heading, blank lines between entries allowed, a wrapped line joined to its entry, so no entry is dropped and an open finding after a blank line still gates Done (0c6f6d91) (independent 13c512d 1)
  - resolved: The same shape in the independent report is refused with "needs findings: as a list of one-line entries, or findings: [], above any heading", which describes what the report already is: a one-line entry list above any heading. The precise new message never fires for it, so the builder is told the report must be something it looks like, with nothing naming the blank line as the cause. Reproduced on the real report record at 13c512de. Resolved: the report's findings are read by the same reader, so a list that begins after a blank line is read rather than called malformed; findings: is absent only when the record has no findings: line above a heading (0c6f6d91) (independent 13c512d 2)
  - resolved: A blank line inside examined: ends the record header, so the findings: line below it is never read; the review is refused with "findings: is missing; write findings: [] when there are none (LOOP-086) (LOOP-108)" and the report with "needs findings: as a list of one-line entries", both naming a field that is present and correctly written, and neither naming examined: or the blank line. Reproduced on both real records at 13c512de. Resolved: the reader does not depend on the flat field header, so a blank line inside examined: no longer hides the findings list (0c6f6d91) (independent 13c512d 3)
  - resolved: A report whose findings list is followed by a blank line and then a prose bullet ("- I also read the tests and the walkthrough.") is refused with "a blank line inside findings: ends the list, and every entry after it is unread", although that bullet is prose and no entry is lost; the identical shape with findings: [] reaches Done and is asserted as honest by an existing test, so the refusal depends on whether the reviewer found anything. The same message adds "put the reviewer's own words in this form without changing them", while the only way to pass is to change the file, and the agreement's instructions for starting the reviewer never ask for the unbroken - list, so an honest reviewer's loose Markdown costs a round every time. Reproduced on the real report record at 13c512de. Resolved: the detector is gone, so no honest list is refused: a bullet under findings: is read as a finding, and the working agreement says prose goes in examined: or after a heading (0c6f6d91) (independent 13c512d 4)
  - resolved: A blank line between two entries of the independent report's findings list silently drops every entry after it, so wake reports Done with a finding the review never carries, which is the LOOP-020 falsifier. Reproduced on the real report at 538ba9a0: two findings with one blank line between them and only the first carried gave "Done: reviews-carry-an-independent-report ... its independent report are clean", and removing the blank line refused the second finding correctly. A loose Markdown list is ordinary in a .md file, and nothing in AGENTS.md or the refusals tells the reviewer or the builder that a blank line loses findings. The same loss happens when further findings follow a Markdown heading, and a "*" bullet after a "-" entry is refused as "has a finding that wraps onto a second line", which names a cause the reader cannot match to the file. Resolved: one reader names a findings list the loop reads only in part, and the report gate uses it: a blank line inside a list with entries, or a bullet the loop does not read, is a repair naming the cause. A list that follows a heading stays ignored, which LOOP-071 requires so a decoy section cannot replace a finding, and tests/record-boundaries.test.mjs holds that (3a289a0b) (independent 538ba9a 1)
  - resolved: The same blank line inside the review's own findings list drops every line after it, and the report gate reads that list, so an honest review is wrongly refused and an open finding can reach Done. Reproduced at 538ba9a0: a review carrying finding 1 and, after a blank line, finding 2 with the correct citation is refused with "no review line cites (independent <commit> 2)" although that line is present; and a review whose whole findings list follows a blank line reaches Done while holding "open: the gate never reads this". The report side of this exact case was fixed earlier in the commitment by naming it a repair; the review side was left unfixed. Resolved: the same reader runs in reviewOf, so a review whose findings a blank line or an unread bullet cuts short is a repair instead of a silent Done; the working agreement says the findings are one unbroken list of - entries above any heading (3a289a0b) (independent 538ba9a 2)
  - resolved: The kernel ignores an independent report that exists on disk but is not committed. Wake asks for the review file to be committed when it has uncommitted changes, but it never does that for the report. Reproduced two ways. First, the review was redone at a later commit and committed, and the new reviewer's report for that commit was written but not committed. With the earlier report still at HEAD, wake refused with "names commit <old> and the review names <new> ... start a new reviewer". That sends the builder to start another reviewer when the only thing missing is a commit. Second, the committed report said findings: [], and an uncommitted edit to the same report added "a real defect". Wake reported Done, although the report on disk has a finding the review does not carry. Resolved: the report is checked for uncommitted changes beside the review, and whichever differs from its committed version is named commit; a new report that only needs committing is no longer sent to a new reviewer, and an edit on disk can no longer hide behind the committed version (7b2e9fab) (independent 03961bb 1)
  - resolved: A review line that cites a finding but does not end with that citation is refused with "no review line cites (independent <commit> n)", although the line visibly cites it. Reproduced with "resolved: a defect, fixed (independent S 1)." (a period after the citation), "resolved: a defect (independent S 1), fixed in abc1234" and "resolved: a defect, fixed (independent S 1) (see 0350326)". AGENTS.md shows the citation last but never says it must end the line, and this refusal does not say so either. Only the wrapped-line refusal mentions it, so a developer cannot tell what to fix. Resolved: a line that cites the finding anywhere but last is refused with "cites it but does not end with it; put the citation last on its line", and the working agreement says the citation goes last (9475641d) (independent 1313f32 1)
  - resolved: A report finding whose own words end with a citation still cannot be carried in the form "resolved: <its words> (independent S n)". Reproduced: the finding "the sixth-round resolution of the wrap refusal does not hold (independent e69a3c8 2)", carried that way, is refused with "its review line cites another finding too". The quoted citation and the builder's citation together form the end of the line, and the refusal does not say the finding's own words are the cause. The documented form "resolved: <its words>, and how (independent S n)" is carried, so only this shorter form is affected, and tests/independent.test.mjs covers only a citation in the middle of a finding. Resolved: the finding's full words are matched first, and the carrying citation is read only from what follows them, so a finding that ends with a citation is carried in the short form as in the long (9475641d) (independent 1313f32 2)
  - resolved: A report finding whose own text contains a citation in the required form can never be carried, even when the builder copies it exactly. Reproduced: the finding "the claimed fix for" + a parenthesised citation of finding 3 of an earlier report + "does not hold", copied word for word and followed by its own citation, is refused with "its review line cites another finding too". When the quoted citation names the current report's commit and the same number, the refusal is "its review line does not begin with its words", because citations are removed from the line before its words are compared. Neither refusal says the finding's own text is the cause. The only ways forward are editing the report, which the working agreement forbids, or starting a new reviewer. A later-round reviewer who checks earlier resolutions by their citations will write findings like this, and tests/independent.test.mjs does not cover the case. Resolved: only the citations at the end of a review line are read as carrying; a citation inside a finding's own words is compared as words, so copying the finding and appending its citation carries it, whichever commit the quoted citation names (03503067) (independent e69a3c8 1)
  - resolved: A review with a finding line that wraps onto a second line is refused even when that line has no citation and the report has findings: []. The refusal gives a cause that does not apply: "a review finding wraps onto a second line, which loses its citation". Reproduced with "resolved: an old defect fixed by a long explanation" followed by the indented line "that wraps, no citation", beside a report with findings: []. Resolved: the wrap check runs only when the report has findings to carry, and its refusal says the citation must sit at the end of the line it carries (03503067) (independent e69a3c8 2)
  - resolved: Finding numbers start again at 1 in every report, but the rule looks for citations across the whole review, so an honest second round is refused. Reproduced: a review with an earlier round's "resolved: the parser drops wrapped lines, fixed in abc (independent 1)" and the new "resolved: empty input crashes wake, fixed (independent 1)" is refused with "2 review lines cite it". The review for this commitment already cites (independent 1) to (independent 5), so any fifth-round report with findings will hit this. The refusal does not say which lines clash or that the earlier round's citations must be removed or renamed. Resolved: a citation is (independent <report commit> n), so an earlier round's (independent <its commit> 1) never collides with this round's; a refusal names the citation it counted (520ada3e) (independent f4f4e27 1)
  - resolved: A line from an earlier round can carry a new report's finding that has the same number and starts with the same words, so a defect a new reviewer says is still there reaches Done with no new line. Reproduced: the review holds only the earlier "resolved: the parser drops wrapped lines, fixed in abc (independent 1)", the new report's finding 1 is "the parser drops wrapped lines", and wake reported Done. Resolved: an earlier round's line cites that round's commit, so it cannot carry a finding of a report at another commit (520ada3e) (independent f4f4e27 2)
  - resolved: When the commit is missing from examined:, the refusal sends the builder to a new reviewer but never says the reviewer must name the commit in its examined: list. AGENTS.md says only "The report names the commit it examined", and the prompt it tells the builder to give does not ask for this, so a new reviewer can be refused the same way. Reproduced: a report that names the review's commit on its reviewer: line and its commit: line, but not in examined:, is refused with "does not name commit ... so it was not written for this review; start a new reviewer". Resolved: the report may name the review's commit anywhere but its commit: line, and the refusal and the working agreement say to ask the reviewer to name the commit it examined in its examined: list (520ada3e) (independent f4f4e27 3)
  - resolved: A review finding that wraps onto a second line loses its citation, and the refusal gives the wrong cause. Reproduced: "resolved: a defect, fixed by a long explanation" followed by the indented line "that wraps (independent 1)" is refused with "no review line cites it". The refusal does not say the line wrapped, although the same problem in the report is named as a wrapped finding. Resolved: a review finding that wraps onto a second line is named repair on the review, with the cause (520ada3e) (independent f4f4e27 4)
  - resolved: A line that cites the same finding twice is refused with the wrong reason. Reproduced: "resolved: a defect, see (independent 1) (independent 1)" is refused with "its review line cites another finding too", although it cites no other finding. Resolved: citations on a line are counted as distinct commit and number pairs, so the same citation twice is one (520ada3e) (independent f4f4e27 5)
  - resolved: tests/independent.test.mjs does not cover a review that carries two rounds of reports, where citation numbers repeat or a line from an earlier round matches a new finding, or a report that names its commit somewhere other than examined:. Resolved: tests/independent.test.mjs gains a two-round review, an old line with the same words, a doubled citation, a commit named on the reviewer: line, and a wrapped review line; it failed on the kernel before this fix (520ada3e) (independent f4f4e27 6)
  - resolved: The fix for editing a stale report's commit: line only compares the report with its previous committed version, so an agent can still reach Done without a new independent report. Reproduced, each reaching Done after the review was redone at a later commit: the old report with a new commit: line and one extra blank line at the end; with a new commit: line and one character added to the reviewer: line; and deleted in one commit, then re-added unchanged except for the commit: line. Resolved: the report's examined list must name the review's commit; a re-dated old report, however else it is edited or re-added, cannot, and is sent to a new reviewer. The commit-line comparison is removed (8c4641fe) (independent c7c1804 1)
  - resolved: A report from a genuinely new reviewer is refused when its wording matches the previous report apart from the commit: line. This is likely when the reviewer finds nothing and gets the same prompt. Reproduced: a report with the same reviewer: and examined: lines and findings: [] was refused as "edited, not written again". Starting another reviewer, as the refusal says, can produce the same words and the same refusal. The test helper avoids this only by putting the commit into the examined line. Resolved: with the comparison removed, a new report in the same words is accepted as long as it names the commit it examined, which a reviewer given the commit range does (8c4641fe) (independent c7c1804 2)
  - resolved: A clause break still lets an unrelated review line carry a short report finding. Reproduced, each reaching Done: report "a defect" carried by "resolved: a defect, unrelated walkthrough typo fixed"; "wake" carried by "resolved: wake (the help text typo), fixed"; "the report" carried by "resolved: the report - writer skill typo fixed". Resolved: a finding is carried only by the one review line that cites (independent n) and begins with its words; words alone never carry (8c4641fe) (independent c7c1804 3)
  - resolved: A report finding that ends with a period is refused in the usual ways of carrying it. Reproduced for the finding "The parser drops wrapped lines.": refused were "resolved: The parser drops wrapped lines. Fixed in abc1234", "resolved: The parser drops wrapped lines, fixed in abc1234" and "resolved: The parser drops wrapped lines; fixed in abc1234". To follow the "resolved: <its text>, and how" form that AGENTS.md and the refusal describe, the builder must write a period followed by a comma (".,"), and the refusal does not say so. Resolved: a finding's own final punctuation is ignored when its line is matched, so resolved: <its words>, and how works for a finding ending with a period (8c4641fe) (independent c7c1804 4)
  - resolved: tests/independent.test.mjs covers only the exact case where nothing but the commit: line changed. It does not test a commit: line edit combined with another small change, a report deleted and re-added, a new report with the same wording, a short finding carried by an unrelated line at a comma or parenthesis, or a finding that ends with a period. Resolved: tests/independent.test.mjs is rewritten around the two rules, with every case the four reviewers reproduced, including re-dated reports edited three ways or re-added, a same-worded new report, unrelated lines, and a finding ending with a period (8c4641fe) (independent c7c1804 5)
  - resolved: The claimed fix for the stale-report bypass changes only the message. An agent can still reach Done without a new independent report. Reproduced: review and report at commit A reach Done. After the review is redone at a later commit B, wake names "review first". Changing only the report's commit: line from A to B and committing gives "Done: first". The gate never checks that the report's findings or examined list changed, or that the report was rewritten after the review's commit. Resolved: a report that differs from its previous committed version only in its commit: line names a new reviewer; the test edits only that line after the review is redone and is refused (d6facf4d)
  - resolved: The looser matching lets an unrelated review finding carry a report finding. The kernel accepts the report's text followed by a hyphen, period or colon, even when that character is part of a longer word or path. Reproduced, each reaching Done: report "a defect" carried by "resolved: a defect-free parser was assumed, unrelated typo fixed"; "bin/cairn" carried by "resolved: bin/cairn.mjs walkthrough typo, fixed"; "wake" carried by "resolved: wake: Done message wording, reworded"; "the report" carried by "resolved: the report-writer skill typo, fixed". Resolved: the text after a carried finding must start a new clause: a comma, period, semicolon, ! or ? followed by a space or the end, or a space and a parenthesis, bracket, dash or Resolved:; the four reproduced cases are refused in the test (d6facf4d)
  - resolved: One review finding can carry several different report findings. Reproduced: the report findings "the gate" and "the gate, the parser and the message are wrong" were both carried by the single review finding "resolved: the gate, the parser and the message are wrong. fixed", and wake reported Done. Resolved: each report finding takes its own review finding, longest first; one line carrying two is refused and two lines are accepted in the test (d6facf4d)
  - resolved: The wrapped-finding check only reads indented list lines, so an unindented findings list skips it. The old prefix bypass then works again. Reproduced: a report with "findings:" then "- the" and a wrapped line "  kernel lets an empty report reach Done" was carried by "resolved: the, fixed walkthrough typo", and wake reported Done. Resolved: the findings block reads unindented list lines too, and an unindented wrapped finding is named repair in the test (d6facf4d)
  - resolved: The gate now reads the review from two different states. The open-finding check (reviewOf) reads the working tree, but the carried-finding check reads HEAD. Reproduced: HEAD's review carries "open: a defect". Changing it on disk to "resolved: a defect, fixed" without committing gives Done, even though the committed review still has an open finding. Separately, a review on disk that drops a finding carried at HEAD also gives Done. Resolved: an uncommitted change to the review is named commit before the report is judged, and the carried findings are read from HEAD (d6facf4d)
  - resolved: Some reasonable ways of carrying a finding are still refused. Reproduced for "resolved: A defect, fixed" (the match is case-sensitive), "resolved: a defect! fixed", "resolved: \"a defect\", fixed" and "resolved: a defect [fixed in abc]". The refusal says "carry it as open: <its text> or resolved: <its text>, and how". It does not say that the text must match exactly, including capital letters, or which punctuation may follow it. Resolved: matching ignores capitals and accepts ! ? and a bracket; the refusal now states the exact rule (d6facf4d)
  - resolved: The refusal can blame the wrong cause. When the report names a commit after the review's commit and the review was not redone, the message still says "a review redone at a later commit needs a new report there". Reproduced. It does not tell the developer that the report and the review simply name different commits, or which one to fix. Resolved: a commit mismatch now names both commits and says they must name the same commit (d6facf4d)
  - resolved: tests/independent.test.mjs does not test any of the cases above: editing only the report's commit: line, a report finding followed by a hyphen or period inside a longer word, one review finding carrying two report findings, an unindented wrapped finding, or a review that differs between the working tree and HEAD. Resolved: tests/independent.test.mjs gains a test for each case above; it failed on the kernel before this fix (d6facf4d)
  - resolved: A review finding written in the form AGENTS.md documents, "resolved: <defect, and how>", is refused. For example, "resolved: a defect, fixed by guarding the input" or "resolved: a defect — fixed in abc123" does not carry the report finding "a defect". The kernel accepts only the exact finding text followed by "Resolved:" (optionally after one of . ; : ,), and neither AGENTS.md, LOOP-020 nor the refusal message mentions that convention. Reproduced. Resolved: a carried finding is the report's text followed by nothing or by punctuation (a comma, period, semicolon, colon, parenthesis or dash) and how; the working agreement now shows open: <its text> and resolved: <its text>, and how (b51c2753)
  - resolved: A review finding with extra words before "Resolved:", such as "resolved: a defect (reproduced). Resolved: fixed", is refused as "not in it as open or resolved: a defect". The message does not say which form would be accepted. Reproduced. Resolved: a parenthesis before Resolved: is accepted, and the refusal names the accepted forms (b51c2753)
  - resolved: When the review is redone at a later commit and the report still names the earlier, real commit, wake names "repair .cairn/reviews/<slug>.independent.md". AGENTS.md defines repair as "fix the named record as the reason says", which invites the builder to edit the report's commit: line. Doing only that reaches Done with no new independent reviewer, which goes against LOOP-020 and the agreement's "a review redone at a later commit needs a new report there". Before the fix, this case named "review <slug>". Reproduced: after changing only the report's commit: line to the review's commit, wake reported Done. Resolved: a report at another commit, at a value that is not a commit, or for another commitment names review with a new reviewer and says never to edit a reviewer's report to fit; the working agreement says the same (b51c2753)
  - resolved: The other repair actions on the report (a wrapped finding, a scalar or misplaced findings list, a wrong commitment) also tell the builder to edit a report that AGENTS.md says to keep verbatim from the independent reviewer. The refusal does not say whether the fix is to reformat the reviewer's text or to ask the reviewer again. Resolved: a shape repair now says to put the reviewer's own words in the form without changing them, or to ask the reviewer again (b51c2753)
  - resolved: The wrapped-finding check runs its regex over all the text before the first heading, prose included, so prose that follows the findings list can trigger it. A report with "findings: []", a blank line, then a prose bullet whose text wraps onto an indented line ("- tried x" / "  and y") is refused with "has a finding that wraps onto a second line", although the report has no findings. The same happens when a wrapped prose bullet follows a real findings list after a blank line. Reproduced. Resolved: the check reads only the findings list, up to a blank or unindented line (b51c2753)
  - resolved: A report finding carried as "resolved: X." (the same text plus a trailing period, with no Resolved: note) is refused when the report says "X", because a trailing period alone is not accepted as a difference. Reproduced. This is minor, but the refusal names the finding without explaining the mismatch. Resolved: a trailing period is accepted as punctuation (b51c2753)
  - resolved: The report is read from HEAD with git show, but the review record is still read from the working tree. A report deleted on disk but not yet committed as deleted still lets Done through. Reproduced. This is a small inconsistency in which state the gate judges. Resolved: the review's findings are read from HEAD, like the report, when the review is committed (b51c2753)
  - resolved: tests/independent.test.mjs no longer covers a report at a real but different commit. The old "another commit" case now uses 0000000, which takes the "not a commit" path, so the stale-report path has no test. There are also no tests for the documented "resolved: <defect, and how>" form, prose after the findings list, or CRLF reports. Resolved: tests/independent.test.mjs gains a stale report at a real earlier commit, the documented carried forms, prose after the findings list and CRLF line endings; it failed on the kernel before this fix (b51c2753)
  - resolved: An independent report with no findings: field reaches Done, so a report holding only "commitment:" and "commit:" is taken as a clean report. The review record gets a repair for the same problem (LOOP-086), but the report is never checked for it. Reproduced. Resolved: a report needs findings: as a list or [], and is named repair otherwise (17185e73)
  - resolved: Findings that sit under a Markdown heading (for example "## Findings" followed by a findings list), or that follow "findings:" after a blank line, are not read, and wake reports Done even though the report raises a defect. Reproduced. Resolved: findings under a heading or after a blank line are not a list in the header, and the report is named repair (17185e73)
  - resolved: Wake reports Done when the report file exists in the working tree but was never committed, because the file's existence is checked on disk, not in the commit. This does not meet the falsifier's "at the review's commit". Reproduced with an untracked report. Resolved: the report is read with git show at HEAD, so a file on disk that was never committed is named as missing (17185e73)
  - resolved: Findings are matched by prefix (the review's finding starts with the report's text), so a short report finding counts as carried by an unrelated review finding. The report finding "the" was carried by "resolved: the walkthrough typo, fixed", and Done followed. Reproduced. Resolved: a report finding must equal a review finding, the open: or resolved: prefix and spacing aside, followed by nothing but a Resolved: note (17185e73)
  - resolved: A report finding that wraps onto an indented second line keeps only its first line, because the parser drops continuation lines of list items. With the prefix match, the finding "the / kernel lets an empty report reach Done" was carried by an unrelated review finding starting with "the", and wake reported Done. Reproduced. Resolved: a header line that continues a finding is named repair, keep each finding on one line (17185e73)
  - resolved: The commit match only asks that one value be a prefix of the other and be at least 7 characters long. A report commit of "<review sha>zzzz-not-a-sha" is accepted, and the value is never checked to be a real commit. Reproduced. Resolved: the commit value must be hexadecimal and resolve with git rev-parse to the same commit as the review's (17185e73)
  - resolved: The commit match is case-sensitive, so a report that names the right commit in uppercase is refused with "the report must be at the review's commit". Reproduced. Resolved: the value is lowercased before it is resolved (17185e73)
  - resolved: The report's commitment: field is never checked, so a report written for another commitment (commitment: someone-else) at the same commit lets Done through. Reproduced. Resolved: a report naming another commitment is named repair (17185e73)
  - resolved: A report finding written as "open: X" is refused even when the review carries "open: X". The kernel strips open:/resolved: from the review's findings but not from the report's, and AGENTS.md says to keep the report verbatim, so a reviewer copying the review format gets a refusal that is hard to understand. Reproduced. Resolved: open: and resolved: are stripped from the report's findings as from the review's (17185e73)
  - resolved: A scalar report line such as "findings: none" is read as one finding named "none". The refusal "finding is not in the review as open or resolved: none" does not tell the developer that the report is malformed, and nothing asks for the report to be repaired. Reproduced. Resolved: a scalar findings value is named repair with the supported forms (17185e73)
  - resolved: When the report is missing or not carried, the action named is "review <slug>", the same action as a stale review. A developer reading the refusal cannot tell from the action alone that the builder's review is fine and only the independent report needs work. This comes from reading the code; it is a wording issue, not a wrong verdict. Resolved: a malformed report is named repair on the report itself; a missing or uncarried one says the review is current and only the report needs work (17185e73)
  - resolved: The Done message still says "the review at <commit> is clean" and does not mention the independent report, so the success message does not show that LOOP-020's new condition was checked. This comes from reading the code. Resolved: Done now says the review and its independent report are clean (17185e73)
  - resolved: tests/independent.test.mjs covers only well-formed reports. None of the cases above is tested (missing or misplaced findings, uncommitted report, prefix or wrapped findings, bad commit value, other commitment, open: prefix). Resolved: tests/independent.test.mjs gains a case for each finding above, and failed on the kernel before this fix (17185e73)
  - resolved: a margin rule directly under a field line was joined to that field as a continuation, so a record with --- under its commit: line had an unreadable commit and was refused with LOOP-058, which names neither the line nor the cause; found while resolving the two above and resolved at 1fa3d06b: the header parser skips a rule and keeps reading the fields below it.
  - resolved: A heading whose title names findings still hides content in its subsections: a report with findings: [], ## Findings, ### 1. The gate drops an entry and one prose line reached Done with nothing named, because the section scan gave such a heading only the lines before its first subheading; a rule after the first content line did the same. The heading's presence is now the whole test, whatever sits beneath it, fixed in abd40f09 (independent 74848eb 1)
  - resolved: A heading whose title merely mentions findings is refused although every finding is in the list, and the repair named changed the reviewer's words. The contradiction is fixed in abd40f09: a report with such a heading is written again, in one step, and never retitled to fit. The refusal itself is kept deliberately, because no rule over a title tells "More findings" from "What the findings mean", and narrowing the title to its first word reopened the silent loss of finding 1 in my own test; this repository's own review freshness-and-scope-guidance-agree.md stays refused if it is ever read as a current review, and the agreement now tells a reviewer to title its other sections something that does not name findings (independent 74848eb 2)
  - resolved: AGENTS.md and docs/manual.md still described the narrower rule the kernel had before 70292c06, so an agent writing to the agreement could not predict the refusal. Both now say that such a heading is refused for its title alone, whatever sits beneath it, that a report carrying one is written again rather than retitled, and that the loop cannot tell one findings-naming title from another, fixed in abd40f09; AGENTS.md and its template stay byte-identical (independent 74848eb 3)
  - resolved: An empty report was told to add a commitment: line and then thrown away on the next wake. A report carrying none of commitment:, commit:, examined: or findings: now names a new reviewer once, with "is empty" for a blank file, fixed in abd40f09 (independent 74848eb 4)
  - resolved: a bullet that begins open: or resolved: under a body heading was recognised only when it was the body's first bullet, because below in listOf matched ENTRY once over the whole body and its tail matches across lines. The bullets are now read one line at a time, so a recognisable finding is named wherever it sits, in a report and in the review alike, fixed in c355cdc9 (independent a84cc81 1)
  - resolved: a report whose findings: list the loop read and the review carried was still refused for the title of an explanatory section. A findings-naming heading is now judged against the record's own list: it holds nothing when findings: declares none, and beside a list that holds entries it is elaboration that may hold prose but not a list, fixed in c355cdc9 (independent a84cc81 2)
  - resolved: the answer to a report whose findings sat under ## Findings as - open: bullets was a repair the next wake threw away, because the emptied heading was then refused for its title alone. With the heading judged against the list, moving the findings into the list leaves an acceptable record, so the repair completes as written, fixed in c355cdc9 (independent a84cc81 3)
  - resolved: a report short of commit: as well as commitment: was told to add the commitment: line and then refused for naming no commit. A report short of more than one field, where one of them is commit:, is now replaced once and named for every line it lacks, fixed in c355cdc9 (independent a84cc81 4)
  - resolved: a report or review that opened with an ordinary Markdown title was refused, and the only fix was deleting the reviewer's title. headerOf now reads past one leading # title, as recordFields already did, so the record is read whole; any other heading still ends the header, fixed in c355cdc9 (independent a84cc81 5)
  - resolved: the "carries no record" message denied a field that was present but empty. The message now names each line the record lacks, and says of a present-but-empty examined: that it is there and holds no entry, fixed in c355cdc9 (independent a84cc81 6)
  - resolved: the fix clause attached to a report repair was generic and, for a findings-under-a-heading repair, told the agent both to keep the reviewer's words and to reword a note. An examined: problem is now answered about examined:, and a report is asked for again rather than reworded, fixed in c355cdc9 (independent a84cc81 7)
  - resolved: every review <slug> message and the AGENTS.md brief now name the record form the gate enforces -- the four fields at the top, one entry per line, every finding in the findings: list, no section titled with the word findings -- so a replacement reviewer is not set up to fail the same way, fixed in c355cdc9 (independent a84cc81 8)
  - resolved: my own, wording: the message quoted an ATX title with its closing hashes, so "## Findings ##" was reported as the heading "Findings ##". The title capture now stops before a closing run of hashes, fixed in c355cdc9
  - resolved: a bullet inside a blockquote was invisible to the whole reader, so "> - open: ..." reached Done with nothing named. One helper now reads what a line says rather than the markup around it, and the body, the findings-naming heading and the examined: list all use it, fixed in 9ada401f (independent 35a9ca0 1)
  - resolved: beside a non-empty findings: list, a findings-naming heading carried a finding as prose or a table row, because my own twenty-seventh-round rule asked for a bullet. Such a section is now refused when it holds a list or any line that says open: or resolved:, and still accepts elaboration prose, fixed in 9ada401f (independent 35a9ca0 2)
  - resolved: a bullet that says open: at the top level of the examined: list was absorbed as an examined entry. examined: now holds no finding at any indent, fixed in 9ada401f (independent 35a9ca0 3)
  - resolved: the prefix was recognised only as the literal start of a bullet's text, so a task box, emphasis, a space before the colon and an HTML list item each hid a finding. The reader now strips quote markers, list markers, task boxes, emphasis and HTML tags, tests each cell of a table row, and allows a space before the colon, fixed in 9ada401f (independent 35a9ca0 4)
  - resolved: headerOf read past exactly one "# " title, so a deeper title, two titles, a title after prose and a setext title were each refused with a repair that could not be carried out. One leading title, hashed at any level or underlined, is now the record's own in both parsers, and the message names that fix, fixed in 9ada401f (independent 35a9ca0 5)
  - resolved: a commit: value with a parenthetical cost a whole new reviewer. When the line's first word is the review's commit it is now repaired, with the reviewer's words kept, fixed in 9ada401f (independent 35a9ca0 6)
  - resolved: a report that quotes the finding shape in an indented code block is refused, because only a fence hides an example; telling a fence from an indented block means implementing Markdown's block structure. Recorded as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020), written into .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md, and the agreement and the manual now say a fence is the only cover, changed in 9ada401f (independent 35a9ca0 7)
  - resolved: a report short of commitment: and findings: was sent round two repairs. Every line the record lacks is named in one message, fixed in 9ada401f (independent 35a9ca0 8)
  - resolved: a resolved: carry needs no account of how it was resolved, so the report's words plus the citation reach Done. Cairn checks recorded results and cannot judge whether a resolution is real, as the working agreement says of it generally; recorded as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020) and written into the next-iteration item, where a canonical record could require the account as a field (independent 35a9ca0 9)
  - resolved: uncarried findings were named one per round. Every uncarried finding is now named in one message, with its reason and its words, fixed in 9ada401f (independent 35a9ca0 10)
  - resolved: the fix clause attached to a report repair did not match the fault, and one sentence asked for the reviewer's words to be kept and changed at once. Each fault now carries its own advice, and the findings-under-a-heading message says only that a fence hides an example, fixed in 9ada401f (independent 35a9ca0 11)
  - resolved: my own, blocked: a leading title written in setext form was not read past, so a record whose only fault was an underlined title was refused. Both parsers now read past one leading title in either form, fixed in 9ada401f
  - resolved: a line that said the finding prefix in the header above the findings list was read by no sweep, so a report and a review both reached Done with the finding named nowhere. Every line outside the findings list is now read for the prefix, the header included, fixed in 7c35392b (independent 7f3032b 1)
  - resolved: the helper missed a prefix whose emphasis or HTML closed after the keyword, so "- **open**: ..." reached Done. Emphasis characters and tags are now removed from the line before the prefix is tested, fixed in 7c35392b (independent 7f3032b 2)
  - resolved: a finding written as its own heading was read by no sweep. A leading run of hashes is now markup like any other, so a heading's own text is read for the prefix, fixed in 7c35392b (independent 7f3032b 3)
  - resolved: the join that folds an indented line into the entry above it tested the strict prefix rather than the helper, so an indented "**open:**" or "open : " was swallowed into an examined entry. It now uses the helper, so examined: holds no finding at any indent in any markup, fixed in 7c35392b (independent 7f3032b 4)
  - resolved: a bullet in a subsection of a findings-naming heading reached Done beside a non-empty list, because the section's body stopped at its first subheading. A section now holds its subsections, judged down to the next heading at its own level, fixed in 7c35392b. The reviewer's second shape, a line underlined with a rule inside that section, is a sibling section at the same level in Markdown, so it falls under the prose limit the developer accepted on 2026-09-17, and I have not claimed it as fixed (independent 7f3032b 5)
  - resolved: a table under a findings-naming heading reached Done beside a non-empty list. A table row now counts as content of such a section, fixed in 7c35392b (independent 7f3032b 6)
  - resolved: an honest outcome table whose cell begins with the prefix is refused, with no repair the agent may make. Recorded as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020): keeping it is the price of not losing a finding written as a table row, which the twenty-eighth round found, and the agreement says a fence is the only cover (independent 7f3032b 7)
  - resolved: the review's commit: message blamed a line below that joined nothing, and a finding inside examined: was told to be written as an entry it already was. The commit: message now names both causes and says to keep only the commit on that line, and a finding-shaped line in a list is told to move into the findings: list, fixed in 7c35392b (independent 7f3032b 8)
  - resolved: my own, silent: a bullet whose prefix is wrapped in quotation marks or parentheses reached Done, as did a finding written as its own heading. All three are named now, fixed in 7c35392b
  - resolved: the prefix helper stripped at most one list marker and a fixed set of lead characters, so a second marker, a number in parentheses, an emoji and a prefix inside an HTML comment each walked past it. Tags, comment markers and emphasis are removed and the line is then read from its first letter, so markup is whatever precedes that letter, fixed in 1376ec71 (independent 356e94f 1)
  - resolved: a table row counted as a row only when the line began with a pipe, so a table written without outer pipes was invisible to the sweep, to what a findings-naming section holds, and inside examined:. A row is now read cell by cell wherever its pipes sit, fixed in 1376ec71 (independent 356e94f 2)
  - resolved: a field written after the findings list was refused with advice that never named the move. Such a line is now told to go above examined:, which also improves the advice on the four older reviews in this repository that carry a verification: line inside examined:, fixed in 1376ec71 (independent 356e94f 3)
  - resolved: a report committed but missing from the working tree was told only to be committed, which would have thrown it away. It is now told to be restored from HEAD, or to have its deletion committed and a new reviewer started, fixed in 1376ec71 (independent 356e94f 4)
  - resolved: a prefix line in the record's own header was refused with a message saying it sat under a heading. Both records now name it as being above the findings list and offer the move into that list, keeping its words, fixed in 1376ec71 (independent 356e94f 5)
  - resolved: my own, silent: a footnote definition reached Done. The first-letter rule covers it with everything else, fixed in 1376ec71
  - resolved: the markup stripper read a line from its first letter, so any marker containing a letter was cover: a checked task box, a lettered or roman marker and a parenthesised letter each reached Done, in the report's body, its examined: list, its header and the review's own body. Markup is now stripped a unit at a time, letters and all, fixed in 35ebf8c4 (independent 606d046 1)
  - resolved: withoutFences paired fence markers by position, so a prose line that merely began with three backticks or tildes opened a fence and blanked every finding down to the next such line. A fence now opens only when its marker stands alone on the line, or with one language word; the specification's own fences are unaffected, since none carries a multi-word info string, and its lint stays clean, fixed in 35ebf8c4 (independent 606d046 2)
  - resolved: an honest examined: entry that begins with the prefix is refused, and the repair would make a finding of it. Recorded as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020): any line beginning with the prefix is read as a finding wherever it sits, and that is the rule that stops a finding from hiding; the agreement tells a reviewer not to begin a line with it outside the findings list (independent 606d046 3)
  - resolved: every repair for a review record's findings ended with two requirement tags. The tag is appended only when the composed sentence does not already carry one, fixed in 35ebf8c4 (independent 606d046 4)
  - resolved: my own, silent: a finding written as the value of the reviewer: field reached Done. A field's value is now read the same way as the line, fixed in 35ebf8c4
  - resolved: under a heading, a bulleted finding whose words began with a label, such as a rating a reviewer was asked to give, was read as prose, because the label strip read the raw cell rather than the stripped text. It now reads the stripped text, so no rating can hide a finding, fixed in 19921633 (independent 3641128 1)
  - resolved: one unpaired fence marker blanked every line to the next marker, so a real finding between two quoted blocks disappeared, in six opener and closer shapes. The findings sweep no longer reads the fence-stripped text: it reads the record's own lines, with the exempt region measured on the record's own header, so no pairing heuristic decides whether a finding is read, fixed in 19921633 (independent 3641128 2)
  - resolved: a decorative tilde rule used twice as a section divider was a closed fence, so the section between the dividers was blanked and its finding disappeared. The same fix covers it: a divider hides nothing from the sweep, fixed in 19921633 (independent 3641128 3)
  - resolved: a fenced example the rule refused to open was read as a live finding, with a message that told the reviewer to do what it already had. The opener goes back to the plain rule, and the agreement and the manual now say plainly that a fence hides a quoted field but no finding, so the prefix is described rather than quoted outside the findings list, fixed in 19921633 (independent 3641128 4)
  - resolved: the carry and citation rules, the commit comparison, a modified, deleted or empty record, freshness and the findings-naming span were reported sound, which I record rather than change. Its one sharp edge, a carrying line with emphasis round the prefix refused as an unrecognized finding, is kept: the review's own list is read literally, and the message names the entry it cannot read (independent 3641128 5)
  - resolved: my own, blocked: a fence whose info string carried a space no longer hid its example. Overtaken by the report's second finding and closed with it in 19921633, since the sweep no longer depends on pairing at all
  - resolved: the region the sweep exempted ran from the findings: line to the record's first heading, so a fenced finding below the list reached Done and a record with no heading exempted everything after its list. The exempt region is now the list's own lines, measured by how far the list reaches, fixed in 92aa67f0 (independent 29b7f21 1)
  - resolved: the exempt span started at the first findings: line the raw text showed, so a fence quoting that field moved the start above itself. The span is now measured from the list the metadata reads, not from the first line that looks like its field, fixed in 92aa67f0 (independent 29b7f21 2)
  - resolved: the label rule stripped one label of at most forty characters, so a second label, a longer one or a bracketed tag still hid the prefix in the body, in the review and inside examined:. A label of any length is now stripped, up to four in a line, and a tag in brackets is markup like any other marker, fixed in 92aa67f0 (independent 29b7f21 3)
  - resolved: three messages still offered a fence as cover, which stopped being true in the previous round, and told a reviewer to do what the refused record had already done. They now say that no fence hides such a line and that the prefix is described rather than written at the start of one, fixed in 92aa67f0 (independent 29b7f21 4)
  - resolved: an honest examined entry that names the prefix after a label is refused, and neither list will take it. Kept as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020): reading any such line as a finding is what stops a finding from hiding, and the agreement tells a reviewer to describe the prefix instead of writing it at the start of a line (independent 29b7f21 5)
  - resolved: the carry and citation rules, the commit comparison, a modified, deleted, empty or committed-deleted record, LOOP-032 and LOOP-058 freshness, the findings-naming span and one leading title in each parser were reported sound, and the position sweep found no unread region outside the exempt span, which is the first round to say the region question is closed. Recorded rather than changed (independent 29b7f21 6)
  - resolved: my own, silent: a label longer than forty characters before the prefix hid the finding. Covered by the report's third finding and closed with it in 92aa67f0
  - resolved: a fence standing between the findings line and the last line the list consumed escaped the sweep, because the reach was measured on the fence-stripped text while the exempt span was cut from the record's own. The list's own lines are now named one by one rather than as a span, so a fence inside the list hides nothing, fixed in 601f567a (independent 1ca39d9 1)
  - resolved: a label that carried no colon of its own swallowed the prefix, because the strip ate up to the first colon, which was the prefix's own. The label rule is gone: the prefix is now read wherever it appears in a record outside the findings list, fixed in 601f567a (independent 1ca39d9 2)
  - resolved: four labels before the prefix hid it, by an off-by-one in a loop that meant to strip four. The loop is gone with the rule it served, fixed in 601f567a (independent 1ca39d9 3)
  - resolved: a heading whose title names findings was allowed to hold prose whenever the list held an entry, which hid prose findings under it. Such a heading holds nothing again, whatever the list holds, which reverses the accommodation made in the twenty-seventh round and returns its complaint to a named limit: a section titled about the findings must be titled otherwise, fixed in 601f567a (independent 1ca39d9 4)
  - resolved: honest text that carries the prefix is refused, and more of it than before: not only at the start of a line but anywhere in a record outside the findings list. That is the price of the rule that ends the class, and it is recorded as a named limit under the developer's ruling of 2026-09-18 (escalation loop-020). Three committed reviews of finished commitments no longer read for this reason, beside the six that already did not, and the compatibility promise in docs/releasing.md is raised for the developer rather than decided here (independent 1ca39d9 5)
  - resolved: a repair that left the record refused for a second reason is gone with the rules that produced it: there is no placement outside the findings list that hides a line, so no message offers one, fixed in 601f567a (independent 1ca39d9 6)
  - resolved: the advice given when the examined list held nothing but a finding-shaped bullet asked for a change the record already carried. Such a line is now told to move into the findings list, which is the only place the prefix belongs, fixed in 601f567a (independent 1ca39d9 7)
  - resolved: my own, silent: five labels stacked before the prefix hid the finding, where the loop capped at four. Closed with the label rule itself in 601f567a
  - resolved: the prefix was recognised only with a space or a tab before its colon, so the word in brackets, in quotation marks, in parentheses, with an entity for the colon, with a no-break or zero-width space, or inside angle brackets each hid a finding. Up to four characters that are neither letter nor digit may now stand between the word and its colon, entities are decoded, and a line is read both with its tags stripped and with only its angle brackets removed, fixed in f0b5bf54 (independent 5a198ef 1)
  - resolved: a fence inside the findings list hid ordinary entries from the list and from the sweep at once, so a report's second and third findings disappeared and only the first was carried. A fence inside the list is now refused outright, fixed in f0b5bf54 (independent 5a198ef 2)
  - resolved: a body section that wrote the findings field a second time was read as nothing whenever the header's list held entries or declared none. It is refused now, read from the fence-stripped text so a quoted example stays out of it as LOOP-071 asks, and the helper that was meant to catch it and never ran is gone, fixed in f0b5bf54 (independent 5a198ef 3)
  - resolved: a leading title with no space after its hash was a title to one parser and prose to the other, so an honest record was refused for fields that were there. Both parsers now read a heading the same way, fixed in f0b5bf54 (independent 5a198ef 4)
  - resolved: a prefixed line outside the list named repair of the report while forbidding the only repair it left. Such a report is named for a new report, as the heading-title case already was, fixed in d92a8248. I recorded this as fixed in f0b5bf54 before it was, and corrected the entry when I checked the verdict rather than my own commit message (independent 5a198ef 5)
  - resolved: the prefix test excluded digits from the gap between the word and its colon, my own narrowing, so a reviewer numbering its findings lost them in the report's body, in its examined list and in the review's own body. A digit there is punctuation like any other, while the gap stays four characters wide, fixed in d469ae1b (independent d68b89d 1)
  - resolved: honest text is refused when an angle-bracketed placeholder sits immediately before a colon, because the second reading removes only the angle brackets. Kept as a named limit under the developer's ruling of 2026-09-18: the second reading is what catches a bare word in angle brackets, which is a shape a reviewer can use to hide a finding, and the agreement tells a reviewer to describe the prefix rather than write it near one (independent d68b89d 2)
  - resolved: a prefixed line below the findings list was still answered with a repair whose own advice said to ask the reviewer instead. Every such report is written again now, wherever the line sits, fixed in d469ae1b (independent d68b89d 3)
  - resolved: the test that a report names the review's commit outside its commit line read fenced text too, so a pasted command block satisfied it. It now reads the reviewer's own words, fixed in d469ae1b (independent d68b89d 4)
  - resolved: a carry accepted a review line that merely began with the finding's words, so a longer word could satisfy it. The comparison now requires a word boundary, fixed in d469ae1b (independent d68b89d 5)
  - resolved: my own, silent: a fence inside the examined list blanked its entries as it did in the findings list, truncating a reviewer's stated scope. Refused now by the same flag the list reader already computed, fixed in d469ae1b
  - resolved: the prefix test lost the word when an HTML comment, or a character reference holding letters, stood between it and the colon, because the comment markers were blanked before the tags were stripped. A line is now read three ways, with comments removed whole, with tags stripped, and with only its angle brackets removed, and every character reference becomes a space first, fixed in d137f971 (independent 0735b11 1)
  - resolved: a findings field carrying one leading space joined the examined list, so the reviewer's findings became things examined, and the gate's own advice to add the empty-list form then reached Done with the defect filed as scope. An indented field is named for what it is now, and never answered with that form, fixed in d137f971 (independent 0735b11 2)
  - resolved: a bullet written above the lists belonged to none and joined the field above it, so it disappeared. It is named now, fixed in d137f971 (independent 0735b11 3)
  - resolved: a fence inside the findings list was reported as a fence inside the examined list, because the examined test spanned to the end of the header and ran first. Each list's test now ends where the next field begins, fixed in d137f971 (independent 0735b11 4)
  - resolved: a prefixed line inside examined: or nested under a finding was still answered with a repair the gate forbids, because those tests ran before the one that asks for a new report. The prefix is judged first now, so one answer covers every position, fixed in d137f971 (independent 0735b11 5)
  - resolved: two values were computed on every list read and never used, one of them the last trace of a rule the gate no longer applies. Both are gone, and removing them paid for this round's new lines, fixed in d137f971 (independent 0735b11 6)
  - resolved: the carry's word boundary uses the ASCII word class, so a review line may continue the finding's last word with a letter outside ASCII. Recorded as a named limit under the developer's ruling of 2026-09-18: the reviewer rated it narrow, the words stay on the page, and PKG-008 keeps shipped text ASCII (independent 0735b11 7)
  - resolved: the line test lost the word when an HTML element stood round it and other markup stood in the gap, because each reading covered one but not both. A line is read five ways now, so no combination of markups hides the prefix, fixed in c133f0a7 (independent 962a52f 1)
  - resolved: a prefixed line inside the findings list was told to move into the list it already sits in. Each list now gives the advice that fits it: inside findings, take its own entry at the list's indent; inside examined, move into the findings list, fixed in c133f0a7 (independent 962a52f 2)
  - resolved: a report with no findings field was told it could declare itself empty although it plainly named defects. When the reader can see the prefix in the record, the empty-list form is no longer offered and the reviewer is asked for a report that lists them, fixed in c133f0a7 (independent 962a52f 3)
  - resolved: my own, silent: a tag pair with content between the word and the colon hid the finding, the mirror of the report's first finding. Both are closed by the five readings, fixed in c133f0a7
  - resolved: A report that says in words that it found nothing is counted as a finding and must be carried. At 3833b02a a report whose findings list holds the single entry "none", or "None.", or "I found no defects at 3833b02a", is answered with the message that finding 1 is not carried, followed by the instruction to carry it in the prefixed form with the citation last. I followed that instruction literally and reached Done, leaving a review entry that records as a finding something the reviewer never found. The controls: the declared-empty form reaches Done, and the same word written as the key line's value is refused as not a list. (wording), and how: Recorded as a named limit under the developer's ruling on escalation loop-020, which fixes only a silent loss inside this commitment, and carried into the waiting next-iteration item reviews-and-reports-are-written-in-a-form-the-loop-reads, where a canonical record removes the guess this message is making. (independent 3833b02 1)
  - resolved: The guard that stops the gate telling an agent to declare a report empty recognises only the finding prefix. At 3833b02a a report carrying the other three fields, with its defect written as a plain bullet under a heading titled "Results", is answered with a repair whose words are to add the declared-empty form when the reviewer found none; I followed that literally and reached Done with the defect unnamed. The control is the same report with the prefix on that bullet, which is answered instead by asking the reviewer for a report whose findings list holds the defects it names. The reading is the limit accepted on 2026-09-18; the instruction to declare the report empty is not. (wording), and how: Recorded as a named limit under the same ruling and carried into that same next-iteration item; the reading is the limit accepted on 2026-09-18, and the instruction to declare the report empty is the part that would change. (independent 3833b02 2)
  - resolved: A report whose examined field is present but holds no entry is answered with a repair on the report telling the agent to write each entry under it and change none of the reviewer's words, which asks the agent to author the reviewer's own observations. The control is the same report with its commit line also absent, where the gate says instead that the record carries no commit line and that examined is there and holds no entry, and asks for a new reviewer. The kernel's own comment draws that line, that a field only the reviewer can write is a new report, and the empty examined list falls on the wrong side of it. (wording), and how: Recorded as a named limit under the same ruling and carried into that same next-iteration item, beside the kernel's own line that a field only the reviewer can write is a new report. (independent 3833b02 3)
  - resolved: The kernel's message for an indented field is unreachable when the indented field is the examined one, so an indented header is never told that it is indented. At 3833b02a a report whose examined field and its entries are indented by two spaces is answered that its commit line carries more than the commit, and an agent that followed that literally would delete the reviewer's entries; a report whose whole header is indented is answered that it carries no commitment, commit, examined or findings line, and asked for a new reviewer, although removing the indent changes no word of the reviewer's. The control is the same record with only the findings field indented, which is answered precisely, that the field is indented and each field belongs at the margin. (wording), and how: Recorded as a named limit under the same ruling and carried into that same next-iteration item. (independent 3833b02 4)
  - resolved: bin/cairn.mjs line 909 declares a second constant beside ENTRY that nothing in the kernel or the tests ever reads. It is dead code in the record reader while PKG-004 caps the files under bin/ at 1900 lines and they stand at 1872, and the review record says the ceiling is now a real constraint on this class of work. (wording), and how: Reproduced at bin/cairn.mjs line 909, where the name appears nowhere else in bin/, tests/ or scripts/, and recorded as a named limit in the waiting next-iteration item reviews-and-reports-are-written-in-a-form-the-loop-reads, where the canonical record rewrites this reader and removes the constant with it. It is not a commitment of its own, since no requirement can be drafted for deleting an unused declaration, and the kernel is not changed now because the release round's review is recorded at this commit and a change would make it stale and cost a further report. (independent 3833b02 5)
  - resolved: The working agreement promises an acceptance the gate does not give. AGENTS.md lines 135 to 138, and the shipped copy at skills/new-project/templates/AGENTS.md lines 135 to 138, say that when the findings field holds entries a heading whose title names findings "is read as elaboration and may hold prose", and bin/cairn.mjs line 982 carries the same sentence as a comment, contradicted by the comment on the next line. At a48ccce4 a report whose findings list holds one carried entry and whose body has a section titled "Findings I could not place" holding only prose is refused, and so is the same report titled "How I reached the findings above"; the control, the identical report titled "What else I saw", reaches Done. The same asymmetry holds for the review record: a section titled "The findings in context" is refused where "Notes" is accepted. The 0.7.0 changelog states the real rule, that such a heading is refused even when every finding is listed, so it is the agreement a reviewer is briefed from that is wrong, and a reviewer who follows it costs a whole round. (wording), and how: Recorded as a named limit under the developer's ruling on escalation loop-020, and captured for the next specification phase as the-working-agreement-promises-a-heading-the-gate-refuses. The agreement's sentence and the gate disagree and both are contract text, so which one changes is the developer's. (independent a48ccce 1)
  - resolved: Two records committed at a48ccce4 state a kernel size that is wrong by 28 lines and use it as the reason to defer work. .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md line 146 says the files under bin/ "stand at 1872 now" and line 156 says PKG-004 is "not violated at 1872 of 1900". At a48ccce4 wc -l over the five tracked files under bin/ gives 1900 and pkg-lint prints 1900, so the headroom named there does not exist; the sibling item .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md says in the same directory that the kernel "sits at exactly 1900". The conclusion still holds, since 1900 does not exceed 1900, but the margin a later reader would plan against does not. (wording), and how: Fixed. Both lines were mine, and I reproduced the count before changing them: wc -l over the tracked files under bin/ gives 1900 and pkg-lint prints 1900. The item now says the files stand at 1900, the ceiling itself, with no headroom left, and says where the wrong number came from. The reviewer's own words above keep 1872 because a report is never edited. (independent a48ccce 2)
  - resolved: The leading bullet of the CHANGELOG 0.7.0 entry describes behaviour that did not change in 0.7.0. It says the stop hook no longer traps a session, judges with the kernel that wrote the latest evidence, opens after three refusals with no progress, writes a record under .cairn/stops/ and is named by the next wake. git diff v0.6.0 v0.7.0 -- bin/hooks/ and -- bin/hook.mjs are both empty, and stopVerdict in bin/cairn.mjs is unchanged between the two tags, while the 0.6.0 entry already describes all five behaviours under PKG-018, PKG-033, PKG-021, PKG-044, PKG-043 and LOOP-139. The same text is the v0.7.0 tag message, so it is what a reader upgrading from 0.6.0 sees first. (wording), and how: Fixed in edb4460a. The developer ruled on escalation pkg-039 that the entry should not carry the bullet at all, so it was removed rather than reworded, and the lead sentence was rewritten to match. The v0.7.0 tag that carried the superseded text as its message was deleted. (independent a48ccce 3)
  - resolved: PKG-045 and LOOP-139 both claim the slot ahead of every action but a live check, and the kernel can honour only one, so PKG-045's falsifier is met. At a48ccce4, with .cairn/policy forbidding attribution, one probe commit that both adds an unexplained record under .cairn/stops/ and carries a Co-Authored-By Claude trailer makes wake name "explain .cairn/stops/<name>.md", which PKG-045's falsifier calls another action. The controls: the same commit without the stop record names reword, and the same stop record with a clean message names explain. The attribution is still caught one action later, so nothing escapes, but one of the two Agreed texts cannot be satisfied as written. (wording), and how: Recorded as a named limit under the same ruling and captured for the next specification phase as pkg-045-and-loop-139-claim-the-same-slot-and-one-cannot-be-satisfied. No change inside this commitment can repair two Agreed texts that claim one slot. (independent a48ccce 4)
  - resolved: An honest report with a stray bullet on the line below its commit field is sent back for a whole new reviewer, where the same bullet one line lower is repaired in place. At a48ccce4 the field then reads "a48ccce4,a stray bullet" and the gate answers that the report "names a48ccce4,a stray bullet, which is not a commit" and asks for a new reviewer, although bin/cairn.mjs carries a repair for a commit line that "carries more than the commit"; that repair is missed because the joined value has no space after the identifier, so its first whitespace-separated word is not a commit either. The control is the same bullet written below a reviewer field and above the examined field, which is answered with a repair that keeps every word the reviewer wrote. (blocked), and how: Recorded as a named limit under the same ruling and written into the next-iteration item reviews-and-reports-are-written-in-a-form-the-loop-reads, where it is now the sharpest argument for the canonical record: an honest report is discarded over a missing space, and a record the loop writes cannot produce the shape at all. (independent a48ccce 5)
  - resolved: .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md line 161 says PKG-004 is "not violated at 1900 of 1900, though there is no headroom left", and gives that as the reason to defer deleting the dead constant at bin/cairn.mjs line 909. At 82633728 the ceiling is 2000 and the kernel is 1900, so there are 100 lines of headroom; line 150 of the same file says the developer raised the ceiling to 2000, which "buys 100 lines and no more". The file contradicts itself eleven lines apart, and the stale figure is the stated reason for not doing work. This is the same class of defect the fortieth report raised as its second entry, reintroduced by the edit that answered it. (wording), and how: Fixed in 83782e65. The paragraph now takes its number from pkg-lint and says so; the stale headroom sentence and the deferral it justified are gone, because the two dead declarations it deferred were deleted in the same commit. (independent 8263372 1)
  - resolved: The paragraph at lines 145 to 151 of the same file is a botched edit that leaves an unfinished sentence: "stand at 1900 now, the ceiling itself, because what giving the / prefix to one list saved has since been spent / less code than the rules it replaced." The tail "less code than the rules it replaced" is the end of the sentence that was replaced and was never removed, so the clause has two verbs and no object; line 148 also runs to 110 characters where the rest of the file wraps near 72, which is the mark of the splice. "Stand at 1900 now, the ceiling itself" is false at this commit as well, since the ceiling is 2000. (wording), and how: Fixed in 83782e65. The spliced paragraph is rewritten whole, wrapped like the rest of the file, and it records that it has now been wrong twice and why, so a later reader knows not to trust a number quoted from a report. (independent 8263372 2)
  - resolved: The rationale added to PKG-004 (docs/spec/package.md line 49), the decision record (docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11) and the CHANGELOG (lines 16 to 17) all say the last twelve review rounds each paid for their fix by trimming comment, and the repository's own records say three or four: .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md names "Rounds 31, 32, 33 and 34" and the sibling item names "Rounds 31, 32 and 33". The kernel history agrees with the records and not with the rationale: `git diff 3833b02a 82633728 -- bin/` is empty, so rounds 39 and 40 changed no kernel file at all and paid for nothing, and the round 27 fix c355cdc9 took the kernel from 1860 to 1872, adding twelve lines with 28 to spare under the old ceiling. An inflated count is a weak argument for the requirement that rests on it. (wording), and how: Fixed in 83782e65. All three records now say rounds 31 to 34, which is what the repository's own items say; the inflated count is gone from the requirement's rationale, the decision record and the changelog. (independent 8263372 3)
  - resolved: Three committed records quote the same direction from the developer and two of them disagree with the third, so at most one is verbatim: docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11 and .cairn/next-iteration/the-kernel-ceiling-is-reached-and-record-reading-is-what-fills-it.md line 10 both quote "set the package cveiling to 2000", while docs/spec/package.md line 47 quotes "set the package ceiling to 2000". In a project whose current commitment is that a reviewer's words are never edited, a quotation of the developer that differs between the specification and the decision record it rests on is worth settling one way or the other. (wording), and how: Fixed in 83782e65. The specification no longer quotes the developer at all; it says the direction was to raise the ceiling and points at the decision record, which carries their words verbatim, as does the next-iteration item. (independent 8263372 4)
  - resolved: tests/pkg-lint.test.mjs observes the PKG-004 threshold but not what the lint counts, so two real enforcement errors pass the whole suite. Replacing the filter on line 60 of scripts/pkg-lint.mjs with one that skips nested paths makes the lint print "kernel 1872 lines, clean", silently dropping bin/hooks/session-start.mjs and bin/hooks/stop.mjs, which PKG-038 makes Cairn execute at run time; `node --test tests/*.test.mjs` then reports 537 tests, 537 pass, 0 fail. Removing the unterminated-last-line term from the same line does the same: 537 pass, 0 fail. No PKG-004 fixture ever places a file under a subdirectory of bin/, and none ever omits a final newline. The threshold mutations are all caught, so the gap is exactly the file set and the counting rule. (wording), and how: Fixed in 83782e65. Two fixtures were added, one with a file under bin/hooks/ and one whose last line is unterminated, and I reproduced the reviewer's method to prove they bite: with the lint mutated to skip nested paths the suite fails 1 of 19, and with the unterminated-last-line term removed it fails 1 of 19. Before the fixtures both mutations passed. (independent 8263372 5)
  - resolved: CHANGELOG.md line 49 cites "escalation loop-020" for the limit that an honest line carrying the finding prefix outside the list is refused, and that limit is recorded and answered in .cairn/escalations/loop-020-2.md, whose question is "The rule that ends the hiding places also refuses three committed reviews that used to read" and whose reasoning states exactly that price. .cairn/escalations/loop-020.md asks a different question, whether to keep fixing every finding or record the rest as limits, and nowhere mentions that refusal. The preceding clause on line 46 cites loop-020-loop-086 correctly and by the same pattern, so a reader follows this one to the wrong record. (wording), and how: Fixed in 83782e65. The changelog now cites escalation loop-020-2, which is the record that asks and answers that question. (independent 8263372 6)
  - open: The 0.7.0 release cannot be re-cut by the shipped path, and no commit carries both the version and the corrected entry. `node scripts/release.mjs 0.7.0` in a throwaway copy of the checkout refuses with "release: 0.7.0 is not an increase over 0.7.0" and exit 3, because line 26 compares against package.json, which already reads 0.7.0. The only commit that sets the version is 96f60acb, and its CHANGELOG still opens with the stop-hook bullet that was removed at edb4460a, so a tag placed there would carry the superseded entry as its message, while a tag placed on a later commit sits on a commit that sets no version. docs/releasing.md step 3 describes the script as the way a release is cut, and PKG-040 describes a release as one commit that sets the version and carries that version's changelog entry; neither is reachable for 0.7.0 from this state without a further version. (wording), and how: Open. Reproduced, and it is the release's own shape rather than a defect in the code: no single commit both sets the version and carries the corrected entry, because the entry was corrected after the release commit was made. The developer is choosing between a tag on the commit that sets the version and a tag on the commit whose changelog is right; nothing is pushed, so neither choice has reached anyone. (independent 8263372 7)
  - resolved: bin/cairn.mjs line 945 declares a local in listOf that is assigned on lines 947, 957 and 959 and never read anywhere in the function or the file. It is dead code in the record reader, beside the unused constant on line 909 that the thirty-ninth report found and that the waiting next-iteration item already files for deletion; this one is not filed anywhere. The ceiling was raised on the argument that the reader needs room to be written plainly, so the reader's dead declarations are the first thing that room should buy. (wording), and how: Fixed in 83782e65. The local is deleted, and so is the constant the thirty-ninth report found, since the raised ceiling removed the reason to defer them; the next-iteration item that filed them now records that they are gone. (independent 8263372 8)
  - resolved: The corrected round count is still wrong, in all three records it was corrected in. docs/spec/package.md line 49, docs/decisions/the-kernel-ceiling-rises-to-2000-lines.md line 11 and CHANGELOG.md lines 16 to 17 now say rounds 31 to 34 each paid for their fix by trimming comment. Summing the tracked files under bin/ at each round's fix commit shows the kernel reached exactly 1900 at round 30's fix 1376ec71, which took it from 1899 to 1900, and stayed at 1900 until the ceiling rose at edb4460a, so rounds 31 to 38 each had to remove as much as they added: 35ebf8c4 added 10 and deleted 10, 19921633 11 and 11, 92aa67f0 12 and 12, 601f567a 10 and 14, f0b5bf54 16 and 12, d469ae1b 19 and 19 including a twelve-line comment block, d137f971 14 and 14, c133f0a7 14 and 14. The number was taken from the two next-iteration items, which disagree with each other, one naming rounds 31, 32 and 33 and the other 31, 32, 33 and 34, and neither agrees with the kernel history, so an inflated count was replaced by an understated one in the requirement's rationale, the decision record it rests on and the release notes. (wording), and how: Fixed in the commit this review names. I measured it myself rather than take a third number on trust: summing the tracked files under bin/ at 1376ec71, 35ebf8c4, 19921633, 92aa67f0, 601f567a, f0b5bf54, d469ae1b, d137f971, c133f0a7 and edb4460a gives 1900 at every one but 601f567a, which is 1896. All three records now drop the round range and state that measured fact, that the kernel had stood at the whole of the ceiling since the thirtieth round, so every fix after it had to remove as much as it added. A range that has been wrong twice is replaced by a statement with no count to get wrong. (independent 20029ff 1)
  - resolved: The paragraph rewritten in .cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md contradicts the history in both halves of its first sentence. Lines 145 and 146 say the files under bin/ reached 1900 in round 33 because giving the prefix to one list cost less code than the rules it replaced. bin/ reached 1900 at round 30's fix 1376ec71 and already stood at 1900 before round 33's fix 92aa67f0, which went from 1900 to 1900; and the change that gave the prefix to one list is round 34's fix 601f567a, which came after round 33 and took bin/ from 1900 down to 1896. The stated cause postdates the stated effect by one round, and the stated date is three rounds late. This is the same paragraph the forty-first report sent back as a splice, rewritten without the number being checked against the repository. (wording), and how: Fixed in the commit this review names. The paragraph now says the kernel reached 1900 at the thirtieth round's fix 1376ec71, names round 34's 601f567a as the one change that bought room and took bin/ to 1896, and records that this paragraph has been wrong three times, each time because a number was copied from a report or another record instead of measured. (independent 20029ff 2)
  - resolved: The edit to PKG-004's rationale left its paragraph unwrapped. docs/spec/package.md line 51 runs to 102 characters where lines 47, 48, 49, 50 and 52 of the same paragraph run 68, 68, 72, 66 and 66, because the replacement text was spliced in without rewrapping the line it joined. An over-long line in a paragraph that otherwise wraps near 72 is exactly the mark the forty-first report used to identify the botched paragraph it sent back, so the commit that fixed that splice left this one in the specification. (wording), and how: Fixed in the commit this review names. The paragraph is rewritten and wraps at 66 to 71 characters throughout. The remaining long line in that file, the 141-character line of the 2026-09-17 revision, is not from this work: it is the same length at 82633728 and predates this commitment. (independent 20029ff 3)
  - resolved: tests/pkg-lint.test.mjs still does not observe the file set the lint reads for PKG-003, PKG-009 and PKG-012, only the set it counts for PKG-004. Narrowing the filter on line 20 of scripts/pkg-lint.mjs to direct children of bin/, so that bin/hooks/session-start.mjs and bin/hooks/stop.mjs stop being scanned for commands, test imports and network or model calls, leaves node --test tests/*.test.mjs at 537 tests, 537 pass, 0 fail and the lint still printing kernel 1900 lines, clean; tests/pkg-lint.test.mjs is the only test of that script, and it stays 19 of 19. The shipped lint does catch the case, since a scratch repository with a fetch call in bin/hooks/stop.mjs is reported as PKG-012 and exits 1, so only the test is blind. This is the same shape as the forty-first report's fifth entry, which was closed for the line count and left standing for the three text checks that read the same directory. (wording), and how: Fixed in the commit this review names. A fixture with a network call in bin/hooks/stop.mjs was added to the PKG-012 test, and I reproduced the reviewer's method to prove it bites: with the scanned set on line 20 narrowed to direct children of bin/, tests/pkg-lint.test.mjs fails 1 of 19, where before the fixture it passed 19 of 19. The printed line count in the clean summary is still unobserved, and that is recorded rather than fixed, since it is a message and not a check. (independent 20029ff 4)

## Review at b2a1ea27, 2026-09-17

The builder's review of this commitment would have found none of these:
my tests exercised the reports I expected to write. The independent
reviewer went straight for the parsers, the commit value and what an
agent copying the format would type, which is what the requirement is
for. All thirteen are resolved as one action after this record.

## Review at 92d5e828, 2026-09-17

The second reviewer checked my resolutions instead of trusting them and
found that one of them reopened the door the requirement exists to
close. That is the case for the independent report in one example.

## Review at ef70db45, 2026-09-17

Third round. Each fix loosened or tightened a rule, and each change opened
an edge the next reviewer found. The bypass that matters, editing one
line of an old report, survived two rounds because I fixed what the
message said and not what the gate checked.

## Review at c7c18041, 2026-09-17

Fourth round. What is left is the cost of guessing: a check that an old
report was only re-dated, and a rule for where a carried sentence ends.
Both are replaced with rules that do not guess: the report names the
commit it examined, and each finding is carried by its number.

## Review at f4f4e27d, 2026-09-17

Fifth round. A citation by number alone is unambiguous within one report
and ambiguous across rounds, which a single review holds. The citation
now names the report's commit as well as the finding's number.

## Review at e69a3c83, 2026-09-17

Sixth round: the fifth round's six resolutions hold, and two edges remain,
a finding that quotes a citation and a wrapped line where nothing needs a
citation.

## Review at 1313f32b, 2026-09-17

Seventh round: the sixth round's resolutions hold for the documented
forms; two refinements to where a citation may sit.

## Review at 03961bb1, 2026-09-17

Eighth round: one defect, the report's own uncommitted changes.

## Review at 538ba9a0, 2026-09-17

Ninth round. The parser's rule, that a blank line ends a field, meets a
Markdown habit, a loose list, and the loop reads half a findings list
without saying so. The review side of it is older than this commitment
and lets an open finding reach Done.

## Review at 13c512de, 2026-09-17

Tenth round. Two rounds of detecting broken lists found two shapes and
missed three. The cause is that the findings list is read through a flat
field reader that ends at a blank line; the fix is to read the list from
the record's text.

## Review at a9cd734c, 2026-09-17

Eleventh round. The reader had to do both jobs: read the shapes an honest
reviewer writes, and refuse the ones it cannot read rather than read them
as nothing.

## Review at 83dec7ed, 2026-09-17

Twelfth round. Reading loosely and naming little is the same silent drop
by another route: a line the reader cannot read must be named whenever
anything follows it, and a report has no prefix to recognise.

## Review at 52010049, 2026-09-17

Thirteenth round. A trailing line that lost its dash and a trailing note
are the same shape, so treating one as prose loses the other. The list
ends at the next field, a heading, or the record's end, and every line
inside it is read or named.

## Review at b0f53f96, 2026-09-17

Fourteenth round. "The next field" was any word with a colon, which is
also what a dropped finding or a reviewer's note looks like. A list ends
only at the fields these records use.

## Review at 6ef294ba, 2026-09-17

Fifteenth round. Three left, each a case the narrowed rule did not
reach: a repair that hides what it was meant to expose, a field name
repeated, and a heading the report side never checked.

## Review at ff0e9e56, 2026-09-17

Sixteenth round. Guessing which Markdown is a finding trades one hole
for another: this round's heading rule refused honest records. The
boundary is made plain instead, and the ambiguity a report's prose
bullet carries is stated as a limit, with the format change that would
end it captured for the developer.

## Review at 896cea21, 2026-09-17

Seventeenth round. Simplifying the heading rule dropped the guard that
kept a declared empty findings list valid, and this repository's own
house style is the proof.

All five are one regression and one fix. Restoring the guard makes the
reader read this repository's own reviews again; the limit that a report's
real findings under a heading are read as prose stands, and its structural
remedy waits in next-iteration.

## Review at 69ec524c, 2026-09-17

Eighteenth round. The seventeenth round's regression is gone in all five
shapes. What is left is the reader's own edges: a capital field name
inside a list ends it silently, one repair names a fix that does not
fix the record, one agreement sentence describes a shape the kernel
refuses, and a citation number can point at nothing.

All five are resolved in 0b44d887, each with a test that fails on the kernel
before it: the two capital-field losses, the misordered record's repair,
the agreement sentence, and the citation that pointed at nothing.

## Review at f6bdf509, 2026-09-17

Nineteenth round. Two of the six are mine from the round before: a
citation of zero slips through a falsy test, and the reorder advice is
given where the fields are already in order. The other four are older
edges of the same reader: a findings field with no space, a setext
heading, a blank line before a continuation, and a doubled commit: line
that defeats the check for a report written for another review.

All six are resolved in 0c0fd5f1, each with a test that fails on the kernel
before it. The suite is 517 tests, all passing, and the six legacy
reviews the reader refuses are refused for the reasons they carried
before this commitment.

## Review at 09abf7f7, 2026-09-17

Twentieth round. Both independent findings are mine from the round
before: the underline rule reads a field's own line as a heading, and
the carry rule counts an earlier round's citation as a second carry.
With them, the manual's advice for a review that found nothing.

All four are resolved in 1fa3d06b, each with a test that fails on the kernel
before it. The suite is 518 tests, all passing.

## Review at 62471010, 2026-09-17

Twenty-first round. The reader edges are nearly out, and what the round
found instead is the other half of the work: a report the loop cannot
read is answered by asking for a new reviewer, where the review in the
same state is told how to repair it, and no shipped document yet says
that Done needs an independent report at all.

All six are resolved in 704ae301, each with a test that fails on the kernel
before it. The suite is 519 tests, all passing, and both lints are clean.

## Review at d88e131a, 2026-09-17

Twenty-second round. The silent one is the whole reason this gate
exists: a report's findings carry no prefix, so a section titled
findings could hold real ones that the loop read as prose. That shape is
refused now, in both records, and the agreement says so.

One process note, recorded because it is true: I fixed these six before
writing this record, where the agreement asks for the record first and
the fix as its own work. The fix is commit d7ec978c, the findings below
name it, and nothing was resolved that this report did not raise.

All six are resolved in d7ec978c, each with a test that fails on the kernel
before it. The suite is 520 tests, all passing, and both lints are clean.

## Review at 362eb5a7, 2026-09-17

Twenty-third round. Five of the seven are one cause: the reader decides
where a record's header ends by guessing which lines are headings and
which are fields, and every guess refuses an honest shape somewhere. I
am replacing the guessing with one rule rather than patching a sixth
heuristic. The silent one cannot be closed by any rule over prose; it is
the developer's to decide, and I raise it as an escalation.

The seventh finding is resolved as a limit, not as code: escalation
loop-020-loop-086, answered ok on 2026-09-17, and stated in the
agreement and the manual at 396eb722. The other six are fixed in 6e97a863.
The suite is 521 tests, all passing, and both lints are clean.

## Review at 3ba15a82, 2026-09-17

Twenty-fourth round. Two silent losses, which is why this round was
worth running: a findings-titled heading written with an underline was
not covered by the rule that catches the hashed form, and an
unterminated fence blanked every line below it, so a real open finding
vanished. Both are fixed as their own work after this record.

All seven are resolved in 32102569, each with a test that fails on the kernel
before it. The suite is 522 tests, all passing, and both lints are clean.
One seeded freshness test failed once under a load average near 13 and
passes deterministically on its own; that is the spawn flake LOOP-137
names, not a defect in this work.

## Review at 8efe6c02, 2026-09-17

Twenty-fifth round. One silent loss again: the rule that a heading
naming findings holds no list looked only for bullets, so a table row or
a paragraph under such a heading passed. Two of the four blocked
findings pull in opposite directions on the same shape, a prose section
under a findings-naming heading, and I resolve them the safe way, by
looking at the section under each heading instead of pairing every
bullet with every title.

All six are resolved in 70292c06, each with a test that fails on the kernel
or the lint before it. The suite is 524 tests, all passing, and both
lints are clean.

## Review at 74848eb4, 2026-09-17

The twenty-sixth reviewer rebuilt each of the twenty-fifth round's six
fixes as a concrete record and confirmed all six, then read every one of
the 68 committed reviews through the reader: 60 accepted, 8 refused, and
the eighth is new. Its four findings split the section rule that shipped
in 70292c06 down the middle. One is silent: the rule scans only the lines
before a heading's first subheading, so findings written under `### 1.`
below a `## Findings` heading are invisible again, and a record the
previous kernel refused now reaches Done. Two are blocked: a heading that
merely mentions findings in its title, such as "How I reached the findings
above", is refused although every finding is in the list, and this
repository's own committed review freshness-and-scope-guidance-agree.md is
among the records that refusal now catches; and an empty report is told to
add a `commitment:` line, after which the next wake discards it. One is
wording: the agreement and the manual still describe the narrower rule the
kernel had before 70292c06.

The pattern of this round is the pattern of the last: the fix for a silent
loss took ground in one direction and gave it back in another, because the
judgement is over prose that carries no marks. That argument is already
captured for the developer in
.cairn/next-iteration/reviews-and-reports-are-written-in-a-form-the-loop-reads.md.

I found nothing of my own this round. My probes at 74848eb4 -- a singular
"Finding" title, an all-caps "FINDINGS" title, a findings heading inside a
fence, bullets after a rule with no heading, and an empty findings-titled
section -- all behaved as the rule intends.

## Review at a84cc814, 2026-09-17

The twenty-seventh reviewer built twenty-four record pairs and found eight
defects. One is silent, and it is older than this commitment's recent
rounds: `below` in `listOf` collects the body's bullets with one regular
expression whose `(\S[\s\S]*)$` swallows the rest of the document into a
single match, so only the first bullet in the body was ever tested for the
prefix a finding carries. A recognisable finding written behind any earlier
bullet reached Done with nothing named, in a report and in the agent's own
review alike. The agreement promises the loop refuses what it can
recognise, so this is a broken promise rather than the accepted prose
limit.

Four are blocked, and three of those are the same fault in different
clothes: an answer the record cannot satisfy. A complete report is
discarded for the title of an explanatory section although every finding
was read from its list; a repair that moves findings out of a
findings-titled heading leaves the empty heading, which the next wake
refuses; a report short of two fields is told to add one and then thrown
away; and a report that opens with a Markdown title is told to keep the
reviewer's words while the only fix is deleting the reviewer's title.

The fourth blocked finding decides the question the twenty-sixth round
left open. I refused a findings-naming heading for its title alone,
choosing a visible refusal over a silent loss. The reviewer shows the
choice was too broad in one direction and unnecessary in the other: when
the record's own `findings:` list holds entries, a section titled about
the findings is elaboration, and refusing it buys nothing; when the list
declares none, such a section is the silent-loss shape and must be
refused. That is the rule I will write.

Three are wording, and one of them is mine: an ATX title keeps its closing
hashes when the message quotes it.

## Review at 35a9ca09, 2026-09-18

The developer ruled on 2026-09-18, answering escalation loop-020, that only
a silent loss is fixed from this round: a case where a real finding
disappears from the gate and nothing is named. A blocked or wording finding
is recorded as a named limit instead, so 0.7.0 can ship. The twenty-eighth
reviewer was briefed to that ruling and found four silent losses, three
blocked cases and four wording faults, in thirty-eight built records.

The four silent losses are one fault seen four ways: the reader recognises
a finding only in the exact shape it expects. A blockquote marker, a task
checkbox, bold around the prefix, a space before the colon, an HTML list
item, a table cell and a bare paragraph under a findings-naming heading all
hide a line that plainly says the prefix. The second of them is mine: the
twenty-seventh round's rule, which judges a findings-naming heading against
the record's own list, switched the protection off exactly when the list
holds entries. Each is fixed by recognising the shape rather than the
markup around it.

The three blocked cases and four wording faults are recorded as limits
under the developer's ruling, except where a message says something false,
which is fixed with the same commit at no cost in rounds: a leading title
of any level or form is read past and the message names the fix, a
`commit:` value with a parenthetical is repaired rather than discarded, and
every uncarried finding and every missing field is named at once instead of
one per wake. The remaining limits are written into the item in
next-iteration: an indented code block that quotes the finding shape is
read as a live finding, and a resolved line needs no account of how it
was resolved, which is discipline rather than a boundary, as the agreement
says of Cairn generally.

## Review at 7f3032b2, 2026-09-18

The twenty-ninth reviewer found six silent losses, and they say something
the previous rounds only implied: the reader sweeps the body and the two
lists, and every region it does not sweep is a place a finding can sit.
The worst is the header above the examined: line, which no sweep covers at
all, so a line naming a defect there is read as an unknown field and
dropped. A finding written as its own heading is invisible for the same
reason: a heading is neither a bullet nor a swept line. A prefix bolded as
a word rather than with the colon inside the emphasis, an indented
continuation joined into an examined entry, a bullet in a subsection of a
findings-naming section, and a table under that section are four more of
the same shape: the rule was written for the case in front of it, not for
the region.

The answer is to sweep the record rather than its parts: every line
outside the findings list is read for the prefix, however it is marked up,
and a findings-naming section is judged over its whole span, subsections
and tables included. Three of my own probes found the same class before
the report arrived, and the two overlap on the heading case.

One blocked finding is recorded as a limit under the developer's ruling of
2026-09-18: an honest outcome table whose cell begins with the prefix is
refused, because table cells are read for it. Keeping that is the price of
not losing a finding written as a table row, which the twenty-eighth round
found; the agreement says a fence is the only cover, and a reviewer's
summary table should not begin a cell with the prefix.

## Review at 356e94fc, 2026-09-18

The thirtieth reviewer re-verified the parts of the gate that predate this
round -- the carry and citation rules, the commit comparison, an
uncommitted and a deleted record, and freshness -- and found each behaving
as its requirement says. Its two silent findings are both about the same
mistake in my own fix: I enumerated the markup I could think of instead of
saying what markup is. A second list marker, a number in parentheses, an
emoji and an HTML comment each defeat a fixed set; a table written without
outer pipes defeats a test that asks for a leading pipe.

The answer is to stop enumerating. Everything before the keyword that is
not a letter is markup, so the line is read from its first letter, and a
row is a row wherever its pipes sit. That is one rule instead of a list,
and it closes my own footnote case with it.

Two wording faults say something worth keeping: a message must name where
a line actually is, and a repair must name the move the writer can make.
A prefix line in the header is now named as being above the list, with the
move into the list offered, and a field written after the list is told to
go above examined:. A report committed but missing from the tree is told
to be restored, not silently discarded.

## Review at 606d046c, 2026-09-18

The thirty-first reviewer did what none before it did: instead of guessing
shapes, it swept a finding-shaped line through every line position of two
rich records for nine markup shapes, and reported that outside a fence
there is no region the reader misses. That makes its two silent findings
rules rather than gaps, and it is the first evidence that the region
question is closed.

Both rules were mine. Reading a line from its first letter turned every
marker that contains a letter into cover, and the plainest of those is a
checked task box, which the helper's own comment claimed to strip. Pairing
fence markers by position let a prose line beginning with three backticks
open a fence and blank the findings below it, a shape this project invites
by telling reviewers to quote examples inside fences. Markup is now
stripped a unit at a time, and a fence opens only on a line of its own or
with one language word.

The kernel is at its agreed ceiling of 1900 lines, so both fixes were paid
for by trimming comment, and the ceiling is now a real constraint on this
class of work. Raising it changes an Agreed requirement, so it is the
developer's, and it waits in next-iteration with the record-format item
that would end the class altogether.

## Review at 3833b02a, 2026-09-18

The release round. The thirty-ninth reviewer is the first to report no
silent loss: no shape it built lost a finding with nothing named. It
tested that claim two ways the earlier rounds did not. It swept a
finding-shaped bullet through every line position of a rich report and a
rich review, 47 positions in all, and each one was named, so outside a
fence there is no region of either record that the reader neither reads
nor sweeps. It then generated 200 report shapes at random from the same
vocabulary of markers, indents, titles, rules, wrappings and line endings
that earlier rounds explored one at a time, and all 200 were named.

Its five findings are all about what a message tells the agent to do next,
not about a finding that vanishes. Three of them are the same defect seen
from three sides: when the gate refuses a record it sometimes prescribes a
repair that only the reviewer could honestly make. Telling an agent to
write entries under an empty examined field asks it to author another
party's observations; telling it to declare a report empty when the report
plainly names a defect in prose asks it to overwrite the reviewer's
meaning; and an indented header is answered about the wrong field
altogether. In each case the gate is right to refuse and wrong about the
remedy, and in each case the honest remedy is the one the kernel already
knows for a record only its writer can fix: ask for the report again.

The first finding is the mirror image. A reviewer who writes that it found
nothing, in words, inside the findings list is told to carry that non-
finding, and an agent that obeys leaves a review claiming a defect nobody
reported. The declared-empty form is the shape that reaches Done, and the
reviewer brief says so, but the message does not.

All four are message defects, and the fifth is a dead constant left in the
record reader by an earlier round. None of them produces a wrong Done on
an honest report: they cost a round, or they record a limit inaccurately.
Under the developer's ruling of 2026-09-18 on escalation loop-020, only a
silent loss is fixed inside this commitment; these are recorded as named
limits and captured for the next one, where the record-format item already
waits and would end the class.

The verification this round is the strongest the commitment has had: about
360 committed record pairs put through wake, each acceptance paired with a
control that differs in one detail, the whole 67-record review history read
again with the same 58 accepted and 9 refused as before, and the test suite
run twice in a pristine clone with no flake.
