# Same Page obligation. The derived fields (locator, digests, falsifier,
# sentence, profile_from, required) are regenerated by `same-page elaborate`
# from the spec and the policy; profile and validators are yours to set and
# survive re-elaboration. A profile you set is marked profile_from:
# obligation. A validators entry is `- name: <validator>`, plus
# `attested_by: developer` (or agent) to vouch for the mapping.
requirement: ENG-033
locator: docs/superpowers/specs/2026-09-02-same-page-conformance-engine.md::ENG-033
keyword: MUST NOT
requirement_digest: sha256:1a3a142be86515779b190500e1fc728e9876e40abad8d6e6838b508c4a37e0a0
falsifier: a validator's output declares a backend binding and the record carries `binding_basis` `backend`.
falsifier_digest: sha256:b902a2538120f3e11918361078f171ec01a11c80e5539710f7f3b41bcae44cb0
profile: default
profile_from: project default
required:
  any:
    - kind: formal
    - kind: model
    - kind: property
    - kind: integration
    - kind: test
    - kind: static
    - kind: manual
validators: []
sentence: The engine MUST NOT accept a backend binding from a free-form claim in a validator result.
