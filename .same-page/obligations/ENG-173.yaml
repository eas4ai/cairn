# Same Page obligation. The derived fields (locator, digests, falsifier,
# sentence, profile_from, required) are regenerated by `same-page elaborate`
# from the spec and the policy; profile and validators are yours to set and
# survive re-elaboration. A profile you set is marked profile_from:
# obligation. A validators entry is `- name: <validator>`, plus
# `attested_by: developer` (or agent) to vouch for the mapping.
requirement: ENG-173
locator: docs/superpowers/specs/2026-09-02-same-page-conformance-engine.md::ENG-173
keyword: MUST
requirement_digest: sha256:b6b28143d4c650e1994eb27aca36a7a54f65bcc708fd346f573ebb8606ccedc8
falsifier: a challenge realizes the violating state, the validator passes anyway, and `same-page verify` reports no weak sensitivity for that requirement.
falsifier_digest: sha256:4da8e9dfa999217ed562603f7836dcdefcac5bbb5011c1240ac1422b4f189d37
profile: default
profile_from: project default
required:
  any:
    - kind: formal
    - kind: model
    - kind: property
    - kind: integration
    - kind: test
    - kind: static
    - kind: manual
validators:
  - name: bun-test
    attested_by: agent
sentence: When a validator passes both the intended implementation and a challenge that realizes the violating state, the engine MUST report weak or vacuous sensitivity for that requirement.
