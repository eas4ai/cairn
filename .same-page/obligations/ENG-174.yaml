# Same Page obligation. The derived fields (locator, digests, falsifier,
# sentence, profile_from, required) are regenerated by `same-page elaborate`
# from the spec and the policy; profile and validators are yours to set and
# survive re-elaboration. A profile you set is marked profile_from:
# obligation. A validators entry is `- name: <validator>`, plus
# `attested_by: developer` (or agent) to vouch for the mapping.
requirement: ENG-174
locator: docs/superpowers/specs/2026-09-02-same-page-conformance-engine.md::ENG-174
keyword: MUST NOT
requirement_digest: sha256:a8c492152590688f9d7b7e40d7e7564799e657a56ba5ec53f98e52db63a791b2
falsifier: a validator passed a challenge that realizes the violating state and an earlier `challenged` record of that validator still counts as challenged.
falsifier_digest: sha256:9dbf8ddf9839de86f5bda35352f2159c3bf843fe53d3539fc51c4bde95736163
profile: default
profile_from: project default
required:
  any:
    - kind: formal
    - kind: model
    - kind: property
    - kind: integration
    - kind: test
    - kind: static
    - kind: manual
validators:
  - name: bun-test
    attested_by: agent
sentence: When a validator passes a challenge that realizes the violating state, the engine MUST NOT preserve a `challenged` sensitivity claim for that validator.
