@AGENTS.md

Merge other branches with `git merge --no-ff` so their commits stay off
this loop's first-parent history. Cairn checks each of this loop's own
commits; reverting a change does not erase a footprint breach. Declare a
missing input when it belongs to the commitment. For correct work outside it, follow AGENTS.md to request exact retention
approval with `cairn escalate --scope --keep`, then rerun checks and review.
For accidental work, capture it, commit restoration, and request the
restoration acknowledgment. Neither route authorizes future scope creep.
