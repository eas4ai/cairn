# Cairn audit, second pass, 2026-09-15

Examined commit: b87cd5f (main, every commitment Done, queue empty)
Requested by: the developer, "do another review", after the first
audit's remediation reached Done
Method: the same four independent read-only passes as the first audit
(kernel logic, contract consistency, kernel-versus-spec enforcement and
tests, scripts and operations), each finding then verified by the
auditor by reading the cited lines or by reproducing it in a
throwaway repository under the session scratchpad with a scratch
HOME. Nothing in this repository was changed or run against by the
audit. Line numbers refer to b87cd5f. A finding the auditor did not
reproduce is marked "by reading" or "unverified".

This is a report. It captures nothing to the backlog or next-iteration
and fixes nothing; see the closing section for the recommended
remediation.

## Summary

| Severity | Meaning | Count |
|---|---|---|
| WRONG | A wrong verdict, a crash, or a loop the agent cannot leave, in a plausible state | 19 |
| GAP | A case no code or directive covers, or a gate that does not observe what it claims | 17 |
| CONFLICT or STALE | Two directives cannot both be followed, or a statement is no longer true | 8 |
| NIT | Message, wording, test labeling, or cosmetic | 13 |

Fifty-seven findings; two claims inside C7 are unverified and marked
so.

Nothing found blocks Cairn in the normal flow: install, wake, check,
review, promote, and the walkthrough all work, and the suite passes
on Node 18 and Node 24. The findings are in the gates the first
remediation added or tightened, in record shapes the parser still
does not accept, and in the hooks' view of where the kernel and the
project are.

The findings to act on first, in the auditor's order:

1. A1 and A2: the two gates the first audit tightened (LOOP-090 and
   the capture gate) are satisfied by any escalation that ever named
   the requirement, open or closed, from any commitment. Every
   escalation in this repository names LOOP-091, so a future
   promoted commitment may rewrite LOOP-091 unseen.
2. C1, C2, C3: the hooks honor one symlink at one path, replace a
   valid link whose target is a wrapper, and are inert below the Git
   toplevel. Each reopens the first audit's A5 state: hook and agent
   judge with different kernels, or the hook does not judge at all.
3. A4: a declared input of `.cairn/*`, `*`, `**`, or `..` passes
   LOOP-105 and traps the loop exactly as the first audit's A13 did.
4. A3: below the Git toplevel, scope restoration is never checked, so
   `escalate --scope` accepts an unrestored tree and an `ok` answer
   acknowledges paths that are still changed.
5. D1: thirty-seven requirements agreed today under the developer's
   direction carry a bare `Status: Agreed` line on the day SPEC-002
   defined the deference marker for exactly that case.
6. B6: promotions are recorded at Judged, which never enters the
   review queue, while the agreement, README, manual and walkthrough
   tell the developer to read promotions in the queue.
7. A5 to A8: four record shapes crash wake or trap it: a list-form
   `Concerns:` or `Rests on:`, a list-form `Promoted from:`, a
   `Specified from:` written as a filename, and `examined: []`.

## A. Kernel defects: wrong verdicts, crashes, traps

Each item names the state, what the kernel does, and the lines.

**A1. WRONG. The LOOP-090 gate is silenced by any escalation that
ever named the changed requirement.** bin/cairn.mjs:273 accepts any
file under .cairn/escalations/ whose Concerns line covers the changed
identifiers, with no regard to when it was raised, which commitment
raised it, what it asked, or what the developer answered. A DEC-016
three-fails escalation from an earlier commitment names exactly one
requirement and satisfies the gate for that requirement forever.
Reproduced: commitment zero escalates R-001 and the developer answers
`instead keep R-001 exactly as written`; a later promoted commitment
rewrites R-001; wake prints `review mechanism R-001`, not `escalate`.
In this repository the four answered escalations all carry
`Concerns: LOOP-091`. The test at tests/continuation.test.mjs:167
observes the falsifier of LOOP-114 (prose slug versus Concerns line)
with an unrelated escalation only. This is less specific than the
fa5ee4f reading, which at least required the commitment's slug.

**A2. WRONG. The capture gate is silenced the same way.**
bin/cairn.mjs:252-253. A next-iteration item with `Changes: R-001`
captured inside the commitment, no `Outside because:` line, and only
an old answered escalation naming R-001 reaches `Done`. That is the
condition docs/decisions/promote-the-capture-gate-s-reading-of-the-
loop-090-escalation.md wrote under `Would be wrong if`.

**A3. WRONG. Below the Git toplevel, scope restoration is never
checked.** bin/cairn.mjs:177 runs `git diff --name-only` without
`--relative`, the one path-emitting call that did not get it under
LOOP-112, so `changed` holds `packages/app/src/extra` while the
snapshot's paths hold `src/extra` and the intersection is empty.
Consequences, reproduced in a nested project: `escalate --scope` in
restore mode is written without the restoration (the flat control
refuses with "restore these paths"), and after `answer ok` the still-
changed path is acknowledged and wake prints `Done`. The re-change
check at :198 is dead the same way.

**A4. WRONG. LOOP-105's covering check misses every wildcard and
parent form; the first audit's A13 trap is still reachable.**
bin/cairn.mjs:543-544 normalizes `./` and a trailing slash and tests
`.`, the empty string, `.cairn` and `.cairn/evidence`. Inputs
`.cairn/*`, `*`, `**`, `:(top)`, `:/`, and `..` from a nested root pass
validation; every check then ends `commit .cairn/evidence/ ...
candidate changed during t; no evidence recorded (LOOP-063)`, and
committing the retained output only moves the name to the next run's
`.err`. Reproduced for `.cairn/*` by the auditor (two checks, zero
receipts) and for the other forms by the kernel pass. The LOOP-105
test at tests/robustness.test.mjs:38-51 observes the three named
faults honestly; the requirement text says "declares an input under
.cairn/evidence/", which these inputs do.

**A5. WRONG. A list-form `Concerns:` or `Rests on:` crashes the
kernel.** bin/cairn.mjs:44 turns `- item` lines into arrays for every
field, and LOOP-102 promised list-form reading for "a record field",
but :290 calls `matchAll` on `Rests on` and :253, :273, :777, :808
call `split` on `Concerns`. Reproduced: a decision with `Rests on:`
as a list makes `cairn reversals` exit 3 with `(f.Rests on ?? "").
matchAll is not a function`; the same record makes wake exit 3 for
any promoted commitment, since :836 reads every decision. The kernel
pass reproduced the `Concerns:` crash on wake once three failing
attempts exist. Neither error carries the record tag, so the LOOP-107
repair path is bypassed and the stop hook stops blocking (C5).

**A6. WRONG. A list-form `Promoted from:` is a repair that `cairn
decide --promotes` cannot clear.** bin/cairn.mjs:92 leaves the array
as read; :836 compares `d.Promotes === c.promoted` strictly. Wake
prints `repair docs/commitments/<slug>.md ... record the promotion
with cairn decide --promotes <item>` on every wake, and following the
instruction changes nothing (decide refuses, the record exists).

**A7. WRONG. `examined: []` passes the "nonempty examined list"
gate.** bin/cairn.mjs:731 tests `asList(f.examined).length`; `findings:
[]` is special-cased at :733, `examined: []` reads as the one-element
list `["[]"]`. Reproduced: a review with `examined: []` and `findings:
[]` reaches `Done`. The working agreement teaches the `[]` idiom for
findings, so an agent will write it for examined.

**A8. WRONG. `Specified from:` and `Promoted from:` accept only the
bare slug; the natural forms bypass SPEC-027 silently.**
bin/cairn.mjs:838 looks up `.cairn/next-iteration/<token>.md` for
each token; `foo.md`, `.cairn/next-iteration/foo.md` and a backticked
slug match nothing, so the unstamped item is never named, wake
reaches Done and counts the item as waiting, and the next
specification phase reads it as unspecified. Reproduced by the
operations pass. `Promoted from:` and `--promotes` must be byte-equal
(:836) the same way. skills/next-iteration/SKILL.md:111-113 and
AGENTS.md:62-66 say "naming the item" without the form.

**A9. WRONG. An unreadable record directory exits 3 with a raw
error.** bin/cairn.mjs:70 tags file reads only; `list()` at :71 and
`files()` at :74 are unguarded. Reproduced: `chmod 000
.cairn/escalations` makes wake print `cairn: EACCES: permission
denied, scandir ...` and exit 3. LOOP-107's falsifier names a record
file; the directory is the same class.

**A10. WRONG. `cairn lint` prints a Node stack trace and exits 1, the
code reserved for a finding, when the directory is missing; `--root`
to a missing directory exits 3 silently.** scripts/spec-lint.mjs:23
calls `readdirSync(dir)` unguarded; bin/cairn.mjs:1365 returns
`.status ?? 3` and prints nothing when the spawn fails. Reproduced
from an empty directory, from a subdirectory of a project, with a
file as DIR, and with `--root /nonexistent`. The skills tell an agent
to run `cairn lint docs/spec`; from a subdirectory it gets a trace
where wake gets "run from the project root or pass --root DIR".

**A11. WRONG. When a mechanism removes the check lock, check prints a
repair for the lock after recording the evidence.** bin/cairn.mjs:986
uses the tagged `read()` inside `finally`; the LOOP-107 catch at
:1383 then prints `repair .git/cairn-check.lock ... cannot read the
record (ENOENT)` and exits 1 although both results were recorded.
Reproduced by the kernel pass.

**A12. WRONG. `dirtyInputs` mis-parses a staged rename.**
bin/cairn.mjs:621 slices three characters off every NUL-separated
token of `git status --porcelain -z`, including a rename's origin
path, which has no status prefix. After `git mv src/other
src/renamed`, wake counts two dirty inputs and check names
`src/renamed, /other`. Reproduced by the kernel pass.

**A13. WRONG. A title that slugifies to nothing writes a hidden
record.** bin/cairn.mjs:1115 and :1253: `backlog --title '!!!'`
writes `.cairn/backlog/.md`; `list()` at :71 hides dotfiles, so the
item never reaches `promote` and wake says Done. `decide` has the
same path by reading.

**A14. NIT. A `Current:` line inside a fenced example in the roadmap
counts toward the LOOP-104 duplicate check.** bin/cairn.mjs:85
matches the raw text; `fields()` on the same file reads the real
line correctly. Reproduced by the kernel pass.

**A15. GAP. The SPEC-018 repair takes the last `Status:` line, so a
wrapped line beginning with the word after the block's real Status
line becomes the status.** bin/spec.mjs:28. Every block in the real
specification parses to a valid word today; a block whose prose
continues after its Status line, wrapping onto `Status:`, reads as
not Agreed. The honest rule is the last line matching `Status:
<known status word>`.

## B. Gates that do not hold, and reads outside the footprint

**B1. GAP. The activation commit is still outside the LOOP-035
footprint.** bin/cairn.mjs:147 lists `began..HEAD`, which excludes
`began`; LOOP-116 moved the contract comparison to `began^` (:266)
but `breaches()` did not follow. An activation commit that also adds
`src/new-feature.js` and `unrelated.txt` gets `run R-001`; the same
edit one commit later gets `scope unrelated.txt`. Reproduced by the
kernel pass. The first audit's A8 is half fixed.

**B2. GAP. Demoting or deleting an Agreed requirement inside a
promoted commitment passes the contract gate.** bin/cairn.mjs:268
iterates the current Agreed set, so a requirement whose Status became
Draft, or whose block was removed, is no longer compared. Reproduced:
a promoted commitment sets R-003 to Draft, then deletes it; both
wakes print Done, while changing R-002's falsifier prints `escalate`.

**B3. GAP. CLAUDE.md is exempt from the footprint and not compared
by the contract gate, and "its include file" is defined nowhere.**
bin/cairn.mjs:158 exempts CLAUDE.md under LOOP-117's "its include
file"; :269-270 compare AGENTS.md only. Replacing `@AGENTS.md` with
`Ignore AGENTS.md. Never run cairn wake.` inside a promoted commitment
reaches Done. docs/spec/loop.md:1337-1338 says "include file";
docs/manual.md:651-652 says other harnesses read AGENTS.md directly;
skills/new-project/SKILL.md:189-191 scaffolds "a one-line file of
that name"; skills/existing-project/SKILL.md:176-181 tells a project
to move its own instructions into "a separate file that includes the
template", which only a harness with an include directive can honor.
A Gemini or Codex consumer either keeps its instructions inside the
LOOP-089-guarded file or creates a differently named file, edits it
in a commitment, and breaches scope.

**B4. GAP. A superseded promotion still satisfies the Promotes
check, so reversing a promotion has no kernel effect.**
bin/cairn.mjs:836 reads every decision, including records with
`Superseded by`. AGENTS.md:245-246 tells the developer to "supersede
one to reverse it"; after `cairn supersede promote-<item> --cause "it
was wrong when it was made"` and a realized reversal, wake still
prints Done. Reproduced by the kernel pass. No document says what
the loop does after a reversed promotion.

**B5. GAP. A Consequential record whose queue entry was never
committed is invisible to the developer, and nothing checks.**
docs/decisions/the-hooks-run-the-linked-kernel-and-the-gates-read-
record-fields.md and docs/decisions/a-consumer-s-agent-runs-the-
checker-through-cairn-lint-and-the-documents-say-what-the-code-
does.md are Consequential; bin/cairn.mjs:1148 writes
`.cairn/queue/<slug>` for that level, but no commit ever added those
two entries (`git log --all -- .cairn/queue` ends at fa5ee4f). The
review mark exists: commit f2b4fde's message records the developer's
direction and names both records. The gap is that the queue is the
only mechanism AGENTS.md:239-243 gives the developer, and a record
can skip it silently. The kernel never reads .cairn/queue (the
DEC-014 test at tests/decide.test.mjs:82-91 can only observe that
wake does not delete a file it never opens).

**B6. CONFLICT. Every human document sends the developer to the
review queue for promotions; promotions are recorded at Judged, which
never queues.** AGENTS.md:245-246 "Read promotions in the review
queue; supersede one to reverse it"; README.md:64 and :78-79;
docs/manual.md:63-64 and :241-243; docs/walkthrough.md:180-181.
Against them: docs/spec/loop.md:981-983 (LOOP-088, "Judged or
Consequential"), docs/spec/decisions.md:48-50 (DEC-004 queues
Consequential), and both promotion records made today at `Level:
Judged`. The developer reading the queue as instructed never sees
LOOP-118 or LOOP-119, requirements the agent agreed alone.

**B7. GAP. The specification lint reads the checkout's skills/
directory, undeclared; a consumer's lint mechanism cannot declare
the checker at all.** scripts/spec-lint.mjs:20 builds the SPEC-019
allow-list from `readdirSync(../skills)`; .cairn/mechanisms/spec-lint
lists docs/spec/, scripts/spec-lint.mjs, bin/spec.mjs and
docs/decisions/. Adding a skill directory named `tmp` would exempt
`/tmp` from SPEC-019 with evidence current, the first audit's B2
class reintroduced. For a consumer, `cairn lint` runs code outside
the repository, which no declaration can name and the kernel digest
does not cover.

**B8. GAP. The package lint reads .cairn/ three ways with nothing
declared and no note.** scripts/pkg-lint.mjs:16 (the tracked set,
used at :38 for .cairn/in-progress), :45 (`readdirSync(.cairn)` for
PKG-003), :34 (`git check-ignore`, which also reads the global
excludes file). The plan (package 7, :185-187) said the listing would
be "noted in the declaration as a known read"; the declaration
carries no note and the review mentions the ASCII scan only.
Declaring `.cairn` is now refused by LOOP-105, so the read is
inexpressible and should be recorded as such.

**B9. GAP. The Escalate verdict is unreachable while an in-progress
record exists, and no document says so.** bin/cairn.mjs:815-820:
reconcile precedes open escalations. AGENTS.md:80 says to remove the
record "when the change is committed"; an agent that escalates
because it cannot finish has nothing to commit. Reproduced: record
plus dirty input plus `cairn escalate` gives `reconcile`; removing
the record gives `present`; after `answer ok` LOOP-110 asks for the
record again. Not a trap; an undocumented sequence.

**B10. NIT. Wake and check name different actions for one state.**
A dirty declared input with no record: wake says `record <path>`
(bin/cairn.mjs:846), check says `commit <path>` (:1045, LOOP-030). An
agent following check's message never writes the record LOOP-022
asks for.

## C. Hooks and installation

**C1. WRONG. The hooks honor one symlink at one path; every other
install route reopens the first audit's A5 state without a message.**
bin/hook.mjs:16 hardcodes `$HOME/.local/bin/cairn`; :23 requires a
symlink whose target ends in `cairn.mjs`; :44 reports "another
checkout" only when the linked kernel differs from its own, which is
never the case when the link is not recognized. A shell wrapper, a
copied file, `npm link` (package.json ships a `bin` entry), or a link
at `~/bin` makes the hook judge with its own checkout while the
agent's `cairn` is another. Reproduced by the operations pass: a
wrapper script at the path, project checked and reviewed under
checkout B (Done there); the stop hook from checkout A blocks with
"the kernel changed" and session-start prints no warning. PKG-021
says "the command link on the path"; the install skill (:19-20)
allows "an installation location the user already chose".

**C2. WRONG. Session-start replaces a valid link whose target is not
named cairn.mjs.** bin/hook.mjs:39-42 treats `isSymbolicLink() &&
!linked()` as dangling. With `~/.local/bin/cairn -> /opt/.../cairn`,
an executable wrapper, session-start printed `cairn: linked
~/.local/bin/cairn -> <this checkout>/bin/cairn.mjs` and the user's
link is gone. Reproduced by the kernel pass. PKG-022's rationale
(package.md:222-223) says only a link that "resolves to nothing" is
missing; docs/manual.md:632-634 and skills/install-cairn/SKILL.md:40-41
still say the hook "leaves any existing file or link alone".

**C3. WRONG. Both hooks are inert for a project below the Git
toplevel, the layout LOOP-112 made the kernel support today.**
bin/hook.mjs:18-22 accepts only `<toplevel>/docs/spec/roadmap.md`.
Reproduced: in `packages/app` the kernel says `run R-001`; the stop
hook prints nothing (stop allowed) and session-start prints no
verdict; with a roadmap at the toplevel too, the hook judges the
wrong project. The remediation plan (:125-126) says the hook "accepts
a roadmap in a subdirectory of the toplevel by walking up from cwd"
and (:316-317) that the plan was applied as decided. The first
audit's A4 is half fixed.

**C4. WRONG. Any failure to create the link aborts the wake verdict
at session start.** bin/hook.mjs:37-46: the link block and `wake(top)`
share one `try`. A read-only or root-owned `~/.local/bin`, a HOME
with no `.local` and no write permission, or `~/.local/bin` as a
regular file each print one stderr line (PKG-022 met) and no `cairn
wake:` block (PKG-019's falsifier). Reproduced by the operations
pass for all three.

**C5. GAP. A linked kernel that runs but fails makes the stop hook
silent and session-start print the child's stack trace as the
verdict.** bin/hook.mjs:33 blocks only on `^Resolvable:`; :46
forwards stdout and stderr verbatim. A link to a directory named
cairn.mjs, to a checkout with bin/spec.mjs deleted, to a file that
prints "not cairn", or to an older kernel that rejects `--root`: stop
prints nothing and exits 0, the guard gone with no signal;
session-start prints `ERR_MODULE_NOT_FOUND` traces inside `cairn
wake:`. Reproduced by both passes. A5's crash class (finding A5
above) lands here too.

**C6. NIT. With Git absent, session-start links and prints nothing
else in a Cairn repository; stop allows; no stderr line.**
bin/hook.mjs:18-21 turns a failed `git rev-parse` into "not a
repository". Reproduced with `PATH=/nonexistent`.

**C7. NIT. The Node version check is unreachable and the README
minimums are slightly wrong.** bin/cairn.mjs:21 statically imports
`parseArgs`, absent before 18.3 (and 16.17); the module fails to link
before :1361 runs; bin/hook.mjs has no check. README.md:118 says
"Node 18", which 18.0-18.2 do not satisfy, and "Git 2.5";
`git rev-parse --is-shallow-repository` (bin/cairn.mjs:318, LOOP-113)
arrived in Git 2.15 by the auditor's recollection, unverified here
(this machine runs 2.55). On older Git the shallow message never
appears and the record is named `build`. By reading; no old Node on
this machine.

## D. Contract and document conflicts, stale statements

**D1. CONFLICT. Thirty-seven requirements agreed today carry no
deference marker, on the day SPEC-002 defined it for this case.**
docs/spec/specification.md:30-39 defines deference as the developer's
recorded ruling that the agent's recommendation stands for a named
scope, and promises that one search for `by ` lists every requirement
the developer did not read line by line. The plan (:315-316) records
that the developer directed the findings be fixed without further
rulings, and docs/decisions/audit-found-contract-defects-are-repaired-
on-the-developer-s-direction.md quotes it. Yet LOOP-101 to LOOP-117,
PKG-004, PKG-021 to PKG-032, SPEC-002, SPEC-028, SPEC-029, DEC-016 and
LOOP-026 carry bare `Status: Agreed 2026-09-15` (37 lines across four
files; the developer read package descriptions in the plan, not
these texts and falsifiers). docs/spec/overview.md:83-86 still says
the `by promotion` marker alone "is the search that lists every
requirement the developer never read", and skills/next-iteration/
SKILL.md:99-101, the phase where deference happens, never names the
marker. The digest excludes the Status line (bin/cairn.mjs:108), so
stamping stales no evidence.

**D2. STALE. LOOP-090 and LOOP-092 falsifiers describe the reading
LOOP-114 and LOOP-119 replaced.** docs/spec/loop.md:1005-1007 "no
escalation names that commitment"; :1047-1050 "no escalation names
the file". The kernel now matches Concerns identifiers (A1, A2). A
reader of LOOP-090 expects the slug in the escalation; a reader of
LOOP-092 expects the path.

**D3. STALE. The manual says all of docs/ is scope-exempt.**
docs/manual.md:511-512, against docs/spec/loop.md:1336-1339 (LOOP-117)
and bin/cairn.mjs:158. A consumer editing `docs/design.md` inside a
commitment gets `scope docs/design.md` after reading that it would
not.

**D4. STALE. Three texts still describe the duplicate-owner refusal
LOOP-056's revision removed, and one describes the old attempt
rule.** docs/spec/loop.md:691-693 (LOOP-067 rationale: "retains the
existing reporting-mode and duplicate-owner checks");
docs/manual.md:492 ("duplicate requirement ownership");
docs/spec/roadmap.md:175-176 ("two declarations for one requirement
are refused by name", with no pointer despite the top note at :5-7).
docs/spec/roadmap.md:129-131 states attempts by "the previous failing
record", not DEC-018's "a digest already in the streak"
(decisions.md:170-172).

**D5. GAP. `cwd:` is a declaration field LOOP-105 validates and no
document defines.** docs/spec/loop.md:1233-1235; bin/cairn.mjs:562,
:1062, :1092. AGENTS.md:83-92 lists command, inputs, requirements,
results, reviewed; the manual and the skills never mention it. A
monorepo consumer needs it and cannot learn it.

**D6. STALE. The manual's action tables lack `record`, `declare` and
`resolve`; no human document mentions `cairn lint`; the README still
sends contributors to the script.** docs/manual.md:146-153 and
:488-503; `record <path>` (bin/cairn.mjs:846) is the verdict a human
most often sees at session start mid-edit. README.md:271 says
`node scripts/spec-lint.mjs`; README, manual and walkthrough never
name the command PKG-028 shipped.

**D7. STALE. Install skill, manual and README describe the hook's
old link behavior.** skills/install-cairn/SKILL.md:40-41 and
docs/manual.md:632-634 ("leaves any existing file or link alone");
README.md:128-129 (incomplete). The hook now replaces a dangling link
or a symlink loop, reports another checkout, and judges with the
linked kernel (bin/hook.mjs:39-44; package.md:222-225). Also
docs/manual.md:658-660 "The link and the hooks follow the updated
files" holds only when link and hooks point at one checkout.

**D8. STALE. The remediation plan records as applied six things that
were not built, and both audit documents say A11 and B10 are still
waiting.** Not built: plan:96-97 "wake says so once when a directory
holds one" (bin/cairn.mjs:71-74 ignore silently); plan:146-147 prose
promotions "read as before for one release, with a repair hint"
(:836 reads `Promotes:` only); plan:185-187 the pkg-lint note (B8);
plan:227-228 "new-project Stage 4 writes that declaration"
(skills/new-project/SKILL.md:157-191 writes no spec-lint
declaration; it says to run `cairn lint`); plan:305-306 `record
<REQ>` against the built `record <path>`; plan:125-126 the hook (C3).
plan:5-6 and :316-317 claim full execution. plan:355-356 and
audit:640-644 say A11 and the capture-gate half of B10 are "captured
to the backlog": both were promoted and reached Done
(roadmap.md:422-436).

**D9. NIT. The glossary has no Kernel entry while two specifications
define it differently, no Deference entry, and its Agreed entry omits
deference while Falsifier names it.** package.md:42-44 (PKG-004:
every run-time file under bin/, three including hook.mjs) versus
loop.md:1095-1097 and bin/cairn.mjs:79 (digest of two);
glossary.md:8-13 versus :38-40. A reader cannot tell whether a
hook.mjs change stales evidence (it does not).

**D10. NIT. Drafting defects the per-sentence lint cannot see.** Two
obligations under one MUST: LOOP-105 "refuse ... naming the fault"
(loop.md:1233-1235), LOOP-108 "accept ... only when ..., naming the
missing field" (:1255-1257), PKG-022 "exit 0 with one line on standard
error" (package.md:215-216). LOOP-118 says "record-writing" but its
falsifier and bin/cairn.mjs:1367 include `reversals`, which only
reads. PKG-028's falsifier "fails" conflates a finding (exit 1) with
failing to run. SPEC-029's falsifier keeps "outside backticks and
quotes" from the withdrawn SPEC-028 draft while the scan runs over
raw lines (specification.md:323-324, spec-lint.mjs:50). LOOP-026's
falsifier still fires on a `--level Blocking` escalation with a
missing field, which LOOP-013 requires to be delivered (loop.md:201-
203 versus :231-234). PKG-004's text says "the files under bin/ that
Cairn executes at run time" and its falsifier "the files under bin/",
while scripts/pkg-lint.mjs:20 counts `bin/*.mjs` (1600 lines in
bin/run.sh pass, reproduced by the enforcement pass). The kernel's
DEC-016 message says "three consecutive failing records"
(bin/cairn.mjs:866) where the count is attempts (decisions.md:55-57).

**D11. NIT. Numbers inside the audit and plan do not match the
repository.** audit.md:603-605 "all 78 records": `git ls-tree fa5ee4f
docs/decisions/` lists 63. audit.md:495 (D1) "34 are named by no
test" against package.md:234-235 and the declarations decision, "36".
plan.md:335-343 "Left as designed" omits B12, which audit.md:643
lists by design and the plan places under R3 (:320).

**D12. NIT. Two sentences and one argument.** docs/walkthrough.md:74-
76 "each block below begins by committing the check before it" holds
for blocks 3 and 4 only; blocks 5-7 open with escalate, answer and
cat. `cairn lint docs/spec extra` ignores the extra positional
silently (bin/cairn.mjs:1365 uses `rest[0]`).

**D13. NIT. PKG-025 and PKG-026 name one mechanism by filename inside
a `Scope: every commitment` file, and their mechanism is a spelling
heuristic.** package.md:250-260; renaming the declaration strands
both requirements; see E1 for what the heuristic sees.

## E. Tests

**E1. GAP. The PKG-026 coverage scan sees three read shapes and
nothing else, and the commitment review says otherwise.**
tests/coverage.test.mjs:25 matches `new URL("../`, `flat("../`,
`raw("../`, `here("../` with a literal string. Not matched, verified
against the regex: `new URL(\`../bin/${f}\`, ...)` (tests/evidence-
integrity.test.mjs:48), `import ... from "../bin/spec.mjs"`
(tests/agreement.test.mjs:8), `cpSync(join(KERNEL, ".."))`
(tests/hooks.test.mjs:80), and a read by a spawned process (the
kernel's bin/spec.mjs, `cairn lint` and the skills listing). All fall
under declared inputs today, so there is no live gap, but
.cairn/reviews/this-repository-declares-and-lints-what-it-reads.md
says "the suite has no other shape today", which is false.
tests/coverage.test.mjs:11 also lists the repository root
(`readdirSync(here(".."))`) for no purpose the test uses.

**E2. NIT. Fourteen test titles name a requirement their body does
not observe.** PKG-025 is a title scan (tests/coverage.test.mjs:15),
so a title can satisfy it without observing the falsifier. Checked
by the enforcement pass, body against falsifier text:
tests/wake.test.mjs:68 (LOOP-006: no mechanism, where the falsifier is
a mechanism with no inputs; :18 of declaration-validation is the
honest one), :74 (LOOP-004), :94 (LOOP-001, LOOP-003: two wakes with
no transition, and the fixture now lands on `repair docs/decisions/
z.md`, not the build path it was written for);
tests/check.test.mjs:130 (LOOP-021: asserts absence only), :46
(LOOP-025: two passing checks, no fail); tests/escalate.test.mjs:44
(DEC-002, on the audit's unobservable list), :14 (LOOP-026: presence
of labels only, not order or one-line-ness; order holds by the
ESC_FIELDS constant at bin/cairn.mjs:1155); tests/robustness.test.
mjs:135 (LOOP-046: the plain directory has no roadmap, so the
roadmap check refuses first), :32 (LOOP-019: this is LOOP-104's
observation); tests/hooks.test.mjs:77 (LOOP-096: the agreement
sentence, observed at tests/skills.test.mjs:91);
tests/skills.test.mjs:159 (LOOP-087: prose; the falsifier is observed
at tests/continuation.test.mjs:31); tests/help.test.mjs:40 (PKG-028:
help listing; tests/lint-command.test.mjs:8 observes it);
tests/execution-order.test.mjs:58 (LOOP-098: never merges a run
receipt and a legacy receipt in sequence order);
tests/decide.test.mjs:31 (DEC-004: observes the queue file, DEC-013).

**E3. GAP. Branches no test observes.** LOOP-113's ambiguous-
identifier branch (bin/cairn.mjs:319-320; the kernel pass built a
real 7-hex collision with fast-import and confirmed the message, so
the code is right and untested). PKG-022's "kernel cannot be found"
case (tests/hooks.test.mjs:98 covers the other two). LOOP-105's
covering check for wildcard forms (A4). The earlier-escalation case
for LOOP-114 and LOOP-119 (A1, A2). Nested `--scope` and nested hooks
(A3, C3). A link to a non-cairn.mjs target (C2). List-form `Concerns:`
and `Promoted from:` (A5, A6). The activation commit's non-record
changes (B1). De-agreeing a requirement, editing CLAUDE.md, and a
superseded promotion inside a promoted commitment (B2, B3, B4). A
crashing linked kernel (C5).

**E4. NIT. One deliberate loss among today's test changes.**
tests/candidate.test.mjs:85-86 removed the LOOP-063 `mode` variant:
what it proved, that a chmod `git status` hides under
`core.filemode=false` cannot certify a candidate, is now the
opposite by design (LOOP-111), and nothing tests an uncommitted mode-
only change under `filemode=true` reaching the LOOP-030 dirty-input
path. The other changes (CRLF test commits, requirement-freshness
commit, decision fixtures gaining headers, docs/notes.md to
docs/recon.md, DEC-014 fixture, the two repository-level lint tests
removed, LOOP-032 label moved) lose no proof; each was checked.

**E5. NIT. Five requirement tests are text proxies.** LOOP-026
(E2), PKG-026 (E1), PKG-029 (tests/skills.test.mjs:144, one bad
substring), PKG-030 (:152, three phrasings), PKG-031 (:152,
substrings "Node 18", "Git 2.5", "Windows": a README saying "Windows
is fully supported" passes). By the first audit's R2 ruling these are
acceptable; they are listed so the limit stays visible.

## F. The two lints

Every rule caught its direct violation in the enforcement pass's
probes (spec-lint: SPEC-001, SPEC-002, SPEC-019, SPEC-020, SPEC-021,
SPEC-024, both markers, PKG-007, PKG-010; pkg-lint: PKG-001 to
PKG-004, PKG-006, PKG-008, PKG-009, PKG-012, PKG-013). Realistic
violations that slipped:

**F1. WRONG. SPEC-028's metacharacter set is `^ $ * | ? \`; a class
or quantifier regex meets SPEC-028's own falsifier.**
scripts/spec-lint.mjs:56. Reproduced on a fixture: `` `/[a-z]+/` ``
is reported as "absolute path /[a-z]+/ (SPEC-019)"; `/[0-9]{3}/`,
`/foo/g`, `/a.b/` likewise; `` `/^\s*$/` `` passes. The first audit's
other E1 examples, `~two seconds` and `` `</div>` ``, are still
flagged as `~two` and `/div`.

**F2. WRONG. SPEC-029 matches only backslash drive paths and
`file://`.** scripts/spec-lint.mjs:54. `C:/Users/alex/config.toml`,
`c:/temp/x` and `file:/tmp/secret` pass clean. Reproduced.

**F3. GAP. PKG-027 misses a wrap inside a list item.**
scripts/pkg-lint.mjs:66-67 joins a paragraph with one space, so `1.
Open Claude\n   Code and paste.` yields "Claude    Code" and the
pattern needs one space. Numbered steps are the normal shape of a
skill. Reproduced.

**F4. GAP. PKG-006's verb list is `run|use|invoke|open|type`.**
"Launch Cursor", "Start Windsurf", "In Claude Code, press enter"
pass. Reproduced.

**F5. GAP. The deferral phrase list misses the common phrasings.**
scripts/pkg-lint.mjs:82: "future version", "not yet supported",
"coming soon", "next release", "later milestone" pass; "later
version" and "for now" are caught. Reproduced.

**F6. GAP. PKG-002's ignore probe has no extension.**
scripts/pkg-lint.mjs:34 probes `.cairn/evidence/probe`; ignoring
`*.out` and `*.err` passes the lint and hides the captured output
from clones, which LOOP-065 then reports as damaged evidence.
Reproduced.

**F7. NIT. Robustness only.** PKG-023: a help text with no
`Commands:` heading yields no commands, and a hyphenated command
reads as its first word (scripts/pkg-lint.mjs:42). SPEC-019:
`root:/var/lib` passes (a colon is not a leading delimiter).
PKG-008: `.rst` is not text by the extension list. LOOP-103: a
lowercase `readme.md` under .cairn/escalations/ is presented and
`docs/decisions/template.md` with a placeholder Realized by is a
`build` on every wake; both by the slug definition, both sharp.

## First-audit items, rechecked

Confirmed fixed at b87cd5f: A1, A2, A3, A5 (for the recognized link),
A6, A9, A10, A11, A12, A14, A15, B1, B3, B4, B5, B8, B9, B13, B14,
B15, B16, B17, C1 to C18, D1 to D5, E1 (the file URL half), E2, E3,
F1, F2, F3, F5. The tests added today observe their falsifiers with
the exceptions in E2.

Half fixed, per the findings above: A4 (hooks and `changedScope`
below the toplevel: C3, A3), A7 and B10 (the substring match became a
Concerns match with no time bound: A1, A2), A8 (the contract
comparison moved to the activation's parent, the footprint did not:
B1), A13 (wildcards: A4), B2 (spec-lint's skills/ read: B7), B11
(Git-level `throw new Error` sites still exit 3 through `usage`, by
reading), E1 (the regex half: F1).

## Examined and found sound

The install flow on a fresh HOME (clone, session-start, link, help
through the link and through a link to a link); the walkthrough run
exactly as tests/walkthrough.test.mjs runs it (exit codes 1,1,1,0,2,
0,1, tree clean after every committing block); every command and
flag the skills and AGENTS.md name exists in `--help` and the parser;
AGENTS.md byte-identical to the template with a move for every kernel
verb (the LOOP-101 test reads the verbs from source); LOOP-118
refusals for every record-writing command outside a repository,
nothing written; the LOOP-110 sequences (record, reconcile, commit,
run) and the mechanism-review flow, no sequence stuck; CRLF in every
record kind; a `trunk` default branch; worktrees (lock under the
worktree's git dir, in-progress per tree, hooks fire, check records);
submodules (undeclared is a scope breach, declared is a LOOP-068
repair, a path inside is LOOP-044); Node 18.20.8 runs help, wake,
lint, both hooks and the full suite (424 pass); both lints clean on a
fresh clone; `files()` filtering for every record kind; the tagged
`read()` and the LOOP-107 catch for record files; the check lock
released before the catch prints; list-form `Requirements:` and
`Specified from:`; the duplicate `Current:` repair; decision header
validation, `Supersedes:` existence, the shallow-clone repair and
the build fallback; superseded records skip the Realized-by check;
`reviewOf`'s three fields and the heading hint; the wake order
(record before scope, capture, contract and evidence; LOOP-106's
guards on revision, three-fails and regression); `check --stale`
counting repairs; `promotedContractVerdict` from a nested root;
`fileIdentity` under `core.filemode=false`; every filename under the
four record directories matches the slug form; all seven Realized-by
identifiers resolve; the seven decision records carry every header
field and the two promotions' `Promotes:` match their commitments'
`Promoted from:`; the roadmap's four pointers exist; docs/spec/
draft.md is gone from the tree and the map; pkg-lint's tracked-file
coverage is complete (every tracked path outside .cairn/ falls under
its inputs); node-test's declaration covers every read the tests make
today.

## Not verified

The Git version that introduced `--is-shallow-repository` (C7);
behavior on Node below 18.3 (C7, reasoned from the static import);
Codex's treatment of `disable-model-invocation` and of an include
directive (B3); `npx skills update` argument semantics.

## Remediation

The findings sort into five groups; each can be one commitment on the
route the first remediation used: the developer's direction recorded
as a deference decision, the requirements it revises or adds stamped
`by deference`, and the work reviewed at Done.

1. The gates bind to the commitment: A1, A2 (an escalation counts
   only when raised after the commitment's activation, or when its
   Concerns line names the commitment's changed identifiers and it
   sits on the commitment's own history), B1 (`began^..HEAD` for the
   footprint when the activation has a parent), B2 (compare the
   activation's Agreed set, so a demoted or deleted requirement is a
   change), B3 (the include file is any root file whose whole content
   is `@AGENTS.md`; the gate compares every such file), B4 (a
   superseded promotion no longer satisfies the Promotes check, and
   the repair says the promotion was reversed). D2 revises the
   LOOP-090 and LOOP-092 falsifiers to the Concerns reading. Contract
   change: D2.
2. Records in every shape: A5, A6 (list-form `Concerns:`, `Rests on:`,
   `Promoted from:` read as the flat form), A7 (`examined: []` is
   empty), A8 (`Specified from:`, `Promoted from:` and `--promotes`
   accept a filename, a path or a backticked slug), A9 (directory
   reads carry the record tag), A10 (lint checks the directory and
   prints one line, exit 3, for a missing one; extra positionals
   refused, D12), A11 (the lock read in `finally` is untagged), A12
   (renames parsed), A13 (an empty slug refused), A14 (`Current:`
   counted outside fences), A15 (the last `Status: <word>` line), A4
   (an input is refused when the paths it matches under HEAD include
   any under .cairn/evidence/, whatever its spelling), B5 (a
   Consequential record whose queue entry never appears in history is
   a repair naming the entry), B10 (check's message names the
   record too). D10 fixes the DEC-016 message. No contract change.
3. The hooks find the kernel and the project: C1 (resolve `cairn`
   through PATH, then the fixed link; report a wrapper or copy as
   "another kernel" and judge with it when it is a cairn.mjs, or say
   once that the hook judges with its own), C2 (replace only a link
   whose target does not exist), C3 (walk up from cwd to the nearest
   docs/spec/roadmap.md under the toplevel), C4 (the link block and
   the verdict in separate tries), C5 (a kernel that prints no
   verdict line makes the stop hook print one stderr line and allow,
   and session-start print one line, not the trace), C6, C7 (a
   guarded `parseArgs` import so the version message is reachable;
   README says Node 18.3 and Git 2.15 once verified). Contract
   change: none; PKG-021's text "the command link on the path" is
   met more literally.
4. Lints and tests observe what they name: B7 (skills/ declared for
   spec-lint; PKG-028's rationale says the checker's own code is
   outside a consumer's footprint, as the kernel is), B8 (the pkg-lint
   reads recorded in the review and PKG-024's rationale), E1 (the
   coverage scan matches any `../` string and any relative import;
   the root listing removed; the review corrected), E2 (retitle or
   strengthen the fourteen tests), E3 (tests for the branches
   listed), F1 (the set extended to `[ ] ( ) { } +`, and SPEC-028's
   text names it), F2 (`[A-Za-z]:[\\/]` and `file:/`), F3
   (whitespace collapsed before matching), F4, F5, F6 (probe `.out`
   and `.err` names too), D13 (PKG-025/026 say "the mechanism that
   runs the test suite"). Contract change: SPEC-028's text, PKG-025
   and PKG-026's wording, PKG-004's falsifier (D10).
5. The documents say what the code does: D1 (stamp the 37 lines `by
   deference audit-found-contract-defects-are-repaired-on-the-
   developer-s-direction`, fix overview.md:83-86, add the marker to
   next-iteration Stage 2), B6 (either `decide --promotes` records at
   Consequential and AGENTS.md says so, or the developer's move
   becomes a search for `by promotion`; the auditor recommends the
   first, since a promotion adds a requirement the developer never
   read, which is what the queue is for), B9 (AGENTS.md: remove the
   record before escalating; the answer's wake asks for it again),
   D3, D4, D5 (AGENTS.md's declaration format and the manual gain
   `cwd:`), D6, D7, D8 (a "Built as" delta in the plan's Result
   section; the audit's Remediation paragraph corrected), D9, D10
   (the split sentences and the falsifier wordings), D11, D12, E4
   (noted in the candidate test). Contract change: B6 (AGENTS.md,
   LOOP-088), B9 and D5 (AGENTS.md), D10 (LOOP-105, LOOP-108,
   PKG-022, LOOP-118, PKG-028, SPEC-029, LOOP-026).

Resolved after this report was recorded, under the current
commitment as an open finding of its review: A1 and A2 (commit
c356a46; both gates now read only the escalations the commitment's
own commits added, with two tests). Group 1 no longer includes them.

Left as designed, with the reason visible: the `record` gate blocks
the stop hook when the developer's own working tree has uncommitted
edits to a declared input (the first audit's R1); a stray slug-named
file under docs/decisions/ is a record by the slug rule (F7); the
text-proxy tests (E5).
